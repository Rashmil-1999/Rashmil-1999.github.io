dist/
node_modules/
.astro/
coverage/
.claude/
package-lock.json

# Design handoff bundle — reference prototype kept byte-exact, not shipped code.
docs/design_handoff_portfolio_refresh/

# Editor workspace settings — co-installed by users' editor extensions. Not load-bearing
# for the build; formatting churn would create noisy diffs for unrelated changes.
.vscode/
