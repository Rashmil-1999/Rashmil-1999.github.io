---
phase: 03-sections-navigation
plan: 06
type: execute
wave: 3
depends_on:
    - 03-01
    - 03-02
    - 03-03
    - 03-04
    - 03-05
files_modified:
    - src/components/SideNav.astro
    - src/layouts/BaseLayout.astro
autonomous: true
requirements:
    - SECTION-01
    - SECTION-09
    - NAV-01
    - NAV-03
    - NAV-04
    - NAV-05
    - STYLE-01
must_haves:
    truths:
        - 'SideNav renders 7 anchor links from src/content/links.yaml (about, education, work, skills, projects, leadership, testimonials) in ascending order — sourced from getCollection("links") + sort by order.'
        - 'Desktop (>= 768px): fixed-left sidebar with profile photo on top, vertical nav list beneath.'
        - 'Mobile (< 768px): sticky top bar with brand text ("Rashmil Panchani") + hamburger button; tapping hamburger toggles aria-expanded on the button and visibility of #sidenav-list; Escape key closes the list; clicking a link closes the list.'
        - 'BaseLayout contains a single vanilla <script> implementing IntersectionObserver scroll-spy that toggles aria-current="page" on the matching nav link when its section is most-intersecting; no React island, no jQuery, no useEffect.'
        - 'CSS scroll-behavior: smooth (from Plan 03-02 global.css) provides native smooth scrolling on anchor click; prefers-reduced-motion: reduce disables it.'
        - 'index.astro composition unchanged (Phase 1 D-07 locked) but renders the full filled site; grep -r "client:load\\|client:visible\\|client:idle" src/components/ returns zero hits (SECTION-09); zero CDN refs in dist.'
    artifacts:
        - path: 'src/components/SideNav.astro'
          provides: 'SideNav with desktop fixed sidebar + mobile sticky top bar + hamburger toggle + 7 anchor links from links.yaml; data-section-link attributes for scroll-spy script consumption'
          contains: "getCollection('links')"
          min_lines: 50
        - path: 'src/layouts/BaseLayout.astro'
          provides: 'BaseLayout with scroll-spy <script> emitting IntersectionObserver that toggles aria-current on data-section-link nodes'
          contains: 'IntersectionObserver'
          min_lines: 25
    key_links:
        - from: 'src/components/SideNav.astro'
          to: 'src/content/links.yaml (7 entries via getCollection)'
          via: "await getCollection('links') + sort by order"
          pattern: "getCollection\\('links'"
        - from: 'src/components/SideNav.astro nav links'
          to: 'data-section-link="<id>" attribute consumed by IntersectionObserver in BaseLayout'
          via: 'inline attribute on each <a> element'
          pattern: 'data-section-link='
        - from: 'src/layouts/BaseLayout.astro <script>'
          to: 'main section[id] queries via document.querySelectorAll'
          via: 'IntersectionObserver with rootMargin -30% 0px -50% 0px and threshold 0'
          pattern: 'IntersectionObserver'
---

<objective>
Fill the last Phase 1 stub (`SideNav.astro`) with a desktop fixed-left sidebar + mobile sticky top bar + hamburger toggle, sourced from `src/content/links.yaml`. Add the single vanilla `<script>` block to `src/layouts/BaseLayout.astro` (per D-30 / RESEARCH.md Open Question 1 — BaseLayout chosen over SideNav so every future page inherits scroll-spy without re-wiring) implementing IntersectionObserver for scroll-spy and the hamburger toggle handler (NAV-03 + NAV-04). Run the final Phase 3 verification: all 5 ROADMAP success criteria pass, including the SECTION-09 grep gate scoped to `src/components/` (the hydration fixture at `src/pages/hydration-test.astro` is a separate legitimate `client:load` site that does NOT regress this phase's gate).

Purpose: Close out Phase 3 with the navigation chrome + scroll-spy interactivity + final phase-wide verification. Wave 3 plan; depends on Wave 2 (section components must be filled before scroll-spy can target real section landmarks; SideNav.astro is the last component change).
Output: Filled `SideNav.astro` (~50-70 lines), modified `BaseLayout.astro` (adds ~25-35 lines of `<script>` block after `<slot />`), plus final phase-wide verification capturing all 5 ROADMAP Phase 3 success criteria.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sections-navigation/03-CONTEXT.md
@.planning/phases/03-sections-navigation/03-RESEARCH.md
@.planning/phases/03-sections-navigation/03-PATTERNS.md
@.planning/phases/03-sections-navigation/03-UI-SPEC.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/SideNav.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/layouts/BaseLayout.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content/links.yaml
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/pages/index.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts
</context>

<tasks>

<task type="auto">
    <name>Task 1: Fill SideNav.astro (desktop fixed sidebar + mobile sticky top bar + hamburger + 7 nav anchors from links.yaml + mobile toggle script)</name>
    <files>src/components/SideNav.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/SideNav.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content/links.yaml (7 entries: about, education, work, skills, projects, leadership, testimonials with order 10..70)
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/SideNav.astro" lines ~35-163 — imports pattern, original CRA Navbar.jsx markup, Astro port pattern, mobile toggle script pattern, diff from CRA listing 4 changes)
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pattern 5 lines 616-708; Pattern 6 IntersectionObserver lines 710-751 — informs Task 2)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-08 desktop fixed sidebar; D-09 mobile sticky top bar + inline collapse; D-10 md breakpoint; D-11 profile photo desktop-only + mobile brand text; D-12 3-signal active state via CSS in global.css; D-13 hamburger ARIA wiring; D-14 sidenav id="sidenav" not in links.yaml — chrome not destination; D-29 zero CDN)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (Copywriting Contract rows "Hamburger button ARIA label", "Mobile brand text"; Interaction Contract rows "Mobile hamburger toggle", "Mobile nav Escape")
    </read_first>
    <action>
        Replace the entire contents of `src/components/SideNav.astro` with the RESEARCH.md Pattern 5 / PATTERNS.md SideNav template.

        Front matter:
        - Import `getCollection` from `astro:content`.
        - Import `Image` from `astro:assets`.
        - Import `profileImage` from `../assets/profile.jpg`.
        - Optionally import `getEntry` to read brand text from `about.yaml` — but per UI-SPEC Copywriting Contract row "Mobile brand text" the literal string "Rashmil Panchani" is hard-coded in the Copywriting Contract; either path is acceptable. Default: read from `about.yaml` via `getEntry('about', 'about')` so a future name change in YAML propagates without touching SideNav — this preserves the CMS-neutral contract from Phase 2.
        - Read links: `const linkEntries = await getCollection('links');` then `linkEntries.sort((a, b) => a.data.order - b.data.order);`

        Template body (`<nav id="sidenav" ...>`):
        1. Outer `<nav id="sidenav" class="sidenav" aria-label="Section navigation">` — `id="sidenav"` per Phase 1 D-23 lowercase lock (NOT CRA `sideNav` mixed-case); `aria-label="Section navigation"` per A11Y-01.

        2. Mobile top bar (visible < md per D-09 + D-10 + D-11):
           - `<div class="mobile-bar flex items-center justify-between md:hidden">` — `md:hidden` hides on desktop.
           - `<span class="brand">Rashmil Panchani</span>` (or `<span class="brand">{about.first_name} {about.last_name}</span>` if executor reads about.yaml).
           - `<button type="button" id="sidenav-toggle" aria-label="Toggle navigation menu" aria-controls="sidenav-list" aria-expanded="false" class="hamburger">` containing `<span aria-hidden="true">☰</span>` per D-13 (the UTF-8 hamburger glyph is decorative; the aria-label provides the accessible name; the aria-hidden wrapper prevents AT from reading the literal glyph per CLAUDE.md § 5 "Emoji as content"). Close `</button>`. Close `</div>`.

        3. Desktop sidebar header (visible >= md per D-11):
           - `<div class="sidenav-header hidden md:block">` — `hidden md:block` shows only on desktop.
           - `<Image src={profileImage} alt={about ? `${about.first_name} ${about.last_name}` : 'Rashmil Panchani'} class="profile-pic mx-auto rounded-full" width={200} height={225} />` — the `.profile-pic` class is defined in global.css (Plan 03-02 — width 200, height 225, object-fit cover).
           - Close `</div>`.

        4. Nav links `<ul>` (collapsed on mobile when `aria-expanded="false"`; always visible on desktop):
           - `<ul id="sidenav-list" role="list" class="sidenav-list hidden md:block">` — `hidden md:block` makes the list invisible on mobile by default (toggled visible by the hamburger script); always visible on desktop (`md:block` overrides).
           - `{linkEntries.map((entry) => (<li><a href={`#${entry.data.id}`} class="nav-link block" data-section-link={entry.data.id}>{entry.data.label}</a></li>))}` — `href` is `#about`, `#education`, etc. (Phase 2 D-06 ids); `data-section-link` attribute is the hook the scroll-spy script (Task 2) uses to find the right anchor to toggle `aria-current="page"` on; `.nav-link` class is defined in global.css (Plan 03-02 — handles the 3-signal active state via `.nav-link[aria-current='page']`).
           - Close `</ul>`.

        5. Close `</nav>`.

        Mobile toggle script (per PATTERNS.md "Mobile toggle script pattern" lines 130-156 — appended as a separate `<script>` block at the bottom of the file, after the front matter closing `---` and after the `<nav>...</nav>` template):
        - `const toggle = document.getElementById('sidenav-toggle');`
        - `const list = document.getElementById('sidenav-list');`
        - Guard: `if (toggle && list) { ... }`
        - Click handler: toggle `aria-expanded` between `'true'`/`'false'` and toggle the `hidden` class on the list.
        - Escape key handler (document-level): if list is open, close it and refocus the toggle button per UI-SPEC Interaction Contract "Mobile nav Escape".
        - Close-on-link-click handler: when any `<a>` inside `#sidenav-list` is clicked AND viewport is `< md`, close the list (per RESEARCH.md Pattern 5 lines 697-705 — "Mobile nav doesn't collapse on link click" UX bug avoided).

        Astro `<script>` is bundled and deferred by default; placing it inside SideNav.astro is equivalent to placing it at the bottom of `<body>` after Astro builds the page.

        Do NOT add `client:load` / `client:visible` / `client:idle` (SideNav is a static `.astro` component; the `<script>` is plain JS, not a React island).
        Do NOT import jQuery, react-scroll, or any nav-related library (NAV-05 / Pitfall 21 explicitly forbid jQuery; CONCERNS.md flags `react-scroll` as dead dep).
        Do NOT use Bootstrap classes (`navbar`, `navbar-expand-lg`, `navbar-dark`, `navbar-brand`, `navbar-toggler`, `data-toggle="collapse"`, `data-target=`, `collapse navbar-collapse`) — these are the CRA Navbar.jsx leftovers; Phase 3 ports the SHAPE, not the classes.
        Do NOT add the IntersectionObserver scroll-spy here — that lives in BaseLayout per D-30 / RESEARCH.md Open Question 1 (Task 2 of this plan).
        Do NOT include SideNav itself in `links.yaml`-derived anchors — `id="sidenav"` is chrome per D-14; only the 7 destination sections appear as nav entries.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getCollection('links'" src/components/SideNav.astro &amp;&amp; \
              grep -q "from 'astro:assets'" src/components/SideNav.astro &amp;&amp; \
              grep -q 'profile.jpg' src/components/SideNav.astro &amp;&amp; \
              grep -q 'id="sidenav"' src/components/SideNav.astro &amp;&amp; \
              grep -q 'aria-label="Section navigation"' src/components/SideNav.astro &amp;&amp; \
              grep -q 'aria-label="Toggle navigation menu"' src/components/SideNav.astro &amp;&amp; \
              grep -q 'aria-controls="sidenav-list"' src/components/SideNav.astro &amp;&amp; \
              grep -qE 'aria-expanded=\\?"false\\?"' src/components/SideNav.astro &amp;&amp; \
              grep -q 'id="sidenav-toggle"' src/components/SideNav.astro &amp;&amp; \
              grep -q 'id="sidenav-list"' src/components/SideNav.astro &amp;&amp; \
              grep -q 'data-section-link' src/components/SideNav.astro &amp;&amp; \
              grep -q 'class="nav-link' src/components/SideNav.astro &amp;&amp; \
              grep -q 'md:hidden' src/components/SideNav.astro &amp;&amp; \
              grep -q 'hidden md:block' src/components/SideNav.astro &amp;&amp; \
              grep -q "e.key === 'Escape'" src/components/SideNav.astro &amp;&amp; \
              grep -q 'matchMedia' src/components/SideNav.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/SideNav.astro &amp;&amp; \
              ! grep -qE 'navbar-expand|navbar-toggler|data-toggle="collapse"|jquery|react-scroll' src/components/SideNav.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: file imports `getCollection` from `astro:content`, `Image` from `astro:assets`, and `profileImage` from `../assets/profile.jpg`.
        - Source: file calls `await getCollection('links')` and sorts by `a.data.order - b.data.order`.
        - Source: file contains `<nav id="sidenav" class="sidenav" aria-label="Section navigation">` (Phase 1 D-23 lowercase id lock; NAV-01).
        - Source: file contains mobile top bar (`md:hidden`) with brand text "Rashmil Panchani" (literal or from about.yaml) AND hamburger `<button type="button" id="sidenav-toggle" aria-label="Toggle navigation menu" aria-controls="sidenav-list" aria-expanded="false">` (NAV-04 + D-13).
        - Source: file contains desktop sidebar header (`hidden md:block`) with `<Image>` profile photo (D-11).
        - Source: file contains `<ul id="sidenav-list" role="list" class="... hidden md:block">` followed by `linkEntries.map(...)` rendering `<a href="#<id>" class="nav-link block" data-section-link={entry.data.id}>` per link entry (NAV-01).
        - Source: file contains a `<script>` block implementing the click handler (toggle aria-expanded + hidden class), Escape handler (close + focus toggle), and close-on-link-click handler with `matchMedia('(max-width: 767px)')`.
        - Source: file does NOT contain Bootstrap classes (`navbar`, `navbar-expand`, `navbar-toggler`, `data-toggle="collapse"`, `data-target=`) — NAV-05 + STYLE-01.
        - Source: file does NOT contain imports from `jquery`, `react-scroll`, or any React library — NAV-05 + Pitfall 21.
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle` — SECTION-09.
        - Source: file does NOT include the sidenav id as a nav anchor (SideNav is chrome per D-14; only the 7 section ids appear as `<a href="#...">`).
    </acceptance_criteria>
    <done>
        SideNav renders desktop sidebar + mobile top bar + 7 nav links from links.yaml + working hamburger toggle script.
    </done>
</task>

<task type="auto">
    <name>Task 2: Add IntersectionObserver scroll-spy <script> to BaseLayout.astro after <slot /></name>
    <files>src/layouts/BaseLayout.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/layouts/BaseLayout.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/layouts/BaseLayout.astro" lines ~670-735 — current shape, scroll-spy script pattern, placement recommendation BaseLayout over SideNav)
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pattern 6 lines 710-751 — full IntersectionObserver script; Pattern 7 lines 757-782 — BaseLayout additions; Open Question 1 — BaseLayout chosen)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-30 single vanilla <script> + IntersectionObserver against main section[id]; D-31 rootMargin -30% 0px -50% 0px + threshold 0)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (Interaction Contract row "Scroll-spy")
    </read_first>
    <action>
        Edit `src/layouts/BaseLayout.astro` surgically. Current file is 24 lines: front matter imports BaseHead + global.css + declares Props; template wraps `<html><head><BaseHead/></head><body><slot /></body></html>`.

        Append a `<script>` block AFTER `<slot />` and BEFORE `</body>`. The script content per RESEARCH.md Pattern 6 lines 713-750:

        - `const sections = document.querySelectorAll('main section[id]');` — selects every `<section id="...">` inside `<main>` (which excludes `<nav id="sidenav">` since SideNav is OUTSIDE `<main>` per `src/pages/index.astro` Phase 1 D-07 composition).
        - `const navLinks = document.querySelectorAll('[data-section-link]');` — selects the 7 anchors set up by SideNav Task 1.
        - Build a `Map` from section id → nav link node: `const linkMap = new Map(); navLinks.forEach((link) => { const id = link.getAttribute('data-section-link'); if (id) linkMap.set(id, link); });`
        - Create the IntersectionObserver with `rootMargin: '-30% 0px -50% 0px'` + `threshold: 0` per D-31 default. On intersection:
          - Filter entries to those with `isIntersecting === true`.
          - Sort by `intersectionRatio` descending; pick the top one.
          - If found, get its `target.id` and walk `linkMap`: set `aria-current="page"` on the matching link; `removeAttribute('aria-current')` on all others.
        - Observe every section: `sections.forEach((section) => observer.observe(section));`

        Reasoning notes:
        - D-30 places the script in BaseLayout (not SideNav) per RESEARCH.md Open Question 1 — runs once per page load, single mount, gives every future page free scroll-spy.
        - The hamburger toggle script lives in SideNav per Task 1 (nav-related concern co-located).
        - Astro `<script>` is bundled and deferred by default; placing it after `<slot />` ensures the DOM exists when it runs.

        Do NOT use `useEffect`, `addEventListener('DOMContentLoaded')`, or any React/jQuery-flavored API — vanilla DOM only per D-30 + Pitfall 21.
        Do NOT add a `client:load` directive to BaseLayout (it's an Astro layout, not a component).
        Do NOT duplicate the mobile hamburger toggle here — that already lives in SideNav per Task 1; keeping them separate makes layer responsibilities clear (BaseLayout = scroll-spy; SideNav = nav state).
        Do NOT modify the front matter imports (BaseHead + global.css).
        Do NOT modify the `<html><head>` block.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "IntersectionObserver" src/layouts/BaseLayout.astro &amp;&amp; \
              grep -q "data-section-link" src/layouts/BaseLayout.astro &amp;&amp; \
              grep -q "'aria-current'\|'aria-current', 'page'" src/layouts/BaseLayout.astro &amp;&amp; \
              grep -q "main section\[id\]" src/layouts/BaseLayout.astro &amp;&amp; \
              grep -qE "rootMargin:\s*['\"]-30%" src/layouts/BaseLayout.astro &amp;&amp; \
              grep -q "&lt;slot" src/layouts/BaseLayout.astro &amp;&amp; \
              grep -q "BaseHead" src/layouts/BaseLayout.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/layouts/BaseLayout.astro &amp;&amp; \
              ! grep -qE 'useEffect|jQuery|\$\(' src/layouts/BaseLayout.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `src/layouts/BaseLayout.astro` retains its existing front matter (imports BaseHead + global.css; declares Props interface; destructures title from Astro.props).
        - Source: file retains the `<html lang="en"><head><BaseHead title={title} /></head><body>...<slot />...</body></html>` skeleton.
        - Source: a `<script>` block exists AFTER `<slot />` and BEFORE `</body>`.
        - Source: `<script>` block contains `document.querySelectorAll('main section[id]')` (NAV-03 scoped to `<main>`).
        - Source: `<script>` block contains `document.querySelectorAll('[data-section-link]')` (matches SideNav's anchor attribute).
        - Source: `<script>` block creates a `new IntersectionObserver(...)` with `rootMargin: '-30% 0px -50% 0px'` and `threshold: 0` per D-31.
        - Source: `<script>` block calls `setAttribute('aria-current', 'page')` on the matching link and `removeAttribute('aria-current')` on others.
        - Source: `<script>` block does NOT use `useEffect`, jQuery (`$(`), or any React import.
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
    </acceptance_criteria>
    <done>
        BaseLayout emits a single vanilla scroll-spy <script> block; targets sections inside <main>; toggles aria-current on data-section-link anchors.
    </done>
</task>

<task type="auto">
    <name>Task 3: Final Phase 3 verification — run all 5 ROADMAP success criteria gates</name>
    <files></files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.planning/ROADMAP.md (Phase 3 § Success Criteria — 5 SCs)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/SideNav.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/layouts/BaseLayout.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/pages/index.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/package.json
    </read_first>
    <action>
        Run the full Phase 3 final-verification gate. This is the load-bearing close-out check that proves Phase 3 satisfies all 5 ROADMAP success criteria + every requirement in this plan's frontmatter.

        1. Clean rebuild: `rm -rf dist .astro && npm run build` — must exit 0.

        2. **SC #1 — All 8 sections render with real content** (visiting `npm run preview` shows About / Education / Work / Skills / Projects / Leadership / Testimonials + SideNav with collection data):
           - `grep -c 'id="about"\\|id="education"\\|id="work"\\|id="skills"\\|id="projects"\\|id="leadership"\\|id="testimonials"\\|id="sidenav"' dist/index.html` returns 8.
           - `grep -c 'Rashmil_Panchani.pdf' dist/index.html` ≥ 1 (About CTA).
           - `grep -c '<blockquote' dist/index.html` ≥ 1 (Testimonials).
           - `grep -c 'data-section-link' dist/index.html` ≥ 7 (SideNav 7 anchors).
           - `grep -c '<svg' dist/index.html` ≥ 20 (Skills + social icons bundled).

           Manual visual check (executor opens `npm run preview` http://localhost:4321/ and confirms): all 8 sections show real content; SideNav shows 7 links + profile photo on desktop; mobile top bar appears at < 768px viewport with brand text + hamburger.

        3. **SC #2 — Smooth scroll + scroll-spy + reduced-motion + mobile toggle**:
           - CSS gate (smooth scroll): `grep -q 'scroll-behavior: smooth' src/styles/global.css` (Plan 03-02 already verified).
           - CSS gate (reduced-motion): `grep -q 'prefers-reduced-motion: reduce' src/styles/global.css`.
           - Script gate (scroll-spy): `grep -q 'IntersectionObserver' src/layouts/BaseLayout.astro` (Task 2 verified).
           - Script gate (mobile toggle): `grep -q "aria-expanded" src/components/SideNav.astro` (Task 1 verified).

           Manual interactive check (in `npm run preview`): click each side-nav link, confirm smooth scroll; scroll the page, confirm aria-current toggles via DevTools inspection of the active link; tab through nav, confirm focus rings visible; resize to < 768px, click hamburger, confirm aria-expanded toggles and the list shows/hides; press Escape, confirm list closes.

        4. **SC #3 — Zero React runtime on section components**:
           - `grep -rE 'client:(load|visible|idle)' src/components/` exits 1 (zero hits in `src/components/`).
           - NOTE: The Phase 1 hydration fixture at `src/pages/hydration-test.astro` does carry `client:load` — that file is OUT OF SCOPE for this gate (it lives in `src/pages/`, NOT `src/components/`). The grep MUST be scoped to `src/components/` per Phase 3 SC #3 wording ("on section components"). This is a deliberate scope deviation from the looser ROADMAP wording — document it inline.
           - `dist/_astro/` still contains a React JS chunk (from the hydration fixture). The smoke test asserts this chunk's presence; SC #3 says "dist/_astro/ contains no React chunk for the home page" — strictly interpreted, the home page (`dist/index.html`) does not embed any `<script type="module" src="/_astro/...react...">` reference. Verify: `grep -E '/_astro/[^"]*\\.js' dist/index.html | grep -i react` exits 1 (zero matches in HOME page HTML; chunks may still exist in _astro/ for other routes).

        5. **SC #4 — Zero CDN UI dependencies; no bootstrap package**:
           - `grep -rE 'fontawesome|iconify\\.design|devicon\\.com|cdnjs\\.cloudflare|stackpath|jsdelivr|fonts\\.googleapis|fonts\\.gstatic' dist/ 2>/dev/null` exits 1 (zero matches).
           - `! grep -q '"bootstrap"' package.json` (bootstrap NOT installed — STYLE-01).
           - `! grep -q 'fontawesome\\|stackpath\\|cdnjs' src/components/SideNav.astro src/components/About.astro src/components/Skills.astro src/components/Projects.astro src/components/Testimonials.astro src/components/Education.astro src/components/Work.astro src/components/Leadership.astro src/components/BaseHead.astro src/layouts/BaseLayout.astro` (zero CDN refs in source).

        6. **SC #5 — Total home-page image weight < 500 KB** (Plan 03-05 already verified; re-run here for final attestation):
           - `TOTAL=$(expr $(du -b dist/index.html | cut -f1) + $(find dist/_astro -maxdepth 1 \\( -name '*.webp' -o -name '*.avif' -o -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' \\) -exec du -b {} + 2>/dev/null | awk '{sum+=$1} END {print sum+0}'))` ; `[ "$TOTAL" -lt 512000 ]`.

        7. **Phase 1 smoke regression** — `npm test` exits 0 (all 5 assertions still pass: section ids, hydration fixture, JS chunk, Tailwind marker).

        8. **Phase 2 content-validation regression** — `npm test` covers both files; content-validation tests should still pass since Phase 3 did not change schemas or fixtures.

        If any gate fails, document the failure in the SUMMARY and surface as a blocker. Do NOT silently work around an SC failure by relaxing a gate.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              rm -rf dist .astro &amp;&amp; \
              npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; \
              for ID in sidenav about education work skills projects leadership testimonials; do grep -q "id=\"$ID\"" dist/index.html || { echo "MISSING id=\"$ID\""; exit 1; }; done &amp;&amp; \
              grep -q 'Rashmil_Panchani.pdf' dist/index.html &amp;&amp; \
              grep -q '&lt;blockquote' dist/index.html &amp;&amp; \
              [ "$(grep -c 'data-section-link' dist/index.html)" -ge 7 ] &amp;&amp; \
              grep -q 'IntersectionObserver' src/layouts/BaseLayout.astro &amp;&amp; \
              grep -q 'scroll-behavior: smooth' src/styles/global.css &amp;&amp; \
              grep -q 'prefers-reduced-motion: reduce' src/styles/global.css &amp;&amp; \
              ! grep -rE 'client:(load|visible|idle)' src/components/ 2&gt;/dev/null &amp;&amp; \
              ! grep -E '/_astro/[^"]*\.js' dist/index.html | grep -qi react &amp;&amp; \
              ! grep -rE 'fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr|fonts\.googleapis|fonts\.gstatic' dist/ 2&gt;/dev/null &amp;&amp; \
              ! grep -q '"bootstrap"' package.json &amp;&amp; \
              TOTAL=$(expr $(du -b dist/index.html | cut -f1) + $(find dist/_astro -maxdepth 1 \( -name '*.webp' -o -name '*.avif' -o -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' \) -exec du -b {} + 2&gt;/dev/null | awk '{sum+=$1} END {print sum+0}')) &amp;&amp; \
              echo "Total home-page image+html weight: ${TOTAL} bytes" &amp;&amp; \
              [ "$TOTAL" -lt 512000 ] &amp;&amp; \
              npm test 2&gt;&amp;1 | grep -qE "passed"
        </automated>
    </verify>
    <acceptance_criteria>
        - Behavior (SC #1): `dist/index.html` contains all 8 section ids (sidenav, about, education, work, skills, projects, leadership, testimonials); `Rashmil_Panchani.pdf` referenced; `<blockquote` present; ≥ 7 `data-section-link` attributes.
        - Behavior (SC #2): `src/styles/global.css` contains `scroll-behavior: smooth` AND `prefers-reduced-motion: reduce`; `src/layouts/BaseLayout.astro` contains `IntersectionObserver`; `src/components/SideNav.astro` contains `aria-expanded`.
        - Behavior (SC #3): `grep -rE 'client:(load|visible|idle)' src/components/` exits 1; `dist/index.html` does not reference any React-prefixed JS chunk under `/_astro/`.
        - Behavior (SC #4): `grep -rE "fontawesome|iconify\\.design|devicon\\.com|cdnjs\\.cloudflare|stackpath|jsdelivr|fonts\\.googleapis|fonts\\.gstatic" dist/` exits 1; `package.json` does NOT contain `"bootstrap"`.
        - Behavior (SC #5): Total of `du -b dist/index.html` plus all `dist/_astro/*.{webp,avif,png,jpg,jpeg}` byte sizes is < 512000 (500 KB).
        - Behavior: `npm test` exits 0 — Phase 1 smoke + Phase 2 content-validation suites green.
        - Behavior: `npx astro check` exits with 0 errors (warnings/hints at baseline 38 from Phase 2 still acceptable).
    </acceptance_criteria>
    <done>
        All 5 ROADMAP Phase 3 success criteria pass; Phase 1 + Phase 2 regression suites green; Phase 3 is ready for `/gsd-transition` to Phase 4.
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vanilla &lt;script&gt; in BaseLayout → browser DOM | Build-time emitted JS runs on every page load |
| SideNav links.yaml data → rendered nav anchors | Build-time read of validated Zod-typed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | XSS via injected attribute in nav link rendering | mitigate | All nav data sourced from Phase 2 Zod-validated links.yaml; entry.data.id passes the regex `^[a-z][a-z0-9-]*$` per Phase 2 D-06; no user-provided content; Astro auto-escapes interpolation. |
| T-03-19 | Tampering | jQuery/CDN script regression | mitigate | NAV-05 explicit prohibition; grep gate in Task 3 enforces zero CDN refs in dist; STYLE-01 enforces no bootstrap package. |
| T-03-20 | Denial of Service | IntersectionObserver runs unbounded on every scroll | accept | Browser-native API designed for scroll-spy; rootMargin filters events; 30 lines of code with no heap growth. Performance baseline is "well below any user-visible threshold". |
| T-03-21 | Tampering | Mobile toggle script DOM injection | accept | Script only toggles classes + ARIA attributes; no innerHTML manipulation; no `eval`. |
</threat_model>

<verification>
After all 3 tasks complete, the full Phase 3 verification gate (Task 3 automated block) is the single source of truth. Additional sanity:

1. `wc -l src/components/SideNav.astro` returns ≥ 50.
2. `wc -l src/layouts/BaseLayout.astro` returns ≥ 25 (was 24; grows by ~25-35 from the script block).
3. `grep -c "getCollection('links'" src/components/SideNav.astro` = 1.
4. `grep -c 'IntersectionObserver' src/layouts/BaseLayout.astro` = 1.
5. `grep -c 'aria-expanded' src/components/SideNav.astro` ≥ 2 (initial false + script toggling).
6. `grep -c 'data-section-link' src/components/SideNav.astro` = 1 (single attribute on the rendered link).
7. `grep -c 'data-section-link' dist/index.html` = 7 (7 nav anchors emitted).
8. `npm run build` exits 0; `npm test` exits 0.
9. Phase 3 SC gates 1-5 all pass per Task 3 automated checks.
</verification>

<success_criteria>
- SideNav.astro renders 7 anchor links from links.yaml + profile photo on desktop + mobile sticky bar with brand text + hamburger + working toggle script.
- BaseLayout.astro emits a single vanilla `<script>` implementing IntersectionObserver scroll-spy (toggling aria-current="page" on the matching nav link).
- All 5 ROADMAP Phase 3 success criteria pass.
- Phase 1 smoke test exits 0 (regression safe).
- Phase 2 content-validation test exits 0 (regression safe).
- `npx astro check` exits with 0 errors.
- Phase 3 closes out cleanly — `/gsd-transition` to Phase 4 ready.
</success_criteria>

<output>
Create `.planning/phases/03-sections-navigation/03-06-SUMMARY.md` documenting:
- Final line counts for SideNav.astro and BaseLayout.astro.
- Output of the full Phase 3 SC gate (Task 3 verify block):
  - SC #1: 8 section ids present.
  - SC #2: smooth-scroll + reduced-motion in global.css; IntersectionObserver in BaseLayout; aria-expanded in SideNav.
  - SC #3: zero client:* hits in src/components/; no React chunk reference in dist/index.html.
  - SC #4: zero CDN refs in dist; no bootstrap in package.json.
  - SC #5: total home-page image weight (bytes) — must record the actual value.
- Confirmation `npm test` and `npx astro check` exit clean.
- Note on the Phase 3 SC #3 scope deviation (`src/components/` vs. `src/`) — surface for sign-off.
