---
phase: 03-sections-navigation
plan: 04
type: execute
wave: 2
depends_on:
    - 03-01
    - 03-02
files_modified:
    - src/components/Education.astro
    - src/components/Work.astro
    - src/components/Leadership.astro
autonomous: true
requirements:
    - SECTION-03
    - SECTION-04
    - SECTION-07
    - SECTION-09
must_haves:
    truths:
        - 'Education section shows 3 entries: name (h3), degree (subheading), graduated date (right column), and optional score paragraph — sourced from src/content/education/*/index.md.'
        - 'Work section shows 2 entries: title (h3), company (subheading), duration (right column), and description body via <Content />.'
        - 'Leadership section shows 1 entry: title (h3), org (subheading), duration (right column), and description body via <Content />.'
        - 'All three sections share the same flat-row layout (NOT card-shadowed) per D-22; date column right on >= md, above on < md.'
        - 'No section component imports react/react-dom or carries client:* directives.'
        - 'Each list section emits <hr class="m-0 border-black/10" /> as its trailing divider per D-03.'
    artifacts:
        - path: 'src/components/Education.astro'
          provides: 'Education section with 3 flat-row entries + optional score field'
          contains: "getCollection('education'"
          min_lines: 30
        - path: 'src/components/Work.astro'
          provides: 'Work section with 2 flat-row entries + markdown body via <Content />'
          contains: "getCollection('work'"
          min_lines: 30
        - path: 'src/components/Leadership.astro'
          provides: 'Leadership section with 1 flat-row entry + markdown body via <Content />'
          contains: "getCollection('leadership'"
          min_lines: 30
    key_links:
        - from: 'src/components/Education.astro'
          to: "getCollection('education') with draft predicate from Phase 2 D-05"
          via: "await getCollection('education', ({data}) => import.meta.env.PROD ? !data.draft : true)"
          pattern: "getCollection\\('education'"
        - from: 'src/components/Work.astro'
          to: "getCollection('work') with draft predicate"
          via: 'same predicate shape'
          pattern: "getCollection\\('work'"
        - from: 'src/components/Leadership.astro'
          to: "getCollection('leadership') with draft predicate"
          via: 'same predicate shape'
          pattern: "getCollection\\('leadership'"
---

<objective>
Fill three Phase 1 list-collection stubs (`Education.astro`, `Work.astro`, `Leadership.astro`) with the same RESEARCH.md Pattern 1 shape — a flat-row layout per entry, date column on the right (or above on < md), title + institution + body on the left, terminated by an `<hr class="m-0 border-black/10" />`.

Purpose: Cover 3 SECTION-* requirements (SECTION-03, SECTION-04, SECTION-07) and the bulk of the "no React on sections" gate (SECTION-09 partial). These three share an identical skeleton; field names are the only difference (Education: `name`/`degree`/`graduated`/optional `score`; Work: `title`/`company`/`duration`; Leadership: `title`/`org`/`duration`). Wave 2 plan; parallel with 03-03 and 03-05.
Output: Three filled `.astro` files following RESEARCH.md Pattern 1, with shared draft predicate, sort-by-order, and markdown body rendering via `<Content />`.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sections-navigation/03-CONTEXT.md
@.planning/phases/03-sections-navigation/03-RESEARCH.md
@.planning/phases/03-sections-navigation/03-PATTERNS.md
@.planning/phases/03-sections-navigation/03-UI-SPEC.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Education.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Work.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Leadership.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts
</context>

<tasks>

<task type="auto">
    <name>Task 1: Fill Education.astro (3 entries; name/degree/graduated/optional score + Content body)</name>
    <files>src/components/Education.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Education.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/Education.astro" lines ~267-348 — imports + collection-read pattern, original CRA row pattern, full Astro port template with `id === rendered.length - 1` conditional class, diff from CRA noting <Content /> addition + text-secondary→text-muted swap)
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pattern 1 lines 354-408)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 trailing hr; D-22 flat-row layout (NOT card-styled); D-23 body via <Content />)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts (education schema: name, degree, graduated, score optional, order, draft)
    </read_first>
    <action>
        Replace the entire contents of `src/components/Education.astro` with the PATTERNS.md Education Astro port template. Structure:

        Front matter:
        - Import `getCollection` and `render` from `astro:content`.
        - Read entries with draft predicate (Phase 2 D-05 inheritance): `const entries = await getCollection('education', ({ data }) => import.meta.env.PROD ? !data.draft : true);`
        - Sort: `entries.sort((a, b) => a.data.order - b.data.order);`
        - Pre-render bodies: `const rendered = await Promise.all(entries.map(async (entry) => ({ entry, Content: (await render(entry)).Content })));` — preserves type inference vs reading body content at render time.

        Template body:
        1. `<section id="education" class="resume-section">` (Phase 1 D-23 id lock).
        2. `<div class="resume-section-content">`.
        3. `<h2 class="mb-5">Education</h2>` (A11Y-02 heading hierarchy).
        4. `{rendered.map(({ entry, Content }, id) => (...))}` — per-entry row with the conditional `mb-5` (omitted on the last entry, preserving the CRA pattern from `b537845:src/components/Education.jsx` lines 13-30):
           - Outer `<div class={ id === rendered.length - 1 ? 'flex flex-col md:flex-row md:justify-between' : 'mb-5 flex flex-col md:flex-row md:justify-between' }>`.
           - Left column `<div class="flex-grow">`:
             - `<h3 class="mb-0">{entry.data.name}</h3>` (school name).
             - `<div class="subheading mb-3">{entry.data.degree}</div>` (uses .subheading class from global.css per Plan 03-02).
             - Optional score: `{entry.data.score && <p>{entry.data.score}</p>}` (score is optional in Phase 2 education schema — the `&&` short-circuit handles undefined safely under strictest tsconfig).
             - `<Content />` (renders the synthesized one-sentence body per Phase 2 D-23 / Plan 02-05 SUMMARY — body is non-empty for all 3 entries).
           - Right column `<div class="flex-shrink-0">`:
             - `<span class="text-muted">{entry.data.graduated}</span>` — `.text-muted` is NOT in global.css yet; it should resolve to `var(--color-muted)` via the `@theme` alias. If Tailwind v4 does not auto-generate `text-muted` from the `--color-muted` token (it should, per CSS-first convention), fall back to `<span style="color: var(--color-muted)">` or add `.text-muted { color: var(--color-muted) }` to global.css. Document the chosen path inline.
        5. Close `</div>` (section-content), `</section>`, then emit `<hr class="m-0 border-black/10" />` per D-03 + Pitfall A.

        Do NOT translate CRA `flex-grow-1` → `flex-grow` blindly without verifying Tailwind v4 emits `flex-grow: 1` for the `flex-grow` class — it does (default), so `flex-grow` is correct.
        Do NOT translate `text-secondary` → `text-muted` without noting the swap inline (PATTERNS.md "Diff from CRA" calls this out; the recovered CRA used Bootstrap `text-secondary`, and Phase 3 standardizes on `text-muted` via the `--color-muted` token).
        Do NOT add `<Content />` outside the left column — body renders inside the title column per CRA pattern.
        Do NOT add `client:load` / `client:visible` / `client:idle`.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getCollection('education'" src/components/Education.astro &amp;&amp; \
              grep -q 'render' src/components/Education.astro &amp;&amp; \
              grep -q 'import.meta.env.PROD' src/components/Education.astro &amp;&amp; \
              grep -q 'id="education"' src/components/Education.astro &amp;&amp; \
              grep -q '&lt;h2.*Education' src/components/Education.astro &amp;&amp; \
              grep -q 'entry.data.name' src/components/Education.astro &amp;&amp; \
              grep -q 'entry.data.degree' src/components/Education.astro &amp;&amp; \
              grep -q 'entry.data.graduated' src/components/Education.astro &amp;&amp; \
              grep -q 'entry.data.score' src/components/Education.astro &amp;&amp; \
              grep -q '&lt;Content' src/components/Education.astro &amp;&amp; \
              grep -q 'rendered.length - 1' src/components/Education.astro &amp;&amp; \
              grep -q 'md:flex-row' src/components/Education.astro &amp;&amp; \
              grep -q '&lt;hr class="m-0 border-black/10"' src/components/Education.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/Education.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: file imports `getCollection` and `render` from `astro:content`.
        - Source: file reads with the draft predicate `({ data }) => import.meta.env.PROD ? !data.draft : true` (Phase 2 D-05).
        - Source: file sorts entries `a.data.order - b.data.order` ascending.
        - Source: file pre-renders bodies via `await Promise.all(entries.map(async (entry) => ({ entry, Content: (await render(entry)).Content })))`.
        - Source: file contains `<section id="education" class="resume-section">` (Phase 1 D-23 id lock).
        - Source: file contains `<h2>` for the Education section heading.
        - Source: file references `entry.data.name`, `entry.data.degree`, `entry.data.graduated`, `entry.data.score`, and includes `<Content />` rendering.
        - Source: file applies the conditional last-row class (`id === rendered.length - 1 ? ...without mb-5 : ...with mb-5`) per the CRA pattern.
        - Source: file does NOT contain Bootstrap classes (`d-flex`, `flex-md-row`, `justify-content-between`, `flex-grow-1`, `text-secondary`).
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
        - Source: file ends with `<hr class="m-0 border-black/10" />` (D-03 + Pitfall A).
    </acceptance_criteria>
    <done>
        Education renders 3 flat rows; score conditional; markdown body via <Content />; trailing hr.
    </done>
</task>

<task type="auto">
    <name>Task 2: Fill Work.astro (2 entries; title/company/duration + Content body) — same skeleton as Education with field swaps</name>
    <files>src/components/Work.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Work.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/Work.astro" lines ~351-369 — same skeleton as Education with explicit field swaps; CRA section id changed from `experience` to `work`)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 trailing hr; D-22 flat-row; D-23 body via Content)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts (work schema: title, company, duration, order, draft)
        @.planning/phases/02-content-layer/02-CONTEXT.md (D-24 canonical id `work` + label `Work` — reconciled from CRA `experience`/`Experience`)
    </read_first>
    <action>
        Reuse the Education.astro skeleton verbatim, applying these field-and-string swaps per PATTERNS.md Work section:

        - `getCollection('education', ...)` → `getCollection('work', ...)`.
        - `<h2>Education</h2>` → `<h2 class="mb-5">Work</h2>` (per Phase 2 D-24 canonical label lock — NOT "Experience").
        - `<section id="education">` → `<section id="work">` (Phase 1 D-23 — NOT "experience"; the existing Work.astro stub already has `id="work"` and a comment about the divergence).
        - Left column field references:
          - `<h3 class="mb-0">{entry.data.name}</h3>` → `<h3 class="mb-0">{entry.data.title}</h3>` (job title).
          - `<div class="subheading mb-3">{entry.data.degree}</div>` → `<div class="subheading mb-3">{entry.data.company}</div>` (company).
          - DELETE the `{entry.data.score && <p>{entry.data.score}</p>}` line — Work schema has no `score` field.
          - Keep `<Content />` for description body (Phase 2 D-19 markdown body).
        - Right column:
          - `<span class="text-muted">{entry.data.graduated}</span>` → `<span class="text-muted">{entry.data.duration}</span>` (work uses `duration` instead of `graduated`).

        Preserve the `id === rendered.length - 1` conditional last-row mb-5 logic verbatim.
        Preserve the draft predicate, sort comparator, and pre-render Promise.all structure verbatim.
        Preserve the trailing `<hr class="m-0 border-black/10" />`.

        Keep the existing stub's comment header noting the id divergence (`id is "work" — diverges from the deleted Work.jsx "experience" id`) — append a Phase 3 note that the canonical label is also `Work`, per Phase 2 D-24.
        Do NOT add `client:load` / `client:visible` / `client:idle`.
        Do NOT reintroduce `Experience` or `experience` anywhere — Phase 1 D-23 + Phase 2 D-24 lock the canonical id/label.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getCollection('work'" src/components/Work.astro &amp;&amp; \
              grep -q 'render' src/components/Work.astro &amp;&amp; \
              grep -q 'import.meta.env.PROD' src/components/Work.astro &amp;&amp; \
              grep -q 'id="work"' src/components/Work.astro &amp;&amp; \
              ! grep -qE 'id="experience"|&gt;Experience&lt;|getCollection..experience' src/components/Work.astro &amp;&amp; \
              grep -q '&gt;Work&lt;' src/components/Work.astro &amp;&amp; \
              grep -q 'entry.data.title' src/components/Work.astro &amp;&amp; \
              grep -q 'entry.data.company' src/components/Work.astro &amp;&amp; \
              grep -q 'entry.data.duration' src/components/Work.astro &amp;&amp; \
              ! grep -q 'entry.data.score' src/components/Work.astro &amp;&amp; \
              grep -q '&lt;Content' src/components/Work.astro &amp;&amp; \
              grep -q '&lt;hr class="m-0 border-black/10"' src/components/Work.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/Work.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: file calls `getCollection('work', ...)` with the standard draft predicate.
        - Source: file contains `<section id="work" class="resume-section">` and does NOT contain `id="experience"` (Phase 1 D-23 + Phase 2 D-24 lock).
        - Source: file contains `<h2>Work</h2>` and does NOT contain the string `Experience` (Phase 2 D-24 canonical label).
        - Source: file references `entry.data.title`, `entry.data.company`, `entry.data.duration` and `<Content />`.
        - Source: file does NOT reference `entry.data.score` (no such field in work schema).
        - Source: file applies the same `id === rendered.length - 1` conditional last-row class.
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
        - Source: file ends with `<hr class="m-0 border-black/10" />`.
    </acceptance_criteria>
    <done>
        Work renders 2 flat rows with title/company/duration + body; no `Experience` regression.
    </done>
</task>

<task type="auto">
    <name>Task 3: Fill Leadership.astro (1 entry; title/org/duration + Content body) — same skeleton as Work with org swap</name>
    <files>src/components/Leadership.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Leadership.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/Leadership.astro" lines ~371-384 — same skeleton as Education/Work with field swap to leadership schema)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 trailing hr; D-22 flat-row layout)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts (leadership schema: title, org, duration, order, draft)
    </read_first>
    <action>
        Reuse the Work.astro skeleton verbatim, applying these field-and-string swaps:

        - `getCollection('work', ...)` → `getCollection('leadership', ...)`.
        - `<h2>Work</h2>` → `<h2 class="mb-5">Leadership</h2>`.
        - `<section id="work">` → `<section id="leadership">` (Phase 1 D-23 id lock).
        - Field references:
          - `{entry.data.title}` (h3) → KEEP same field name (`title` is in both work and leadership schemas).
          - `{entry.data.company}` (subheading) → `{entry.data.org}` (leadership uses `org` instead of `company`).
          - `{entry.data.duration}` (right column) → KEEP same field name (`duration` in both schemas).
          - `<Content />` for description body — KEEP.

        Preserve the draft predicate, sort, pre-render Promise.all, conditional last-row mb-5, and trailing `<hr class="m-0 border-black/10" />` verbatim.

        Do NOT add `client:load` / `client:visible` / `client:idle`.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getCollection('leadership'" src/components/Leadership.astro &amp;&amp; \
              grep -q 'render' src/components/Leadership.astro &amp;&amp; \
              grep -q 'import.meta.env.PROD' src/components/Leadership.astro &amp;&amp; \
              grep -q 'id="leadership"' src/components/Leadership.astro &amp;&amp; \
              grep -q '&gt;Leadership&lt;' src/components/Leadership.astro &amp;&amp; \
              grep -q 'entry.data.title' src/components/Leadership.astro &amp;&amp; \
              grep -q 'entry.data.org' src/components/Leadership.astro &amp;&amp; \
              grep -q 'entry.data.duration' src/components/Leadership.astro &amp;&amp; \
              ! grep -q 'entry.data.company' src/components/Leadership.astro &amp;&amp; \
              grep -q '&lt;Content' src/components/Leadership.astro &amp;&amp; \
              grep -q '&lt;hr class="m-0 border-black/10"' src/components/Leadership.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/Leadership.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: file calls `getCollection('leadership', ...)` with the standard draft predicate.
        - Source: file contains `<section id="leadership" class="resume-section">` (Phase 1 D-23 id lock).
        - Source: file contains `<h2>Leadership</h2>`.
        - Source: file references `entry.data.title`, `entry.data.org` (NOT `entry.data.company`), `entry.data.duration` and `<Content />`.
        - Source: file applies the conditional last-row class (will trigger immediately since there is only 1 entry — the iteration body must still handle the case correctly; the ternary returns the no-mb-5 path).
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
        - Source: file ends with `<hr class="m-0 border-black/10" />`.
    </acceptance_criteria>
    <done>
        Leadership renders 1 flat row with title/org/duration + body; trailing hr.
    </done>
</task>

<task type="auto">
    <name>Task 4: Build + smoke verification — confirm all three sections render expected counts and content</name>
    <files></files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Education.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Work.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Leadership.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts
    </read_first>
    <action>
        Run verification:

        1. `rm -rf dist .astro` (flush cache).
        2. `npm run build` — exit 0 expected.
        3. Confirm rendered output:
           - `grep -c 'id="education"' dist/index.html` = 1 (single section per page).
           - `grep -c 'id="work"' dist/index.html` = 1.
           - `grep -c 'id="leadership"' dist/index.html` = 1.
           - `grep -c 'id="experience"' dist/index.html` = 0 (Phase 1 D-23 / Phase 2 D-24 — no regression to CRA id).
           - Education entry count: depends on rendered output but minimum check is to look for h3 elements inside `id="education"` ≥ 3 (3 schools per Phase 2 Plan 02-05).
           - Work entry count: 2 h3 elements inside `id="work"`.
           - Leadership entry count: 1 h3 element inside `id="leadership"`.
        4. `npm test` — Phase 1 smoke must exit 0 (all 5 assertions).
        5. Grep gates:
           - `grep -E "client:(load|visible|idle)" src/components/Education.astro src/components/Work.astro src/components/Leadership.astro` exits 1 (zero hits).
           - `grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/` exits 1 (zero CDN refs).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              rm -rf dist .astro &amp;&amp; \
              npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; \
              grep -q 'id="education"' dist/index.html &amp;&amp; \
              grep -q 'id="work"' dist/index.html &amp;&amp; \
              grep -q 'id="leadership"' dist/index.html &amp;&amp; \
              ! grep -q 'id="experience"' dist/index.html &amp;&amp; \
              ! grep -E "client:(load|visible|idle)" src/components/Education.astro src/components/Work.astro src/components/Leadership.astro 2&gt;/dev/null &amp;&amp; \
              ! grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/ 2&gt;/dev/null &amp;&amp; \
              npm test 2&gt;&amp;1 | grep -qE "5 (passed|tests passed)"
        </automated>
    </verify>
    <acceptance_criteria>
        - Behavior: `npm run build` exits 0.
        - Behavior: `dist/index.html` contains `id="education"`, `id="work"`, `id="leadership"`.
        - Behavior: `dist/index.html` does NOT contain `id="experience"` (canonical id lock).
        - Behavior: `grep -E "client:(load|visible|idle)" src/components/{Education,Work,Leadership}.astro` exits 1 (zero hits — SECTION-09 enforcement).
        - Behavior: `grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/` exits 1 (zero CDN refs).
        - Behavior: `npm test` exits 0 — Phase 1 smoke green.
    </acceptance_criteria>
    <done>
        Three list-collection sections render; clean build; smoke green; no React/CDN regressions.
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 markdown bodies → rendered HTML | Sanitized markdown via Astro `<Content />` pipeline |
| Phase 2 typed frontmatter → rendered HTML | Zod-validated build-time values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | XSS via markdown body in education/work/leadership | mitigate | Astro `<Content />` sanitizes through remark/rehype pipeline; content authored by project owner only; Phase 2 D-20 ensures non-empty body content for each list entry. |
| T-03-13 | Tampering | Bootstrap class regression (CRA `text-secondary`, `flex-md-row`, etc.) | mitigate | Plan task instructions name every CRA→Tailwind swap explicitly; verify grep gates catch `d-flex`, `text-secondary`. |
</threat_model>

<verification>
After all 4 tasks complete:

1. `wc -l src/components/Education.astro src/components/Work.astro src/components/Leadership.astro` reports ≥ 30 lines each.
2. `grep -c "getCollection('education'" src/components/Education.astro` = 1.
3. `grep -c "getCollection('work'" src/components/Work.astro` = 1.
4. `grep -c "getCollection('leadership'" src/components/Leadership.astro` = 1.
5. `grep -c '<hr' src/components/Education.astro` = 1.
6. `grep -c '<hr' src/components/Work.astro` = 1.
7. `grep -c '<hr' src/components/Leadership.astro` = 1.
8. `grep -E "client:(load|visible|idle)" src/components/Education.astro src/components/Work.astro src/components/Leadership.astro` exits 1.
9. `grep -c 'experience' src/components/Work.astro src/components/Leadership.astro dist/index.html 2>/dev/null` returns 0 (canonical id lock).
10. `npm run build` exits 0.
11. `npm test` exits 0.
</verification>

<success_criteria>
- Education.astro renders 3 flat rows (name/degree/optional score/graduated + body).
- Work.astro renders 2 flat rows (title/company/duration + body); no `experience` regression.
- Leadership.astro renders 1 flat row (title/org/duration + body).
- All three sections terminate with `<hr class="m-0 border-black/10" />`.
- Zero React runtime / zero CDN refs.
- `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-sections-navigation/03-04-SUMMARY.md` documenting:
- Final line counts for the 3 modified components.
- Confirmation `npm run build` exits 0.
- Confirmation `dist/index.html` contains all 3 section ids and zero `id="experience"` references.
- Confirmation `dist/index.html` shows expected entry counts (≥ 3 in education, 2 in work, 1 in leadership — based on rendered h3 counts inside each section).
