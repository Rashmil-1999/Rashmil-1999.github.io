---
phase: 03-sections-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - package.json
    - package-lock.json
    - astro.config.mjs
    - src/components/BaseHead.astro
autonomous: true
requirements:
    - STYLE-02
    - STYLE-05
must_haves:
    truths:
        - 'astro-icon integration is wired into astro.config.mjs and resolves Iconify identifiers at build time without any CDN <script> in output.'
        - 'Fontsource packages are installed so per-weight CSS imports resolve in Wave 2 (Plan 03-02 imports them).'
        - 'astro.config.mjs sets trailingSlash: "always" so anchor links never trigger a 301 redirect hop.'
        - 'BaseHead no longer references fonts.googleapis.com or fonts.gstatic.com — fonts are self-hosted starting in Plan 03-02.'
        - 'npm run build exits 0 after the integration wiring (no scroll-spy, no theme tokens yet — just a clean build with the new integrations registered).'
    artifacts:
        - path: 'package.json'
          provides: 'devDeps for astro-icon + 4 Iconify packs; deps for 2 Fontsource packages'
          contains: 'astro-icon'
        - path: 'astro.config.mjs'
          provides: 'icon() integration registered, trailingSlash set, build.format explicit'
          contains: 'icon()'
        - path: 'src/components/BaseHead.astro'
          provides: 'BaseHead with Google Fonts preconnects deleted'
          contains: '<title>{title}</title>'
    key_links:
        - from: 'astro.config.mjs'
          to: 'astro-icon'
          via: 'import icon from "astro-icon" + integrations: [react(), icon()]'
          pattern: "import icon from 'astro-icon'"
        - from: 'package.json'
          to: '@iconify-json/devicon, @iconify-json/simple-icons, @iconify-json/logos, @iconify-json/lucide'
          via: 'devDependencies'
          pattern: '@iconify-json/(devicon|simple-icons|logos|lucide)'
        - from: 'package.json'
          to: '@fontsource/saira-extra-condensed, @fontsource/mulish'
          via: 'dependencies'
          pattern: '@fontsource/(saira-extra-condensed|mulish)'
---

<objective>
Wire the Phase 3 build-time integrations and dependency manifest so subsequent plans can import the `<Icon>` component, reference Fontsource per-weight CSS files, and emit a build that does not hit any external font CDN.

Purpose: Wave 1 prerequisite for every other Phase 3 plan. Plan 03-02 (Wave 2) cannot import `@fontsource/*` until they exist on disk; Plans 03-03..03-06 (Wave 3) cannot author their files until astro-icon resolves at build time. This plan deliberately leaves visual output unchanged so its failure mode is isolated to "deps + config + BaseHead trim" and not entangled with section-component authoring.
Output: Updated package.json + package-lock.json (npm install ran), updated astro.config.mjs (icon integration, trailingSlash, build.format), trimmed BaseHead.astro (2 preconnect lines deleted).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sections-navigation/03-CONTEXT.md
@.planning/phases/03-sections-navigation/03-RESEARCH.md
@.planning/phases/03-sections-navigation/03-PATTERNS.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/astro.config.mjs
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/BaseHead.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/package.json
</context>

<tasks>

<task type="auto">
    <name>Task 1: Install Phase 3 dependencies (astro-icon, 4 Iconify packs, 2 Fontsource packages)</name>
    <files>package.json, package-lock.json</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/package.json
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (sections "Standard Stack — New", "Package Legitimacy Audit", "Installation")
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-28, D-33, D-34)
    </read_first>
    <action>
        Run the two install commands per RESEARCH.md "Installation" section:
        1. `npm install --save-dev astro-icon@^1.1.5 @iconify-json/devicon@^1.2.62 @iconify-json/simple-icons@^1.2.84 @iconify-json/logos@^1.2.11 @iconify-json/lucide@^1.2.109` — adds 5 build-time devDependencies (per D-28; astro-icon resolves Iconify ids at build, packs are JSON data).
        2. `npm install @fontsource/saira-extra-condensed@^5.2.7 @fontsource/mulish@^5.2.8` — adds 2 runtime dependencies (per D-33, D-34; Fontsource CSS imports get processed by Vite into hashed `.woff2` files in dist/_astro/).

        Use the static `@fontsource/mulish` package (NOT `@fontsource-variable/mulish`) per the planner choice locked in RESEARCH.md Open Question 2 — static per-weight imports match the Saira pattern and the 4-weight subset is bounded.

        Do NOT install `bootstrap`, `react-scroll`, `react-script-tag`, `jquery`, or any Font Awesome / Devicon CDN package — STYLE-01 explicitly forbids; D-29 forbids any CDN icon source.

        Do NOT add a `--legacy-peer-deps` or `--force` flag — if astro-icon's peer fails against Astro 6, surface as a blocker rather than silently downgrading (RESEARCH.md Assumption A3).

        After both installs, verify with `npm ls astro-icon @iconify-json/devicon @iconify-json/simple-icons @iconify-json/logos @iconify-json/lucide @fontsource/saira-extra-condensed @fontsource/mulish` — all 7 should report resolved versions matching the ^-range floors specified above.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              npm ls astro-icon @iconify-json/devicon @iconify-json/simple-icons @iconify-json/logos @iconify-json/lucide @fontsource/saira-extra-condensed @fontsource/mulish 2&gt;/dev/null &amp;&amp; \
              grep -q '"astro-icon"' package.json &amp;&amp; \
              grep -q '"@fontsource/saira-extra-condensed"' package.json &amp;&amp; \
              grep -q '"@fontsource/mulish"' package.json &amp;&amp; \
              ! grep -q '"bootstrap"' package.json &amp;&amp; \
              ! grep -q '"@fontsource-variable/mulish"' package.json
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `package.json` `devDependencies` contains all five keys: `astro-icon`, `@iconify-json/devicon`, `@iconify-json/simple-icons`, `@iconify-json/logos`, `@iconify-json/lucide` with `^`-pinned versions matching the floors specified (per D-28).
        - Source: `package.json` `dependencies` contains `@fontsource/saira-extra-condensed` and `@fontsource/mulish` (NOT `@fontsource-variable/mulish`) with `^`-pinned versions (per D-33, D-34).
        - Source: `package.json` does NOT contain `bootstrap`, `react-scroll`, `react-script-tag`, `jquery`, or any `font-awesome*` key (STYLE-01 + D-29).
        - Behavior: `npm ls <pkg>` exits 0 for all 7 newly-installed packages.
        - Behavior: `package-lock.json` is regenerated (mtime newer than session start) with corresponding `node_modules/.package-lock.json` integrity hashes for each new package.
    </acceptance_criteria>
    <done>
        All 7 packages declared in `package.json` with no peer-dependency warnings escalated to errors. `npm ls` reports clean tree.
    </done>
</task>

<task type="auto">
    <name>Task 2: Wire astro-icon integration + trailingSlash + build.format in astro.config.mjs</name>
    <files>astro.config.mjs</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/astro.config.mjs
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "astro.config.mjs (build config)")
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pitfall E "Bundle bloat from importing the whole Iconify pack"; Assumption A7)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-28 icon integration; D-38 trailingSlash)
    </read_first>
    <action>
        Edit `astro.config.mjs` surgically. Current file imports `defineConfig`, `react`, `tailwindcss` and exports a config with `site`, `output: 'static'`, `integrations: [react()]`, `vite.plugins: [tailwindcss()]`.

        Add (in order):
        1. `import icon from 'astro-icon';` after the existing `react` import.
        2. New top-level config key `trailingSlash: 'always'` between `output: 'static'` and `integrations` (per D-38 — prevents 301 redirect hops on internal anchors).
        3. New top-level config key `build: { format: 'directory' }` directly after `trailingSlash` (per D-38; Astro 6 default per RESEARCH.md A7 — explicit set is "belt and suspenders" so future Astro versions don't regress).
        4. Modify `integrations: [react()]` to `integrations: [react(), icon()]` — call `icon()` with NO arguments per Pitfall E (defaulting to per-glyph SVG inlining; do NOT pass `include` or `iconDir` overrides).

        Do NOT remove the existing `// @ts-check` directive at the top.
        Do NOT remove the existing comment block about user-site / no-base / Pitfall 10.
        Do NOT modify the `vite.plugins` block (Tailwind wiring is locked Phase 1 D-03 and unrelated to this change).

        After editing, run `npx astro check` to confirm zero TypeScript errors against the new config (the `icon` integration ships its own types via astro-icon@1.1.5).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "import icon from 'astro-icon';" astro.config.mjs &amp;&amp; \
              grep -qE "trailingSlash:\s*'always'" astro.config.mjs &amp;&amp; \
              grep -qE "build:\s*\{\s*format:\s*'directory'" astro.config.mjs &amp;&amp; \
              grep -qE "integrations:\s*\[react\(\),\s*icon\(\)\]" astro.config.mjs &amp;&amp; \
              ! grep -qE "icon\(\s*\{" astro.config.mjs &amp;&amp; \
              npx astro check 2&gt;&amp;1 | grep -qE "0\s+errors?"
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `astro.config.mjs` contains `import icon from 'astro-icon';` exactly once (per D-28).
        - Source: `astro.config.mjs` contains `trailingSlash: 'always'` (per D-38).
        - Source: `astro.config.mjs` contains `build: { format: 'directory' }` (per D-38, A7).
        - Source: `astro.config.mjs` `integrations` array is `[react(), icon()]` — exactly two integration entries; `icon()` called with zero arguments (Pitfall E; do NOT pass `include: { ... }` or `iconDir: ...`).
        - Source: `astro.config.mjs` still exports a default object via `defineConfig({ ... })` (no structural rewrite).
        - Behavior: `npx astro check` exits 0 with 0 errors (warnings/hints baseline of 38 from Phase 2 still acceptable).
    </acceptance_criteria>
    <done>
        astro.config.mjs declares the icon integration and trailing-slash config; astro check exits clean.
    </done>
</task>

<task type="auto">
    <name>Task 3: Delete Google Fonts preconnects from BaseHead.astro; verify clean build (baseline-only CDN gate; final strict gate owned by Plan 03-06)</name>
    <files>src/components/BaseHead.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/BaseHead.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/BaseHead.astro")
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-36, D-37)
        @.planning/phases/01-foundation/01-CONTEXT.md (D-13 — original preconnect rationale; Phase 4 SEO-01 owns BaseHead expansion)
    </read_first>
    <action>
        Edit `src/components/BaseHead.astro` surgically. Current file's template body (lines after the `---` fence) is exactly 6 lines:
        1. `<meta charset="utf-8" />`
        2. `<meta name="viewport" content="width=device-width, initial-scale=1" />`
        3. `<title>{title}</title>`
        4. `<link rel="icon" href="/favicon.svg" />`
        5. `<link rel="preconnect" href="https://fonts.googleapis.com" />`
        6. `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />`

        Delete lines 5 and 6 (the two `<link rel="preconnect">` lines pointing at Google Fonts) per D-36. Fonts will be self-hosted via Fontsource starting in Plan 03-02; the preconnects have no purpose.

        Do NOT add `<link rel="preload">` for font weights — D-37 defers preload to Phase 4 if LCP/CLS issues surface.
        Do NOT add OG / Twitter / canonical / description meta — Phase 4 SEO-01..05 owns BaseHead expansion.
        Do NOT modify the `interface Props { title: string }` block in the front matter.
        Do NOT modify the existing comment header (it cites D-13 + Phase 4 SEO-01 — leave that breadcrumb).

        Optionally replace the deleted lines with a single comment line documenting the deletion, e.g., `{/* D-36: Google Fonts preconnects removed — fonts self-hosted via @fontsource/* starting in Plan 03-02 */}` — this is planner discretion but helpful for future readers.

        After editing, run `npm run build` to confirm the trimmed BaseHead still produces a working `dist/index.html` with the integration registered in Task 2.

        Note on the CDN-cleanliness verify gate below: this task's verify only confirms BaseHead no longer hits fonts.googleapis.com / fonts.gstatic.com (the two specific URLs whose removal is the load-bearing change here). The full strict CDN gate (no fontawesome / iconify.design / devicon.com / cdnjs / stackpath / jsdelivr / Google Fonts anywhere in dist) is OWNED BY Plan 03-06 Task 3, after all section content has been authored. At this Wave-1 stage dist is essentially empty of section content so a strict gate would pass trivially — keeping the gate scoped here to the two preconnect URLs reflects what this plan actually proves. This is defense-in-depth, not the load-bearing verification (Plan 03-06 owns that).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              ! grep -qE 'fonts\.googleapis\.com|fonts\.gstatic\.com' src/components/BaseHead.astro &amp;&amp; \
              grep -q 'title' src/components/BaseHead.astro &amp;&amp; \
              grep -q 'rel="icon"' src/components/BaseHead.astro &amp;&amp; \
              npm run build 2&gt;&amp;1 | tail -20 | grep -qE "Complete|0 errors" &amp;&amp; \
              ! grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" dist/ 2&gt;/dev/null
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `src/components/BaseHead.astro` does NOT contain the strings `fonts.googleapis.com` or `fonts.gstatic.com` (per D-36).
        - Source: `src/components/BaseHead.astro` still contains the title block and `rel="icon"` favicon link (surgical change — only the two preconnect lines removed).
        - Source: `src/components/BaseHead.astro` does NOT add `rel="preload"` link tags (D-37 defers preload to Phase 4).
        - Source: `src/components/BaseHead.astro` does NOT add `<meta name="description">`, `<meta property="og:*">`, `<meta name="twitter:*">`, or `<link rel="canonical">` (Phase 4 SEO-01..05 scope).
        - Behavior: `npm run build` exits 0; `dist/index.html` exists after build.
        - Behavior: `grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" dist/` exits 1 (zero matches — the two URL strings whose removal this plan owns). The full strict CDN sweep is owned by Plan 03-06 Task 3 after Wave 3 section content lands.
    </acceptance_criteria>
    <done>
        BaseHead emits only charset/viewport/title/favicon; no Google Fonts URL anywhere in dist; baseline CDN gate green (final strict gate runs in Plan 03-06).
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → local lockfile | Third-party package code lands on disk and is executed at build time |
| BaseHead → browser | HTTP `<link>` references that browsers fetch on page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm install of 7 new packages | mitigate | All 8 packages received `[OK]` slopcheck verdict on 2026-05-27 (RESEARCH.md "Package Legitimacy Audit"). No `postinstall` hooks on any of the 8 packages (verified `npm view <pkg> scripts.postinstall` returns nothing). Versions pinned with `^` to floor matching researched values. |
| T-03-02 | Tampering | astro-icon integration build-time behavior | accept | astro-icon is an Astro integration that runs at build only (per-glyph SVG inlining); no runtime JS executed in the browser from icon resolution. Trust derives from package legitimacy (T-03-01). |
| T-03-03 | Information Disclosure | Google Fonts preconnects (currently shipping in dist/) | mitigate | Delete preconnects per D-36; no third-party font request remains in built HTML or network panel. GDPR-cleaner posture inherited (no Google Fonts IP-tracking on visitor load). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Package Legitimacy Audit at RESEARCH.md L177-191 (table) covers all 8 packages with [OK] verdicts; no [ASSUMED]/[SUS]/[SLOP] packages in this install set, so no blocking human checkpoint required per planner-source-audit.md gate. |
</threat_model>

<verification>
After all 3 tasks complete:

1. `cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && npm run build` exits 0.
2. `grep -c 'fonts\.googleapis\.com' src/components/BaseHead.astro` returns 0 (zero matches).
3. `grep -c "import icon from 'astro-icon'" astro.config.mjs` returns 1 (exactly one import line).
4. `grep -c "trailingSlash: 'always'" astro.config.mjs` returns 1.
5. `grep -c 'astro-icon' package.json` returns ≥ 1 (in devDependencies).
6. `grep -c '@fontsource/mulish' package.json` returns 1; `grep -c '@fontsource-variable/mulish' package.json` returns 0.
7. `grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" dist/ 2>/dev/null` exits 1 (the scoped Wave-1 gate; the broader CDN sweep is in Plan 03-06).
8. `npx vitest run tests/smoke.test.ts` exits 0 (Phase 1 smoke test still passes — Tailwind marker still present, section ids still emitted, hydration fixture still ships).
</verification>

<success_criteria>
- All 7 new packages installed and declared in `package.json` with `^`-pinned versions.
- `astro.config.mjs` declares `icon()` integration with no arguments, `trailingSlash: 'always'`, and `build: { format: 'directory' }`.
- `src/components/BaseHead.astro` no longer references Google Fonts domains.
- `npm run build` exits 0; `dist/` contains no Google Fonts preconnects.
- `npx vitest run tests/smoke.test.ts` (Phase 1 smoke) still passes — no regressions to scaffold.
- No new files created outside the 4 declared in `files_modified` frontmatter.
</success_criteria>

<output>
Create `.planning/phases/03-sections-navigation/03-01-SUMMARY.md` documenting:
- Exact versions resolved for all 7 packages (`npm ls` output for each).
- Confirmation `npx astro check` exits 0.
- Confirmation `npm run build` exits 0 with no Google Fonts URLs in dist.
- Confirmation `npx vitest run tests/smoke.test.ts` exits 0 (smoke suite green).
- Note: this plan owns only the scoped Google-Fonts-URL gate; the full strict CDN sweep (fontawesome / iconify.design / devicon.com / cdnjs / stackpath / jsdelivr / Google Fonts) is owned by Plan 03-06 Task 3.
</output>
</content>
</invoke>