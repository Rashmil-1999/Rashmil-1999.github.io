---
phase: 03-sections-navigation
plan: 05
type: execute
wave: 3
depends_on:
    - 03-01
    - 03-02
files_modified:
    - src/components/Projects.astro
autonomous: true
requirements:
    - SECTION-06
    - SECTION-09
    - SECTION-10
    - STYLE-03
must_haves:
    truths:
        - 'Projects section renders 13 project cards from src/content/projects/*/index.md, sorted by `order` ascending.'
        - 'Each card uses Astro <Image /> from astro:assets with src={entry.data.cover}; no <img src=...> for project covers; no image_map lookup table anywhere in the codebase.'
        - 'Cards alternate row direction at >= md (image left for odd, right for even); stack vertically on < md (image above text). The alternating row direction is guaranteed by the CSS rule `@media (min-width: 768px) { .project-card:nth-child(even) { flex-direction: row-reverse } }` added in Plan 03-02 — Plan 03-05 only applies the .project-card class, does not author the CSS.'
        - 'Every external anchor with target="_blank" includes rel="noopener noreferrer" AND a visually-hidden "(opens in new tab)" span; one anchor per card via the stretched-link ::after pattern (no nested anchors).'
        - 'Tech-stack chips render as pill <li> items inside a role="list" <ul> with name only (no per-tech icon — D-17).'
        - 'Total home-page image weight in dist after build is < 500 KB (Phase 3 SC #5; verified against optimized WebP/AVIF outputs from Astro Image pipeline).'
    artifacts:
        - path: 'src/components/Projects.astro'
          provides: 'Projects section with 13 alternating-row cards, optimized images, tech chips, stretched-link a11y, external-link safety triple'
          contains: "getCollection('projects'"
          min_lines: 50
    key_links:
        - from: 'src/components/Projects.astro'
          to: "13 src/content/projects/*/index.md entries"
          via: "await getCollection('projects', predicate) with sort by order"
          pattern: "getCollection\\('projects'"
        - from: 'src/components/Projects.astro'
          to: 'astro:assets Image component'
          via: "import { Image } from 'astro:assets' + per-card Image src={entry.data.cover}"
          pattern: "from 'astro:assets'"
        - from: 'src/components/Projects.astro'
          to: 'project URL navigation (external links)'
          via: 'href={entry.data.url} target="_blank" rel="noopener noreferrer"'
          pattern: 'rel="noopener noreferrer"'
---

<objective>
Fill the Phase 1 Projects stub with the alternating-row card pattern from RESEARCH.md Pattern 2 / PATTERNS.md, reading 13 project entries from `src/content/projects/`. Each card renders a Phase 2 typed `cover` image through Astro's `<Image />` (which produces optimized WebP/AVIF at native source aspect — STYLE-03 / SECTION-10), a stretched-link anchor for whole-card clickability (D-18 / no nested anchors), tech-stack pill chips with name only (D-17), and the external-link safety triple (`target="_blank"` + `rel="noopener noreferrer"` + visually-hidden new-tab announcement).

Purpose: Cover the heaviest component in Phase 3 (image pipeline + alternating layout + stretched-link a11y + tech chip rendering). The image-weight gate (Phase 3 SC #5: total < 500 KB) is the load-bearing verification for STYLE-03 — the existing CRA site shipped 4.8 MB / 1 MB raw PNGs; Astro `<Image />` with native source aspect should bring this under 500 KB across 13 cards. Wave 3 plan; parallel with 03-03 and 03-04 because no shared files. Plan 03-02 has pre-added BOTH the alternating-row CSS fallback rule AND the .text-muted helper; this plan emits the `.project-card` class + the Tailwind arbitrary variant `md:[&:nth-child(even)]:flex-row-reverse` and lets either path (or both — they produce identical CSS) win at build time. No conditional re-edits to global.css inside this plan.
Output: `src/components/Projects.astro` filled with ~55-75 lines per RESEARCH.md Pattern 2.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sections-navigation/03-CONTEXT.md
@.planning/phases/03-sections-navigation/03-RESEARCH.md
@.planning/phases/03-sections-navigation/03-PATTERNS.md
@.planning/phases/03-sections-navigation/03-UI-SPEC.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Projects.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/styles/global.css
</context>

<tasks>

<task type="auto">
    <name>Task 1: Fill Projects.astro (13 alternating-row cards with Image + stretched-link + tech chips + external-link safety) — pre-decided widths={[200,400]} for CLS safety</name>
    <files>src/components/Projects.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Projects.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/Projects.astro" lines ~467-598 — imports + read pattern, original CRA pattern WITH the image_map anti-pattern explicitly flagged for deletion, full Astro port template with stretched-link, stretched-link contract bullets, diff from CRA listing 5 changes)
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pattern 2 lines 412-486 + Pitfall C "Astro Image not getting dimensions from image() field" warning sign CLS; Assumption A5 fallback if `md:[&:nth-child(even)]:flex-row-reverse` arbitrary variant doesn't render — Plan 03-02 already added the CSS fallback, so Plan 03-05 emits the arbitrary variant and relies on global.css as belt-and-suspenders)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 trailing hr; D-15 alternating direction; D-16 native aspect — NO forced crop; D-17 pill chips name-only; D-18 stretched-link a11y pattern + opens-in-new-tab; D-19 markdown body via Content)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (Copywriting Contract row "Project card CTA" — "View on GitHub →" copy; Interaction Contract "Project card whole-card click")
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts (projects schema: title, url, tech_stack[], cover (image), alternates[] optional, order, draft)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/styles/global.css (verify the `@media (min-width: 768px) { .project-card:nth-child(even) { flex-direction: row-reverse } }` rule is present from Plan 03-02 Task 2)
    </read_first>
    <action>
        Replace the entire contents of `src/components/Projects.astro` with the RESEARCH.md Pattern 2 / PATTERNS.md Projects template.

        Front matter:
        - Import `getCollection` and `render` from `astro:content`.
        - Import `Image` from `astro:assets`.
        - Read entries: `const entries = await getCollection('projects', ({ data }) => import.meta.env.PROD ? !data.draft : true);` (Phase 2 D-05).
        - Sort: `entries.sort((a, b) => a.data.order - b.data.order);`
        - Pre-render bodies: `const rendered = await Promise.all(entries.map(async (entry) => ({ entry, Content: (await render(entry)).Content })));`

        Template body:
        1. `<section id="projects" class="resume-section">` (Phase 1 D-23 id lock).
        2. `<div class="resume-section-content">`.
        3. `<h2 class="mb-5">Projects</h2>` (A11Y-02 heading hierarchy).
        4. `{rendered.map(({ entry, Content }) => (...))}` — per-entry `<article class="project-card relative mb-5 flex flex-col md:flex-row md:[&:nth-child(even)]:flex-row-reverse">` (D-15 alternating + D-18 stretched-link container needs `relative` + `.project-card` class hooks the Plan 03-02 fallback CSS rule).

           Pre-decision on the alternating-row variant (RESEARCH.md Assumption A5): Plan 03-02 Task 2 has already added `@media (min-width: 768px) { .project-card:nth-child(even) { flex-direction: row-reverse } }` to `src/styles/global.css` as a guaranteed fallback. This task emits BOTH paths — the Tailwind v4 arbitrary variant `md:[&:nth-child(even)]:flex-row-reverse` on the class string AND relies on the `.project-card` class hook for the global.css rule. The two paths produce identical CSS (`flex-direction: row-reverse` on `.project-card:nth-child(even)` at min-width 768px); CSS cascade tolerates both, so no decision shifts to execution. Do NOT edit global.css from this plan.

           Inside the `<article>`:

           a. Image column `<div class="md:w-1/3 text-center">`:
              - Pre-decision on dimensions (Pitfall C): emit `<Image src={entry.data.cover} alt={entry.data.title} widths={[200, 400]} class="mx-auto max-h-[200px] max-w-[200px] object-contain" loading="lazy" />`. The `widths={[200, 400]}` is the pre-decided value (200px display × 2x DPR for retina) — this guarantees Astro emits intrinsic `width=`/`height=` attributes on the rendered `<img>` regardless of whether Astro 6's auto-inference from typed `ImageMetadata` fires reliably. Per D-16 native source aspect (no forced crop); `loading="lazy"` for below-the-fold cards (the first project card is technically above-the-fold but the perf cost is minimal; matches CRA's implicit behavior).

           b. Text column `<div class="relative z-10 md:w-2/3">` — the `relative z-10` lifts description text above the stretched-link `::after` overlay so text selection still works (D-18 contract bullet):
              - `<h3>` containing the stretched anchor with classes `project-card-link text-primary after:absolute after:inset-0`, `href={entry.data.url}`, `target="_blank"`, `rel="noopener noreferrer"`, containing `{entry.data.title}` followed by an aria-hidden `→` arrow span and a `<span class="sr-only">(opens in new tab)</span>`. The `after:absolute after:inset-0` arbitrary Tailwind utility emits `::after { position: absolute; inset: 0; }` — the stretched-link `::after` overlays the whole card. Card container is `position: relative` from the outer `<article class="... relative ...">` per the contract. Per D-18: one anchor per card; do NOT nest a separate "View on GitHub" anchor — that link is the SAME anchor, with the trailing arrow as the visible affordance.

                Reconciliation of D-18 vs UI-SPEC "View on GitHub →" copy: the project TITLE is the visible link text (CRA pattern); the UI-SPEC's "View on GitHub →" copy is realized as a trailing `→` arrow inside the title anchor (`<span aria-hidden="true"> →</span>`) — reads correctly to AT and avoids the nested-anchor problem. Document this choice inline with a comment citing D-18 + UI-SPEC reconciliation.

              - A `<div class="relative z-10">` wrapping the `<Content />` component — description body lifted above the stretched-link overlay.

              - `<h4 class="mt-3">Tech Stack:</h4>` — preserve the CRA heading order (description above, Tech Stack header beneath).

              - `<ul role="list" class="flex flex-wrap gap-2">` — `role="list"` for A11Y (Tailwind reset compensation).
                - `{entry.data.tech_stack.map((tech) => (<li class="rounded-full bg-black/5 px-2 py-1 text-xs">{tech}</li>))}` per D-17 pill chips with name only (no icon). Neutral `bg-black/5` per RESEARCH.md "Pill-chip styling" Claude's Discretion fallback.

           c. Close `</div>` (text column), close `</article>` (card).
        5. After the `.map()`, close `</div>` (section-content), `</section>`, then emit `<hr class="m-0 border-black/10" />` per D-03 + Pitfall A.

        Do NOT introduce `const image_map = { ... }` (SECTION-10 / Pitfall 20 — Phase 2 already eliminated; Phase 3 must not reintroduce).
        Do NOT use raw `<img src={...} />` for project covers (STYLE-03 / Pitfall 4 — `<Image />` mandatory).
        Do NOT nest anchors (D-18 a11y requirement — one anchor per card via stretched-link).
        Do NOT add `client:load` / `client:visible` / `client:idle`.
        Do NOT use Bootstrap classes (`card`, `bg-light`, `col-md-4`, `btn btn-secondary`, `card-body`).
        Do NOT edit `src/styles/global.css` from this plan — Plan 03-02 owns the alternating-row CSS rule. If you find the `.project-card:nth-child(even)` rule missing from global.css, halt and surface as a Plan 03-02 acceptance-criteria failure.
    </action>
    <verify>
        <automated>
            set -eo pipefail; \
              cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getCollection('projects'" src/components/Projects.astro &amp;&amp; \
              grep -q 'render' src/components/Projects.astro &amp;&amp; \
              grep -q "from 'astro:assets'" src/components/Projects.astro &amp;&amp; \
              grep -q 'import.meta.env.PROD' src/components/Projects.astro &amp;&amp; \
              grep -q 'id="projects"' src/components/Projects.astro &amp;&amp; \
              grep -qE 'h2[^&gt;]*&gt;[^&lt;]*Projects' src/components/Projects.astro &amp;&amp; \
              grep -qE 'Image\s+src=' src/components/Projects.astro &amp;&amp; \
              grep -q 'entry.data.cover' src/components/Projects.astro &amp;&amp; \
              grep -q 'entry.data.tech_stack' src/components/Projects.astro &amp;&amp; \
              grep -q 'entry.data.url' src/components/Projects.astro &amp;&amp; \
              grep -q 'widths={\[200, 400\]}' src/components/Projects.astro &amp;&amp; \
              grep -q 'rel="noopener noreferrer"' src/components/Projects.astro &amp;&amp; \
              grep -q 'target="_blank"' src/components/Projects.astro &amp;&amp; \
              grep -q 'opens in new tab' src/components/Projects.astro &amp;&amp; \
              grep -q 'after:absolute after:inset-0' src/components/Projects.astro &amp;&amp; \
              grep -q 'class="project-card' src/components/Projects.astro &amp;&amp; \
              grep -q 'role="list"' src/components/Projects.astro &amp;&amp; \
              grep -qE '\bContent\b' src/components/Projects.astro &amp;&amp; \
              grep -q 'flex-row-reverse' src/components/Projects.astro &amp;&amp; \
              ! grep -qE 'const image_map|let image_map|image_map\[' src/components/Projects.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/Projects.astro &amp;&amp; \
              ! grep -qE 'class="card\b|\bcol-md-' src/components/Projects.astro &amp;&amp; \
              grep -qE '\.project-card:nth-child\(even\)' src/styles/global.css
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: file imports `getCollection`, `render` from `astro:content` and `Image` from `astro:assets`.
        - Source: file reads with draft predicate `({ data }) => import.meta.env.PROD ? !data.draft : true`.
        - Source: file sorts by `a.data.order - b.data.order` ascending.
        - Source: file pre-renders bodies via `await Promise.all(entries.map(...{ Content: (await render(entry)).Content }))`.
        - Source: file contains `<section id="projects" class="resume-section">` (Phase 1 D-23 id lock).
        - Source: file contains a `<h2>` with the text `Projects` (A11Y-02 heading hierarchy).
        - Source: file contains an `<Image>` element with `src={entry.data.cover}` and `alt={entry.data.title}` — NO raw `<img>` for project covers (STYLE-03 / Pitfall 4 / SECTION-10).
        - Source: file contains the literal prop `widths={[200, 400]}` on the `<Image>` component (pre-decided Pitfall C mitigation — guarantees intrinsic dimensions in rendered HTML regardless of auto-inference).
        - Source: file does NOT contain `const image_map`, `let image_map`, or `image_map[` (SECTION-10 / Pitfall 20).
        - Source: each external project anchor has all three: `target="_blank"`, `rel="noopener noreferrer"`, and a child sr-only `(opens in new tab)` span (D-18 / RESEARCH.md "Security Domain").
        - Source: file contains the stretched-link Tailwind utility `after:absolute after:inset-0` on the title anchor (D-18 pattern).
        - Source: file applies `class="project-card ..."` on the outer `<article>` and emits `md:[&:nth-child(even)]:flex-row-reverse` as part of that class string (belt-and-suspenders with the Plan 03-02 global.css rule).
        - Source: file iterates `entry.data.tech_stack.map(...)` rendering `<li class="rounded-full bg-black/5 px-2 py-1 text-xs">{tech}</li>` inside `<ul role="list">` (D-17 pills name-only).
        - Source: file does NOT contain Bootstrap classes (`card`, `bg-light`, `col-md-4`, `btn btn-secondary`, `card-body`, `card-text`, `card-title`).
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
        - Source: file ends with the trailing horizontal-rule line carrying classes `m-0 border-black/10` (D-03 + Pitfall A).
        - Source (cross-plan precondition): `src/styles/global.css` contains the `@media (min-width: 768px) { .project-card:nth-child(even) { flex-direction: row-reverse; } }` rule from Plan 03-02 Task 2 (executor verifies before consuming `.project-card`).
    </acceptance_criteria>
    <done>
        Projects renders 13 alternating-row cards with optimized images (widths={[200,400]} pre-decided), stretched-link a11y, name-only tech chips; no image_map; no React runtime; no conditional CSS edits.
    </done>
</task>

<task type="auto">
    <name>Task 2: Build + image-weight verification — confirm 13 cards render, total image weight < 500 KB, intrinsic dimensions present</name>
    <files></files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Projects.astro
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pitfall C — already pre-mitigated in Task 1 via widths={[200,400]})
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.planning/ROADMAP.md (Phase 3 SC #5: total image weight on dist/index.html under 500 KB)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts
    </read_first>
    <action>
        Run the heavy verification (this is the load-bearing gate for STYLE-03). This task is pure verification — no decisions branch on the output, no conditional file edits.

        1. `rm -rf dist .astro` (flush cache; Pitfall 1 prevents stale-cache image-weight false-positive).
        2. `npm run build` — exit 0 expected. If build errors with `Could not find image for cover field` or similar, the typed image() reference broke; re-check Phase 2 colocation (all 13 project entries should have a `cover: ./<slug>.{png,jpg,webp,jpeg}` line that resolves to a file in the same `src/content/projects/<slug>/` directory).

        3. Confirm Pitfall C / CLS mitigation (already guaranteed by the pre-decided `widths={[200, 400]}` prop in Task 1):
           - `grep -cE 'img[^&gt;]+width="[0-9]+"' dist/index.html` should return ≥ 13 (one intrinsic-width attribute per Astro-emitted img tag for project covers).

        4. Total image-weight gate (Phase 3 SC #5):
           - Sum the byte sizes of `dist/index.html` plus every file under `dist/_astro/` whose extension is `.webp`, `.avif`, `.png`, `.jpg`, or `.jpeg`. The total must be < 512000 bytes (500 KB).
           - Command: `expr $(du -b dist/index.html | cut -f1) + $(find dist/_astro -maxdepth 1 \\( -name '*.webp' -o -name '*.avif' -o -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' \\) -exec du -b {} + 2>/dev/null | awk '{sum+=$1} END {print sum+0}')` should output a number < 512000.

           If the total exceeds 500 KB, run the breakdown to identify the heaviest offender:
           - `find dist/_astro -maxdepth 1 \\( -name '*.webp' -o -name '*.avif' -o -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' \\) -exec du -h {} + | sort -h | tail -10` — flag the heaviest few. Phase 3 SC #5 is a hard gate; if a single cover is huge after the Sharp pipeline runs, the colocated source asset is unexpectedly large and the situation surfaces as a Phase 2 colocation issue (not a Plan 03-05 fix). Halt and report — do NOT alter `widths` to mask the underlying problem.

        5. Stretched-link sanity:
           - `grep -cE 'article[^&gt;]*\\bproject-card\\b' dist/index.html` should return 13 (one per project card).
           - The pattern `after:absolute after:inset-0` should generate a CSS rule in `dist/_astro/*.css`: `grep -lE '::after.*absolute.*inset' dist/_astro/*.css` should return ≥ 1 file (Tailwind v4 `after:` variant emission).

        6. Alternating rows: confirm either the Tailwind arbitrary variant emitted or the Plan 03-02 fallback rule is doing the work (or both — they produce identical CSS):
           - `grep -lE 'nth-child\\(even\\)' dist/_astro/*.css` should return ≥ 1 file (Plan 03-02 global.css rule emits this; the Tailwind variant may also emit a parallel rule). Zero matches indicates BOTH paths failed — surface as a regression.

        7. Grep gates:
           - `grep -E "client:(load|visible|idle)" src/components/Projects.astro` exits 1.
           - `grep -rE "fontawesome|iconify\\.design|devicon\\.com|cdnjs\\.cloudflare|stackpath|jsdelivr" dist/` exits 1.

        8. `npx vitest run tests/smoke.test.ts` — Phase 1 smoke exits 0.
    </action>
    <verify>
        <automated>
            set -eo pipefail; \
              cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              rm -rf dist .astro &amp;&amp; \
              npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; \
              grep -q 'id="projects"' dist/index.html &amp;&amp; \
              [ "$(grep -cE 'img[^&gt;]+width=\"[0-9]+\"' dist/index.html)" -ge 5 ] &amp;&amp; \
              TOTAL=$(expr $(du -b dist/index.html | cut -f1) + $(find dist/_astro -maxdepth 1 \( -name '*.webp' -o -name '*.avif' -o -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' \) -exec du -b {} + 2&gt;/dev/null | awk '{sum+=$1} END {print sum+0}')) &amp;&amp; \
              echo "Total home-page image+html weight: ${TOTAL} bytes" &amp;&amp; \
              [ "$TOTAL" -lt 512000 ] &amp;&amp; \
              grep -lE 'nth-child\(even\)' dist/_astro/*.css &amp;&amp; \
              ! grep -E "client:(load|visible|idle)" src/components/Projects.astro 2&gt;/dev/null &amp;&amp; \
              ! grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/ 2&gt;/dev/null &amp;&amp; \
              npx vitest run tests/smoke.test.ts
        </automated>
    </verify>
    <acceptance_criteria>
        - Behavior: `npm run build` exits 0 (proves all 13 project entries' `cover` image() references resolved).
        - Behavior: `dist/index.html` contains `id="projects"`.
        - Behavior: `dist/index.html` contains ≥ 5 `<img ... width="N">` instances with intrinsic width attributes (lower bound; ideal is 13 — proves Pitfall C mitigation; CLS prevention; widths={[200, 400]} is doing its job).
        - Behavior: Total bytes of `dist/index.html` plus all `dist/_astro/*.{webp,avif,png,jpg,jpeg}` files is < 512000 (500 KB) — Phase 3 SC #5.
        - Behavior: `dist/_astro/*.css` contains a CSS rule emitting the after-pseudo-element absolute/inset stretched-link pattern (Tailwind v4 `after:absolute after:inset-0` variant emission).
        - Behavior: `dist/_astro/*.css` contains an `nth-child(even)` rule (sourced from Plan 03-02 global.css; the Tailwind arbitrary variant may also emit a parallel rule).
        - Behavior: `grep -E "client:(load|visible|idle)" src/components/Projects.astro` exits 1 (zero hits).
        - Behavior: `grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/` exits 1 (zero CDN refs).
        - Behavior: `npx vitest run tests/smoke.test.ts` exits 0 (smoke-only scope; full `npm test` runs at Plan 03-06 Task 3).
    </acceptance_criteria>
    <done>
        Projects build emits 13 cards; total image weight < 500 KB; stretched-link CSS present; nth-child(even) rule in dist; smoke green.
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 typed cover images → optimized output | Build-time Sharp pipeline reads ImageMetadata + emits WebP/AVIF |
| project.url → external navigation | Each project card opens a github.com URL in a new tab |
| project markdown body → rendered HTML | Sanitized via Astro Content component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | Tab-nabbing on external project URLs | mitigate | Every `<a target="_blank">` includes `rel="noopener noreferrer"` per D-18 / SECTION-06 / RESEARCH.md "Security Domain"; verified by grep gate. |
| T-03-15 | Tampering | Unverified large image bypassing optimization | mitigate | STYLE-03 + Pitfall 4 enforce `<Image />` for project covers; Phase 3 SC #5 budget gate (< 500 KB total) verifies Sharp pipeline ran. |
| T-03-16 | Tampering | XSS via project description markdown body | mitigate | Astro `<Content />` sanitizes via remark/rehype pipeline; content authored by project owner only. |
| T-03-17 | Tampering | Reintroduction of image_map lookup table | mitigate | SECTION-10 + Pitfall 20 explicit prohibition; grep gate `! grep -E "const image_map|image_map\[" src/components/Projects.astro` enforces zero hits. |
</threat_model>

<verification>
After all 2 tasks complete:

1. `wc -l src/components/Projects.astro` returns ≥ 50.
2. `grep -c "getCollection('projects'" src/components/Projects.astro` = 1.
3. `grep -cE 'Image\s+src=' src/components/Projects.astro` ≥ 1 (Astro Image component).
4. `grep -c 'image_map' src/components/Projects.astro` = 0 (SECTION-10).
5. `grep -c 'rel="noopener noreferrer"' src/components/Projects.astro` ≥ 1.
6. `grep -c 'after:absolute after:inset-0' src/components/Projects.astro` ≥ 1 (stretched-link).
7. `grep -c 'flex-row-reverse' src/components/Projects.astro` ≥ 1 (alternating rows).
8. `grep -c 'widths={\[200, 400\]}' src/components/Projects.astro` ≥ 1 (pre-decided Pitfall C mitigation).
9. `grep -E "client:(load|visible|idle)" src/components/Projects.astro` exits 1.
10. `grep -cE 'class="m-0 border-black/10"' src/components/Projects.astro` = 1 (trailing hr).
11. `npm run build` exits 0 + total image weight < 500 KB.
12. `npx vitest run tests/smoke.test.ts` exits 0.
</verification>

<success_criteria>
- 13 project cards render in `dist/index.html` (proven by article count + heading count inside id="projects").
- Every project image is an optimized output from `<Image />` (WebP/AVIF) with intrinsic `width=`/`height=` attributes (guaranteed by pre-decided `widths={[200, 400]}` prop).
- Total home-page image weight (HTML + all WebP/AVIF/PNG/JPG/JPEG in dist/_astro/) < 500 KB.
- Alternating row direction at ≥ md (image left for odd, right for even) — emitted as CSS in dist (from Plan 03-02 global.css `.project-card:nth-child(even)` rule, with Tailwind arbitrary variant as belt-and-suspenders).
- Every external project anchor has the safety triple + new-tab announcement; one anchor per card; stretched-link `::after` overlay present.
- Tech-stack pills render as `<li>` items inside `<ul role="list">` with name only (D-17).
- Zero `image_map` references in source.
- Zero React runtime / zero CDN refs in dist.
- `npx vitest run tests/smoke.test.ts` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-sections-navigation/03-05-SUMMARY.md` documenting:
- Final line count of `src/components/Projects.astro`.
- Total home-page image weight (computed bytes), proving the < 500 KB gate.
- Largest 3 emitted images by size (so the user can see the optimization at work vs. CRA's 4.8 MB / 1 MB originals).
- Confirmation of intrinsic dimensions in rendered `<img>` (count of `width="N"` instances; expected ≥ 13 via widths={[200, 400]}).
- Confirmation that `dist/_astro/*.css` contains the `.project-card:nth-child(even)` rule (sourced from Plan 03-02 global.css).
- Confirmation `npm run build` exits 0 and `npx vitest run tests/smoke.test.ts` exits 0.
- Note: this plan consumes `.project-card:nth-child(even)` from `src/styles/global.css` (Plan 03-02 ownership). No CSS rules added here; no conditional re-edits at execution time.
</output>
</content>
</invoke>