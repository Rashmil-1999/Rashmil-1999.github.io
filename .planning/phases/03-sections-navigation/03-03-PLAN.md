---
phase: 03-sections-navigation
plan: 03
type: execute
wave: 2
depends_on:
    - 03-01
    - 03-02
files_modified:
    - src/components/About.astro
    - src/components/Skills.astro
    - src/components/Testimonials.astro
autonomous: true
requirements:
    - SECTION-02
    - SECTION-05
    - SECTION-08
    - SECTION-09
must_haves:
    truths:
        - 'About section displays name, current_status, contact message, email mailto link, description paragraph, social icons (LinkedIn + Github), and a Download Resume button — all sourced from src/content/about.yaml.'
        - 'Skills section displays 6 category groups with horizontal-flowing icon+name pairs; every <Icon name="prefix:name"> resolves to a bundled SVG from astro-icon (no <img> tags for icons, no CDN <script> for Iconify).'
        - 'Testimonials section displays at least 1 <blockquote> with the markdown body rendered via <Content /> and attribution via <cite>; no trailing <hr> after Testimonials section.'
        - 'No section component imports anything from react, react-dom, or carries a client:* directive — all rendering is static.'
        - 'The Phase 1 Tailwind marker (text-[#abc123]) is preserved somewhere in src/components/ (executor moves it to a visually-hidden span or to SideNav, NOT deletes it).'
    artifacts:
        - path: 'src/components/About.astro'
          provides: 'About section with name/status/contact/social/download CTA'
          contains: 'getEntry'
          min_lines: 40
        - path: 'src/components/Skills.astro'
          provides: 'Skills section with category groups + bundled icons'
          contains: 'astro-icon/components'
          min_lines: 25
        - path: 'src/components/Testimonials.astro'
          provides: 'Testimonials section with blockquote + attribution; no trailing <hr>'
          contains: '<blockquote'
          min_lines: 20
    key_links:
        - from: 'src/components/About.astro'
          to: 'src/content/about.yaml (via getEntry)'
          via: "await getEntry('about', 'about')"
          pattern: "getEntry\\('about'"
        - from: 'src/components/About.astro'
          to: '/Rashmil_Panchani.pdf'
          via: '<a href="/Rashmil_Panchani.pdf" download>Download Resume</a>'
          pattern: "Rashmil_Panchani\\.pdf"
        - from: 'src/components/Skills.astro'
          to: 'astro-icon/components <Icon name={item.icon}>'
          via: 'import { Icon } from "astro-icon/components"'
          pattern: "from 'astro-icon/components'"
        - from: 'src/components/Testimonials.astro'
          to: "getCollection('testimonials') + render() for markdown body"
          via: 'await render(entry); <Content />'
          pattern: "getCollection\\('testimonials'"
---

<objective>
Fill three Phase 1 stub components (`About.astro`, `Skills.astro`, `Testimonials.astro`) with real markup that reads from Phase 2 collections. These three are bundled because they share two patterns not used by the list-collection plans 03-04 / 03-05: (1) singleton reads via `getEntry()` (About + Skills) and (2) blockquote markup with NO trailing `<hr>` (Testimonials).

Purpose: Cover 4 of the 10 SECTION-* requirements + the only `<Icon>` consumers among the resume sections (About social + Skills tech-stack). Wave 2 plan; runs in parallel with 03-04 and 03-05 because no shared files.
Output: Three filled `.astro` files plus one trivial preservation edit somewhere in `src/components/` to keep the Phase 1 Tailwind marker (`text-[#abc123]`) detectable by `dist/_astro/*.css` (executor's choice — move it onto an empty `<span aria-hidden="true" class="sr-only text-[#abc123]"></span>` inside About, or onto BaseLayout, or leave a tiny comment marker in another component; the smoke-test gate is the authority).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sections-navigation/03-CONTEXT.md
@.planning/phases/03-sections-navigation/03-RESEARCH.md
@.planning/phases/03-sections-navigation/03-PATTERNS.md
@.planning/phases/03-sections-navigation/03-UI-SPEC.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/About.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Skills.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Testimonials.astro
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content/about.yaml
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content/skills.yaml
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts
</context>

<tasks>

<task type="auto">
    <name>Task 1: Fill About.astro (singleton read + name h1 + accent span + status + email + description + social icons + Download Resume CTA + mobile-only profile photo)</name>
    <files>src/components/About.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/About.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content/about.yaml
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/About.astro" lines ~166-265 — imports pattern, original CRA accent-span pattern, full Astro port template, diff from CRA, Pitfall B caveat on getEntry second-arg)
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pattern 3 lines 504-575 + Pitfall B on getEntry id mismatch + Pitfall C on Image dimensions)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 trailing hr; D-25 layout incl. mobile-only profile photo; D-26 social icons with aria-label + new-tab announcement; D-27 Resume Download CTA copy)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (Copywriting Contract row "Primary CTA (About section)" and "Social icon link ARIA labels")
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts (assertion that &lt;dist/index.html&gt; contains id="about" and that dist/_astro/*.css contains the #abc123 marker)
    </read_first>
    <action>
        Replace the entire contents of `src/components/About.astro` with the RESEARCH.md Pattern 3 / PATTERNS.md About template. The new file structure:

        Front matter (between `---` fences):
        - Import `getEntry` from `astro:content`.
        - Import `Image` from `astro:assets`.
        - Import `Icon` from `astro-icon/components` (per D-26).
        - Import `profileImage` from `../assets/profile.jpg`.
        - Call `const aboutEntry = await getEntry('about', 'about');` (Pitfall B note: if Astro 6's `file()` loader derives a different id, check via `console.log(await getCollection('about'))` in dev once and adjust the second arg; the current `about.yaml` top-level key is `about:`).
        - Add the throw guard: `if (!aboutEntry) throw new Error('about.yaml missing — was Phase 2 completed?');`
        - `const about = aboutEntry.data;`

        Template body (`<section id="about" class="resume-section">` + content):
        1. Outer `<section id="about" class="resume-section">` — id MUST be `about` (Phase 1 D-23 lock; smoke test assertion).
        2. Inner `<div class="resume-section-content">` wrapper (matches recovered CRA pattern + global.css `.resume-section-content { width: 100% }`).
        3. Mobile-only profile photo per D-25 + D-11: `<div class="text-center md:hidden"><Image src={profileImage} alt={`${about.first_name} ${about.last_name}`} class="profile-pic mx-auto mb-4 rounded-full" width={200} height={225} /></div>`. The `md:hidden` keeps the photo invisible on desktop where the sidebar (Plan 03-06) renders it.
        4. `<h1 class="mb-0">{about.first_name}{' '}<span class="text-primary">{about.last_name}</span></h1>` — the accent span is the single-use carryover from CRA (PATTERNS.md "Shared Patterns" lines ~1061-1067).
           - Append (inline, inside the h1 or immediately after) a Tailwind marker preservation node: `<span aria-hidden="true" class="sr-only text-[#abc123]"></span>` — this keeps Phase 1 smoke assertion #5 (`#abc123` in `dist/_astro/*.css`) passing without polluting visual output. Add an inline comment citing Phase 1 D-12 + smoke test gate.
        5. `<div class="subheading mb-5">` containing:
           - `<p class="mb-3">{about.current_status}</p>`
           - `<p class="lead mb-0">{about.contact_message}</p>`
           - `<a href={`mailto:${about.email}`}>{about.email}</a>` — uses the `.text-link` / default link styling (about email is a plain `mailto:` — internal-style link, no `target="_blank"`, no new-tab span).
        6. `<p class="lead mb-5">{about.description}</p>`.
        7. Social row + Download CTA wrapper: `<div class="flex flex-wrap items-center gap-4">`:
           - Inner social list: `<div class="flex gap-3">{about.social.map((handle) => (...))}</div>` where each entry is `<a href={handle.url} aria-label={handle.name} target="_blank" rel="noopener noreferrer" class="social-icon"><Icon name={handle.icon} aria-hidden="true" class="size-6" /><span class="sr-only">(opens in new tab)</span></a>` per D-26.
           - Download Resume CTA: `<a href={`/${about.resume_download}`} download class="btn-primary">Download Resume</a>` per D-27 (visible "Download Resume" label, NOT icon-only). `about.resume_download` is `Rashmil_Panchani.pdf` from `about.yaml`; the leading `/` makes the URL site-root-relative.
        8. Close `</div>` (section-content), close `</section>`, then emit `<hr class="m-0 border-black/10" />` per D-03 + Pitfall A (Tailwind v4 default `<hr>` border is `currentColor` which would render way darker than the recovered CRA `rgba(0,0,0,.1)`).

        Do NOT add `client:load` / `client:visible` / `client:idle` anywhere — SECTION-09 + Phase 3 SC #3 forbid (verified by grep in Plan 03-06).
        Do NOT import `bootstrap` or use Bootstrap classes like `d-flex`, `col-md-*`, `btn-secondary` — STYLE-01.
        Do NOT add a desktop-side profile photo here — D-25 explicitly states "Photo is **not** repeated here (the desktop sidebar already shows it per D-11)".
        Do NOT add a `<h2>About</h2>` heading — the recovered CRA About section does not have one (the name `<h1>` IS the section heading).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getEntry('about'" src/components/About.astro &amp;&amp; \
              grep -q "from 'astro-icon/components'" src/components/About.astro &amp;&amp; \
              grep -q "from 'astro:assets'" src/components/About.astro &amp;&amp; \
              grep -q 'id="about"' src/components/About.astro &amp;&amp; \
              grep -q 'class="resume-section"' src/components/About.astro &amp;&amp; \
              grep -q 'class="text-primary"' src/components/About.astro &amp;&amp; \
              grep -q 'mailto:' src/components/About.astro &amp;&amp; \
              grep -q 'Rashmil_Panchani.pdf\|about\.resume_download' src/components/About.astro &amp;&amp; \
              grep -q '&gt;Download Resume&lt;' src/components/About.astro &amp;&amp; \
              grep -q 'rel="noopener noreferrer"' src/components/About.astro &amp;&amp; \
              grep -qE 'aria-label=\{handle\.name\}' src/components/About.astro &amp;&amp; \
              grep -q 'sr-only.*(opens in new tab)\|(opens in new tab).*sr-only' src/components/About.astro &amp;&amp; \
              grep -q 'md:hidden' src/components/About.astro &amp;&amp; \
              grep -q 'text-\[#abc123\]' src/components/About.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/About.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `src/components/About.astro` imports `getEntry` from `astro:content`, `Image` from `astro:assets`, `Icon` from `astro-icon/components`, and `profileImage` from `../assets/profile.jpg`.
        - Source: file contains `await getEntry('about', 'about')` with `if (!aboutEntry) throw new Error(...)` guard (Pitfall B mitigation).
        - Source: file contains `<section id="about" class="resume-section">` (Phase 1 D-23 id lock).
        - Source: file contains `<h1>` with first_name + `<span class="text-primary">{about.last_name}</span>` accent span (CRA carryover).
        - Source: file contains `<a href={`mailto:${about.email}`}>` (no target="_blank" on mailto).
        - Source: file contains `<a ... download class="btn-primary">Download Resume</a>` with `href={`/${about.resume_download}`}` — D-27 copy + leading slash for site-root path.
        - Source: file contains social row mapping `about.social.map((handle) => (...))` where each `<a>` has all four: `href={handle.url}`, `aria-label={handle.name}`, `target="_blank"`, `rel="noopener noreferrer"` AND a child `<Icon name={handle.icon} aria-hidden="true" ...>` AND a child `<span class="sr-only">(opens in new tab)</span>`.
        - Source: file contains mobile-only profile photo wrapped in `<div class="... md:hidden">` (D-25).
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle` (SECTION-09).
        - Source: file does NOT contain Bootstrap class strings like `d-flex`, `col-md-`, `btn-secondary`, `card bg-light` (STYLE-01).
        - Source: file contains the Tailwind marker `text-[#abc123]` somewhere (e.g., on a sr-only span) so smoke assertion #5 still passes (Phase 1 D-12).
        - Source: file ends with `<hr class="m-0 border-black/10" />` (D-03 + Pitfall A).
    </acceptance_criteria>
    <done>
        About section renders name, status, contact CTA, description, social row, Download Resume CTA, mobile-only profile photo; smoke marker preserved.
    </done>
</task>

<task type="auto">
    <name>Task 2: Fill Skills.astro (singleton read + category groups + horizontal icon+name rows with bundled astro-icon)</name>
    <files>src/components/Skills.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Skills.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content/skills.yaml
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/Skills.astro" lines ~387-464 — imports + read pattern, original CRA dual-rendering branches, Astro port pattern, diff from CRA, Pitfall B caveat on getEntry second-arg)
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pattern 4 lines 580-614 + Pitfall D on astro-icon build failure for unknown ids + Pitfall E on bundle bloat)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 trailing hr; D-20 category heading + horizontal flowing row; D-21 no runtime fallback)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (Copywriting Contract row "Skills icon decorative ARIA")
    </read_first>
    <action>
        Replace the entire contents of `src/components/Skills.astro` with the RESEARCH.md Pattern 4 template. The new file structure:

        Front matter:
        - Import `getEntry` from `astro:content`.
        - Import `Icon` from `astro-icon/components`.
        - `const skillsEntry = await getEntry('skills', 'skills');` (Pitfall B caveat: same second-arg verification as About — `skills.yaml` top-level key is `skills:`).
        - Throw guard: `if (!skillsEntry) throw new Error('skills.yaml missing');`
        - Sort categories by order: `const categories = [...skillsEntry.data.categories].sort((a, b) => a.order - b.order);` — note the spread because the schema returns a readonly array under `noUncheckedIndexedAccess`.

        Template body (`<section id="skills" class="resume-section">`):
        1. Outer `<section id="skills" class="resume-section">` (Phase 1 D-23 id lock).
        2. `<div class="resume-section-content">`.
        3. `<h2 class="mb-5">Skills</h2>` (section heading — recovered CRA had no explicit h2 because Bootstrap inherits, but UI-SPEC + WCAG heading hierarchy require `<h2>` per section per A11Y-02 / Phase 4 — Phase 3 emits the proper hierarchy now).
        4. `{categories.map((category) => (...))}` — outer per-category `<div class="mb-5">`:
           - `<div class="subheading mb-3">{category.name}</div>` — uses the `.subheading` class from global.css (Saira uppercase 500 1.5rem).
           - `<ul role="list" class="dev-icons flex flex-wrap gap-4">` — `role="list"` per A11Y-01 (Tailwind reset strips semantic list role when `list-style: none` is implicit); `flex flex-wrap gap-4` handles the horizontal flow with wrap per D-20.
           - Inside the `<ul>`: `{category.items.map((item) => <li class="flex flex-col items-center"><Icon name={item.icon} aria-hidden="true" class="size-6" /><span class="mt-1 text-xs">{item.name}</span></li>)}`.

           Per D-20: icon stacked above name (`flex flex-col items-center`); icon is decorative (`aria-hidden="true"` per UI-SPEC Copywriting "Skills icon decorative ARIA"); name `<span>` is the accessible name beneath the icon.
        5. Close `</div>`, `</section>`, then emit `<hr class="m-0 border-black/10" />` per D-03 + Pitfall A.

        Important — Pitfall D: every `category.items[].icon` value in the current `skills.yaml` is already a verified Iconify identifier (Phase 2 D-16 regex + Phase 2 D-17 curated mapping; e.g., `devicon:python`, `simple-icons:django`, `logos:graphql`, `lucide:code`). If a value is unknown to the installed packs, astro-icon will FAIL THE BUILD with `Icon "...:..." not found` — that is the desired behavior per D-21. Do NOT add a runtime `try/catch` or fallback `<Icon name="lucide:help-circle">` — surface errors loudly.

        Important — Pitfall E: do NOT add `include: { devicon: ['*'] }` to `astro.config.mjs` (already handled by Plan 03-01 which left `icon()` argless). This task does not modify astro.config.mjs.

        Do NOT add `client:load` / `client:visible` / `client:idle` (SECTION-09).
        Do NOT use Bootstrap classes (`d-flex`, `list-inline-item`, `mr-3`) — Tailwind only.
        Do NOT emit a per-item `<button>` (CRA's `.btn-secondary mr-2 mb-2 disabled` pattern is for Project tech-stack chips, NOT Skills — Plan 03-05 handles those).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getEntry('skills'" src/components/Skills.astro &amp;&amp; \
              grep -q "from 'astro-icon/components'" src/components/Skills.astro &amp;&amp; \
              grep -q 'id="skills"' src/components/Skills.astro &amp;&amp; \
              grep -q 'class="resume-section"' src/components/Skills.astro &amp;&amp; \
              grep -q '&lt;h2' src/components/Skills.astro &amp;&amp; \
              grep -q 'Skills' src/components/Skills.astro &amp;&amp; \
              grep -q 'class="subheading' src/components/Skills.astro &amp;&amp; \
              grep -q 'role="list"' src/components/Skills.astro &amp;&amp; \
              grep -q 'aria-hidden="true"' src/components/Skills.astro &amp;&amp; \
              grep -q '&lt;Icon name=\{item\.icon\}' src/components/Skills.astro &amp;&amp; \
              grep -q '&lt;hr class="m-0 border-black/10"' src/components/Skills.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/Skills.astro &amp;&amp; \
              ! grep -qE '\bbtn-secondary\b|\bd-flex\b|\blist-inline-item\b' src/components/Skills.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `src/components/Skills.astro` imports `getEntry` from `astro:content` and `Icon` from `astro-icon/components`.
        - Source: file calls `await getEntry('skills', 'skills')` with throw guard.
        - Source: file sorts categories by `a.order - b.order` (Phase 2 D-25 default order 10/20/30/40/50/60).
        - Source: file contains `<section id="skills" class="resume-section">` (Phase 1 D-23 id lock).
        - Source: file contains `<h2 class="mb-5">Skills</h2>` (heading hierarchy per A11Y-02; recovered CRA had no explicit h2 but Phase 3 emits proper hierarchy).
        - Source: file maps over `categories` and inside each iterates `category.items`, emitting `<Icon name={item.icon} aria-hidden="true" class="size-6" />` followed by `<span ... >{item.name}</span>` — icon and name are siblings inside `<li class="flex flex-col items-center">` (icon above name per D-20).
        - Source: `<ul>` carries `role="list"` (A11Y-01 / Tailwind reset compensation).
        - Source: file does NOT contain Bootstrap classes (`d-flex`, `list-inline-item`, `mr-3`, `btn-secondary`).
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
        - Source: file ends with `<hr class="m-0 border-black/10" />` (D-03 + Pitfall A).
    </acceptance_criteria>
    <done>
        Skills section renders 6 category groups with bundled-SVG icons and decorative ARIA per D-20.
    </done>
</task>

<task type="auto">
    <name>Task 3: Fill Testimonials.astro (list-collection read + blockquote + cite + role/org attribution; NO trailing hr per D-03)</name>
    <files>src/components/Testimonials.astro</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Testimonials.astro
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/components/Testimonials.astro" lines ~601-666 — original CRA pattern, Astro port pattern with role/org, diff from CRA)
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-03 — Testimonials is the ONLY section without trailing &lt;hr&gt;; D-24 — blockquote + cite + Mulish italic 400; D-05 draft predicate)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/content.config.ts (testimonials schema: user, role, org optional, order, draft)
    </read_first>
    <action>
        Replace the entire contents of `src/components/Testimonials.astro` with the PATTERNS.md Testimonials Astro port template. The new file structure:

        Front matter:
        - Import `getCollection` and `render` from `astro:content`.
        - Read with draft predicate: `const entries = await getCollection('testimonials', ({ data }) => import.meta.env.PROD ? !data.draft : true);` per Phase 2 D-05.
        - Sort: `entries.sort((a, b) => a.data.order - b.data.order);`
        - Render bodies: `const rendered = await Promise.all(entries.map(async (entry) => ({ entry, Content: (await render(entry)).Content })));` — same shape as Pattern 1.

        Template body (`<section id="testimonials" class="resume-section">`):
        1. `<section id="testimonials" class="resume-section">` (Phase 1 D-23 id lock).
        2. `<div class="resume-section-content">`.
        3. `<h2 class="mb-5">Testimonials</h2>` (A11Y-02 heading hierarchy).
        4. `{rendered.map(({ entry, Content }) => (...))}` — per-entry `<blockquote class="blockquote">`:
           - Quote body wrapped in italic (D-24 — Mulish 400i for testimonial text): `<div class="mb-0 lead italic"><Content /></div>` — note `<Content />` renders the markdown body (not a frontmatter field) per Phase 2 D-19.
           - `<footer class="blockquote-footer">` containing `<cite>{entry.data.user}</cite>` followed by conditional `{entry.data.role && entry.data.org && (<span class="text-muted"> — {entry.data.role}, {entry.data.org}</span>)}`. The current snapshot has user="Roopam Mishra", role="Founder", org="Phionike Solutions: Design-Tech Studio" (per Plan 02-05 SUMMARY decomposition); the conditional handles future testimonials without role/org.
        5. Close `</blockquote>`, `</div>` (section-content), `</section>`.

        **CRITICAL: NO trailing `<hr class="m-0 border-black/10" />` after Testimonials.** Per D-03 + CRA pattern (original Testimonials.jsx has no `<hr>` either — Testimonials is the last section). Add a one-line comment immediately after `</section>` documenting this: `{/* D-03: Testimonials is the only section without a trailing <hr> (last section; matches recovered CRA pattern) */}`.

        Do NOT use `<em>` to wrap the body — D-24 specifies italic via Mulish 400i; the `<div class="italic">` wrapper with body-font is the right shape. The recovered CRA pattern used `<em>{testimonial.text}</em>` over plain text, but Phase 2 D-19 moved the text into the markdown body, so we wrap the `<Content />` not the raw text.
        Do NOT add `cite title="Source Title"` — that was a CRA bug (recovered file had hardcoded `title="Source Title"` on every `<cite>`; PATTERNS.md flags it as a drop).
        Do NOT add `client:load` / `client:visible` / `client:idle`.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "getCollection('testimonials'" src/components/Testimonials.astro &amp;&amp; \
              grep -q 'render' src/components/Testimonials.astro &amp;&amp; \
              grep -q 'import.meta.env.PROD' src/components/Testimonials.astro &amp;&amp; \
              grep -q 'id="testimonials"' src/components/Testimonials.astro &amp;&amp; \
              grep -q '&lt;blockquote' src/components/Testimonials.astro &amp;&amp; \
              grep -q '&lt;cite' src/components/Testimonials.astro &amp;&amp; \
              grep -q 'italic' src/components/Testimonials.astro &amp;&amp; \
              grep -q '&lt;Content' src/components/Testimonials.astro &amp;&amp; \
              ! grep -q 'title="Source Title"' src/components/Testimonials.astro &amp;&amp; \
              ! grep -q '&lt;hr' src/components/Testimonials.astro &amp;&amp; \
              ! grep -qE 'client:(load|visible|idle)' src/components/Testimonials.astro
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `src/components/Testimonials.astro` imports `getCollection` and `render` from `astro:content`.
        - Source: file uses the draft predicate `({ data }) => import.meta.env.PROD ? !data.draft : true` (Phase 2 D-05 inheritance).
        - Source: file sorts entries by `a.data.order - b.data.order` ascending.
        - Source: file renders markdown bodies via `<Content />` (Phase 2 D-19) inside an `italic`-classed wrapper (D-24).
        - Source: file contains `<section id="testimonials" class="resume-section">` (Phase 1 D-23 id lock).
        - Source: file contains `<h2>` for the section heading (A11Y-02).
        - Source: file contains `<blockquote>` and `<cite>` elements; cite shows `entry.data.user`.
        - Source: file conditionally renders `entry.data.role` + `entry.data.org` (both must be truthy to show the attribution suffix).
        - Source: file does NOT contain `title="Source Title"` (the CRA bug; PATTERNS.md "Diff from CRA" line drops it).
        - Source: file does NOT contain ANY `<hr>` element (D-03 — Testimonials is the only section without trailing hr).
        - Source: file contains an inline comment immediately after `</section>` documenting the deliberate omission of `<hr>` (citing D-03 + last-section status).
        - Source: file does NOT contain `client:load`, `client:visible`, or `client:idle`.
    </acceptance_criteria>
    <done>
        Testimonials renders blockquote + cite + role/org attribution; no trailing hr; markdown body italicized via the .italic + .lead wrapper.
    </done>
</task>

<task type="auto">
    <name>Task 4: Build + smoke verification — confirm all three sections render, marker preserved, no React runtime, no CDN refs</name>
    <files></files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/About.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Skills.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/components/Testimonials.astro
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts (5 assertions including #5 marker check and #4 React JS chunk presence)
    </read_first>
    <action>
        Run the verification sequence:

        1. `rm -rf dist .astro` (flush cache).
        2. `npm run build` — should exit 0. If `Icon "...:..." not found` errors from astro-icon, an Iconify id in `about.yaml` or `skills.yaml` is broken; halt and surface the bad id (don't add silent fallbacks — D-21).
        3. `npm test` — Phase 1 smoke must exit 0. All 5 assertions:
           - dist/index.html exists.
           - All 8 section ids (sidenav, about, education, work, skills, projects, leadership, testimonials) in `dist/index.html`.
           - dist/hydration-test/index.html exists (hydration fixture untouched).
           - dist/_astro/*.js contains a React chunk (hydration fixture's island ships its JS).
           - dist/_astro/*.css contains `#abc123` (Tailwind marker — Plan 03-03 Task 1 preserved it on About's sr-only span).

        4. Behavior gates:
           - `grep -c 'id="about"' dist/index.html` ≥ 1 (About filled).
           - `grep -c 'id="skills"' dist/index.html` ≥ 1 (Skills filled).
           - `grep -c 'id="testimonials"' dist/index.html` ≥ 1 (Testimonials filled).
           - `grep -c '<blockquote' dist/index.html` ≥ 1 (testimonial rendered).
           - `grep -c 'Download Resume' dist/index.html` = 1 (About CTA text).
           - `grep -c 'Rashmil_Panchani.pdf' dist/index.html` ≥ 1 (resume PDF reference).
           - `grep -c '<svg' dist/index.html` ≥ 32 (bundled SVG icons for 30 skills + 2 social — Pitfall E sanity check; exact count may vary if Astro inlines or splits some).
           - `grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs|stackpath|jsdelivr" dist/ 2>/dev/null` exits 1 (zero CDN refs).
           - `grep -rE "client:(load|visible|idle)" src/components/About.astro src/components/Skills.astro src/components/Testimonials.astro` exits 1 (zero hits in the three new files).

        5. If smoke #5 fails (no `#abc123` in dist/_astro/*.css): the marker preservation in About.astro Task 1 didn't survive. Re-verify the sr-only span carries `text-[#abc123]` literally (Tailwind v4 source detection requires the exact class string in source).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              rm -rf dist .astro &amp;&amp; \
              npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; \
              grep -q 'id="about"' dist/index.html &amp;&amp; \
              grep -q 'id="skills"' dist/index.html &amp;&amp; \
              grep -q 'id="testimonials"' dist/index.html &amp;&amp; \
              grep -q '&lt;blockquote' dist/index.html &amp;&amp; \
              grep -q 'Download Resume' dist/index.html &amp;&amp; \
              grep -q 'Rashmil_Panchani.pdf' dist/index.html &amp;&amp; \
              [ "$(grep -c '&lt;svg' dist/index.html)" -ge 20 ] &amp;&amp; \
              ! grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/ 2&gt;/dev/null &amp;&amp; \
              ! grep -E "client:(load|visible|idle)" src/components/About.astro src/components/Skills.astro src/components/Testimonials.astro 2&gt;/dev/null &amp;&amp; \
              npm test 2&gt;&amp;1 | grep -qE "5 (passed|tests passed)"
        </automated>
    </verify>
    <acceptance_criteria>
        - Behavior: `npm run build` exits 0 (no `Icon "...:..." not found` errors — proves every Iconify id in about.yaml + skills.yaml resolves against installed packs).
        - Behavior: `dist/index.html` contains `id="about"`, `id="skills"`, `id="testimonials"`.
        - Behavior: `dist/index.html` contains `<blockquote`, the text `Download Resume`, the string `Rashmil_Panchani.pdf`.
        - Behavior: `dist/index.html` contains ≥ 20 `<svg` substrings (lower bound — actual count is 30 skills + 2 social ≈ 32, but Astro may split some into hashed chunks; 20 floor catches a wholesale failure to inline).
        - Behavior: `grep -rE "fontawesome|iconify\.design|devicon\.com|cdnjs\.cloudflare|stackpath|jsdelivr" dist/` exits 1 (zero CDN refs — STYLE-02 / D-29).
        - Behavior: `grep -E "client:(load|visible|idle)" src/components/About.astro src/components/Skills.astro src/components/Testimonials.astro` exits 1 (zero hits — SECTION-09).
        - Behavior: `npm test` exits 0 — all 5 Phase 1 smoke assertions pass (section ids, hydration fixture, JS chunk, marker — proving Plan 03-03 preserved the Tailwind marker per the About sr-only span).
    </acceptance_criteria>
    <done>
        Three sections render real content; clean rebuild; smoke green; zero CDN refs; zero React hydration on section components.
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| about.yaml / skills.yaml content → rendered HTML | Build-time read of validated Zod-typed data |
| about.social[].url → external browser navigation | User-clicked links open in new tab |
| /Rashmil_Panchani.pdf → file download | Resume served from public/ at site root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Tab-nabbing on external social links | mitigate | Every `<a target="_blank">` in About.social row includes `rel="noopener noreferrer"` per D-26 + RESEARCH.md "Security Domain" + UI-SPEC Copywriting Contract. |
| T-03-08 | Information Disclosure | Email `mailto:` link to scrapers | accept | Mailto exposure is the existing pattern per CONCERNS.md; harvesting risk accepted at the project level. M2 may add obfuscation. |
| T-03-09 | Tampering | XSS via markdown body in testimonials | mitigate | Astro `<Content />` sanitizes markdown output through the standard remark/rehype pipeline; content authored by project owner only (CMS-neutral schema means future editors go through Zod validation per CONTENT-08). |
| T-03-10 | Information Disclosure | mailto link exposing user email | accept | Same as T-03-08. |
| T-03-11 | Tampering | astro-icon resolving unknown Iconify id at build | mitigate | Pitfall D — astro-icon fails build loudly on unknown ids (desired behavior per D-21); no silent fallback added. |
</threat_model>

<verification>
After all 4 tasks complete:

1. `wc -l src/components/About.astro` returns ≥ 40.
2. `wc -l src/components/Skills.astro` returns ≥ 25.
3. `wc -l src/components/Testimonials.astro` returns ≥ 20.
4. `grep -c "getEntry('about'" src/components/About.astro` returns 1.
5. `grep -c "getEntry('skills'" src/components/Skills.astro` returns 1.
6. `grep -c "getCollection('testimonials'" src/components/Testimonials.astro` returns 1.
7. `grep -c '<hr' src/components/Testimonials.astro` returns 0 (D-03 — no trailing hr).
8. `grep -c '<hr' src/components/About.astro` returns 1 (D-03 — trailing hr).
9. `grep -c '<hr' src/components/Skills.astro` returns 1 (D-03 — trailing hr).
10. `grep -rE "client:(load|visible|idle)" src/components/About.astro src/components/Skills.astro src/components/Testimonials.astro` exits 1.
11. `npm run build` exits 0.
12. `npm test` exits 0 (5 passing assertions).
</verification>

<success_criteria>
- About.astro renders name + accent span + status + email link + Download Resume CTA + social row + mobile-only profile photo.
- Skills.astro renders 6 category groups (sorted by `order`) with bundled-SVG icons and decorative ARIA.
- Testimonials.astro renders blockquote + cite + optional role/org attribution; NO trailing hr.
- Tailwind marker (`#abc123`) preserved on a sr-only span inside About.astro; smoke test green.
- Zero CDN refs in dist (no fontawesome / iconify.design / devicon.com / cdnjs / stackpath / jsdelivr).
- Zero React runtime emitted from the three new components (no `client:*` directives).
- `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-sections-navigation/03-03-SUMMARY.md` documenting:
- Final line counts for the 3 modified components.
- Confirmation that `getEntry('about', 'about')` and `getEntry('skills', 'skills')` second-arg ids resolved correctly (Pitfall B — note actual ids found via `console.log(await getCollection('about'))` in dev if executor ran that check).
- Confirmation `npm run build` exits 0 (no Icon-not-found errors — all Iconify ids resolved).
- Confirmation `dist/index.html` contains section ids `about`, `skills`, `testimonials` + the strings `Download Resume`, `Rashmil_Panchani.pdf`, `<blockquote`.
- Confirmation `<svg` count in dist/index.html (sanity check on icon inlining).
- Confirmation marker `#abc123` survives in dist/_astro/*.css (where it was placed in About).
