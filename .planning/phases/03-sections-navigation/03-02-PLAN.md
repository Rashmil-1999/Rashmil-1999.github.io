---
phase: 03-sections-navigation
plan: 02
type: execute
wave: 2
depends_on:
    - 03-01
files_modified:
    - src/styles/global.css
autonomous: true
requirements:
    - STYLE-01
    - STYLE-04
    - NAV-02
must_haves:
    truths:
        - 'Body background, text color, accent, link colors are sourced from a single @theme block in global.css (no inline styles, no scattered hex).'
        - 'Saira Extra Condensed (headings + .subheading) and Mulish (body) load from Fontsource self-hosted files — no Google Fonts request appears in the browser network panel.'
        - 'Smooth scroll on anchor click is honored by default; prefers-reduced-motion: reduce disables it via a @media guard.'
        - 'Sidebar is fixed at >= 768px with width 19rem and body padding-left 19rem; on < 768px the sidebar is sticky-top and body has padding-top 3.375rem.'
        - 'Active nav link state (driven later by aria-current="page" from Plan 03-06 scroll-spy script) renders with 3 visual signals: accent color, accent-colored 4px left border, 800 weight.'
        - 'The Phase 1 Tailwind marker (#abc123) still appears in dist/_astro/*.css after this plan runs — global.css does NOT remove or shadow the marker utility.'
        - 'Helper classes .text-muted, .social-icon, and project-card alternating-row rule are pre-defined here so Plan 03-03/04/05 consume them without conditional fallbacks.'
    artifacts:
        - path: 'src/styles/global.css'
          provides: 'Tailwind v4 entry + Fontsource @imports + @theme tokens + base body/heading rules + smooth-scroll guard + sidebar/section layout CSS + .text-muted + .social-icon + project-card alternating-row rule'
          min_lines: 90
          contains: '--color-bg: #eee2dc'
    key_links:
        - from: 'src/styles/global.css'
          to: '@fontsource/saira-extra-condensed/500.css, /700.css'
          via: '@import statements'
          pattern: "@import\\s+'@fontsource/saira-extra-condensed/(500|700)\\.css'"
        - from: 'src/styles/global.css'
          to: '@fontsource/mulish/400.css, /400-italic.css, /800.css, /800-italic.css'
          via: '@import statements'
          pattern: "@import\\s+'@fontsource/mulish/(400|400-italic|800|800-italic)\\.css'"
        - from: 'src/styles/global.css @theme'
          to: 'Tailwind v4 utility generation (--color-bg → bg-bg, --color-accent → text-accent, etc.)'
          via: 'CSS-first @theme block per D-02'
          pattern: '@theme\\s*\\{'
---

<objective>
Fill the empty `@theme {}` placeholder in `src/styles/global.css` (Phase 1 D-14) with the recovered CRA brand tokens (per D-06 / RESEARCH.md "Recovered CRA Tokens"), import Fontsource per-weight CSS files (D-33 / D-34), declare base body + heading rules that match the existing site, add the smooth-scroll + prefers-reduced-motion guard (D-32 / NAV-02), and lay down the sidebar / `.resume-section` / `.nav-link` / `.profile-pic` / `.text-primary` / `.btn-primary` / `.text-muted` / `.social-icon` / project-card alternating-row CSS classes that the section components in Wave 3 will reference.

Purpose: Centralize all visual tokens and base layout CSS in one file so Wave 3 plans only emit Tailwind utilities + a small set of recovered semantic classes. Without this plan running first, Wave 3's section components would render but look wrong (no cream background, no Saira headings, default Tailwind border on `<hr>`, no muted text color on date columns, no social-icon styling on About anchors, no alternating row direction on Projects). This plan is wave 2 (not wave 1) because its 6 Fontsource @imports depend on Plan 03-01 having installed the @fontsource/* packages — running them in parallel would fail at npm run build with "Cannot find module '@fontsource/saira-extra-condensed/500.css'".

Output: `src/styles/global.css` populated with ~95-130 lines covering: tailwind import, 6 Fontsource imports, `@theme` block (5 color tokens + 2 font tokens + 1 muted alias), body base, heading base, smooth-scroll guard, `.subheading`, `.lead`, `.resume-section` + `.resume-section-content`, sidebar layout media queries, `.nav-link` + `.nav-link[aria-current='page']`, `.profile-pic`, `.text-primary`, `.btn-primary` + `:hover`, `.text-muted`, `.social-icon` + `:hover`, and `.project-card:nth-child(even) { flex-direction: row-reverse }` inside @media (min-width: 768px).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sections-navigation/03-CONTEXT.md
@.planning/phases/03-sections-navigation/03-RESEARCH.md
@.planning/phases/03-sections-navigation/03-PATTERNS.md
@.planning/phases/03-sections-navigation/03-UI-SPEC.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/styles/global.css
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts
</context>

<tasks>

<task type="auto">
    <name>Task 1: Fill global.css with Fontsource imports + @theme tokens + base body/heading + smooth-scroll guard</name>
    <files>src/styles/global.css</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/styles/global.css
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (sections "Recovered CRA Tokens" — brand colors table, typography table, spacing & dimensions table; section "Pattern 8: global.css Additions" — full CSS block lines 786-948; Pitfall F "Fontsource imports not picked up by Vite")
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/styles/global.css" — target shape and CRA-recovery delta on .btn-primary navy-vs-accent reconciliation, lines ~770-905)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (sections "Spacing Scale", "Typography", "Color", "Responsive Breakpoints")
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-02, D-04, D-05, D-06, D-10, D-12, D-32, D-33, D-34, D-35)
    </read_first>
    <action>
        Replace the contents of `src/styles/global.css` (currently 9 lines: tailwind @import + empty @theme block) with the full Pattern 8 / PATTERNS.md target shape. Sections in order:

        1. `@import 'tailwindcss';` (keep — Phase 1 D-14 entry).

        2. Six Fontsource per-weight imports per D-34. Use the EXACT per-weight paths (Pitfall F mitigation — `@fontsource/mulish` package root is NOT a valid import):
           - `@fontsource/saira-extra-condensed/500.css`
           - `@fontsource/saira-extra-condensed/700.css`
           - `@fontsource/mulish/400.css`
           - `@fontsource/mulish/400-italic.css`
           - `@fontsource/mulish/800.css`
           - `@fontsource/mulish/800-italic.css`

        3. `@theme { ... }` block with EXACTLY these tokens (values from RESEARCH.md "Recovered CRA Tokens" — sourced from `git show b537845:src/App.css`):
           - `--color-bg: #eee2dc;` (D-04 — cream/off-white body background)
           - `--color-text: #6c757d;` (D-06 — Bootstrap gray body text)
           - `--color-accent: #bd5d38;` (D-05 — single warm rust/orange accent; CRA `--primary` / `--orange`)
           - `--color-link: #123c69;` (D-06 — deep navy default link color)
           - `--color-link-hover: #123c69ad;` (D-06 — navy at ~68% alpha)
           - `--color-muted: #6c757d;` (alias of --color-text for semantic clarity)
           - `--font-heading: 'Saira Extra Condensed', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;`
           - `--font-body: 'Mulish', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;`

           Do NOT add spacing scale tokens (`--spacing-*`) — UI-SPEC.md "Spacing Scale" confirmed Tailwind v4 default 4-based scale covers all recovered rem values; D-02 explicitly allows skipping if "utilities don't cover it" (they do).
           Do NOT add a `--color-primary` token — RESEARCH.md Open Question 4 resolved to `--color-accent` with a manual `.text-primary` rule below (preserves CRA class recognition; planner pick documented).

        4. `body { ... }` base rule (recovered values):
           - `background-color: var(--color-bg);`
           - `color: var(--color-text);`
           - `font-family: var(--font-body);`
           - `font-weight: 400;`
           - `line-height: 1.5;`

        5. `h1, h2, h3, h4, h5, h6 { ... }` rule:
           - `font-family: var(--font-heading);`
           - `text-transform: uppercase;` (recovered line ~10474 of pre-wipe App.css).
        6. `h1 { line-height: 1; }` (recovered line ~10487).

        7. Smooth-scroll + reduced-motion guard (per D-32 / NAV-02):
           - `html { scroll-behavior: smooth; }`
           - `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }`

        Do NOT add fences (```css) inside the file — fenced blocks are markdown-only.
        Do NOT add a top-of-file comment listing every D-XX; one short header comment naming the file role + "filled by Plan 03-02 (Phase 3)" is enough.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "@import 'tailwindcss';" src/styles/global.css &amp;&amp; \
              grep -cE "^@import '@fontsource/(saira-extra-condensed|mulish)/" src/styles/global.css | grep -q "^6$" &amp;&amp; \
              grep -q -- "--color-bg: #eee2dc" src/styles/global.css &amp;&amp; \
              grep -q -- "--color-accent: #bd5d38" src/styles/global.css &amp;&amp; \
              grep -q -- "--color-link: #123c69" src/styles/global.css &amp;&amp; \
              grep -q -- "--color-muted: #6c757d" src/styles/global.css &amp;&amp; \
              grep -q -- "--font-heading:" src/styles/global.css &amp;&amp; \
              grep -q -- "--font-body:" src/styles/global.css &amp;&amp; \
              grep -q "scroll-behavior: smooth" src/styles/global.css &amp;&amp; \
              grep -q "prefers-reduced-motion: reduce" src/styles/global.css
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: `src/styles/global.css` line 1 is `@import 'tailwindcss';` (preserved from Phase 1).
        - Source: file contains EXACTLY 6 `@import '@fontsource/...';` lines: 2 for saira-extra-condensed (500, 700), 4 for mulish (400, 400-italic, 800, 800-italic) — per D-34 locked weight set.
        - Source: file contains NO `@import '@fontsource/mulish';` or `@import '@fontsource-variable/mulish';` (Pitfall F + RESEARCH.md Open Question 2 — static per-weight imports only).
        - Source: `@theme { ... }` block contains all 6 color tokens (including the `--color-muted: #6c757d` alias) + 2 font tokens listed in the action; no `--spacing-*` tokens; no `--color-primary` token.
        - Source: `body` selector sets `background-color: var(--color-bg)`, `color: var(--color-text)`, `font-family: var(--font-body)`, `font-weight: 400`, `line-height: 1.5`.
        - Source: `h1, h2, h3, h4, h5, h6` selector sets `font-family: var(--font-heading)` and `text-transform: uppercase`.
        - Source: `h1` selector sets `line-height: 1`.
        - Source: file contains `html { scroll-behavior: smooth; }` followed by `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }` (per D-32 / NAV-02).
    </acceptance_criteria>
    <done>
        global.css `@theme` block populated; base body + heading + smooth-scroll + reduced-motion guards land.
    </done>
</task>

<task type="auto">
    <name>Task 2: Add recovered .resume-section + .subheading + .lead + sidebar layout + .nav-link + .profile-pic + .text-primary + .btn-primary + .text-muted + .social-icon + alternating-row CSS</name>
    <files>src/styles/global.css</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/styles/global.css
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (section "Recovered CRA Tokens — Spacing & dimensions" table; "Pattern 8: global.css Additions" full CSS block lines 786-948; Pitfall A on Tailwind v4 currentColor default for hr — informs Wave 3 markup, NOT this CSS file; Assumption A5 on the project-card alternating-row arbitrary variant — Plan 03-02 pre-adds the CSS fallback so Plan 03-05 does not branch)
        @.planning/phases/03-sections-navigation/03-PATTERNS.md (section "src/styles/global.css" — note the CRA-RECOVERY DELTA at lines ~905: recovered CRA `.btn-primary` is `#123c69` navy, NOT accent — Plan 03-02 must reconcile; section "Recovered CRA Tokens" for `.social-icons .social-icon` 3.5rem rounded-circle treatment)
        @.planning/phases/03-sections-navigation/03-UI-SPEC.md (sections "Color" reserved-for list — Resume Download button uses accent per D-27; sidebar "active" link uses 3 signals; "Responsive Breakpoints")
        @.planning/phases/03-sections-navigation/03-CONTEXT.md (D-10 breakpoint widening 992px→768px; D-12 3-signal active link; D-15 alternating direction; D-26 social-icon class; D-27 Resume CTA accent)
    </read_first>
    <action>
        Append the following CSS to `src/styles/global.css` (after the Task 1 base rules). All values are from RESEARCH.md "Recovered CRA Tokens" + UI-SPEC.md. Do NOT use Tailwind utility classes inside `global.css` — these are recovered semantic classes consumed by `.astro` component markup.

        Sections in order (identifiers, signatures, values — NOT a full code block; executor reads RESEARCH.md Pattern 8 lines 786-948 verbatim):

        1. `.subheading` — `font-family: var(--font-heading); text-transform: uppercase; font-weight: 500; font-size: 1.5rem;` (recovered lines ~10484-10489).

        2. `.lead` — `font-size: 1.15rem; font-weight: 400;` (recovered line ~10491).

        3. `.resume-section` — `padding: 5rem 1rem; max-width: 75rem;` (recovered lines ~10573-10580). Followed by `@media (min-width: 768px) { .resume-section { min-height: 100vh; padding-left: 3rem; padding-right: 3rem; } }` (recovered behavior; CRA used 992px, Phase 3 widens to 768px per D-10).

        4. `.resume-section-content` — `width: 100%;` (recovered line ~10582).

        5. Sidebar layout for desktop (≥ 768px per D-10, widened from CRA 992px):
           - `@media (min-width: 768px) { body { padding-left: 19rem; } .sidenav { position: fixed; top: 0; left: 0; width: 19rem; height: 100vh; background-color: var(--color-bg); display: flex; flex-direction: column; } }`

        6. Sidebar layout for mobile (< 768px):
           - `@media (max-width: 767px) { body { padding-top: 3.375rem; } .sidenav { position: sticky; top: 0; background-color: var(--color-bg); z-index: 50; } }`

        7. `.nav-link` — 3-signal active state per D-12 / A11Y-04:
           - Base: `font-weight: 800; letter-spacing: 0.05rem; text-transform: uppercase; padding: 0.5rem 1rem; color: var(--color-text); border-left: 4px solid transparent;`
           - Active: `.nav-link[aria-current='page'] { color: var(--color-accent); border-left-color: var(--color-accent); }` — 3 signals = accent text + accent border-left + 800 weight (weight already 800 baseline; emphasis from the color + border per D-12).

        8. `.profile-pic` — `width: 200px; height: 225px; object-fit: cover;` (recovered line ~10600).

        9. `.text-primary` — `color: var(--color-accent);` (single-use accent span carryover from CRA `<h1>{first_name}<span class="text-primary">{last_name}</span>`; RESEARCH.md Open Question 4 resolution — preserve the visible class name).

        10. `.btn-primary` — Resume Download CTA. Per D-27 + UI-SPEC.md "Color" reserved-for list item 5: USE ACCENT background, NOT the recovered CRA navy. RESEARCH.md PATTERNS.md "CRA RECOVERY DELTA" (lines ~905) flags the recovered CRA `.btn-primary` as navy `#123c69`; Phase 3 reconciles to accent per D-27 ("accent background or accent border — exact style follows the recovered CRA pattern, D-06"). The UI-SPEC "Refresh same vibe" interpretation (D-01) lands on accent. Document this reconciliation as a comment inline in the .btn-primary rule (e.g., `/* D-27 reconciliation: UI-SPEC accent (#bd5d38), NOT recovered CRA navy (#123c69) */`).
           - Base: `display: inline-flex; align-items: center; padding: 0.5rem 1rem; background-color: var(--color-accent); color: white; text-decoration: none; border-radius: 0.25rem;`
           - Hover: `.btn-primary:hover { background-color: #a44e2f; }` (accent darkened ~10%).

        11. `.text-muted` — `color: var(--color-muted);` — explicit utility class consumed by `Education.astro`, `Work.astro`, `Leadership.astro` right-column date `<span>`. This rule is added here (rather than relying on Tailwind v4 auto-generating `text-muted` from the `--color-muted` token) so Plan 03-04 does NOT branch on whether the auto-generation happened. PATTERNS.md "Diff from CRA" notes the CRA used Bootstrap `text-secondary`; Phase 3 standardizes on `text-muted` via the `--color-muted` token alias.

        12. `.social-icon` — recovered CRA `.social-icons .social-icon` rounded-circle treatment (per RESEARCH.md "Recovered CRA Tokens"). Consumed by `About.astro` social anchors per D-26. Rule:
            `.social-icon { display: inline-flex; align-items: center; justify-content: center; width: 3.5rem; height: 3.5rem; border-radius: 9999px; background-color: var(--color-text); color: var(--color-bg); text-decoration: none; }`
            Hover: `.social-icon:hover { background-color: var(--color-accent); }` (accent on hover per CRA pattern).
            Inline comment: `/* D-26: recovered CRA .social-icons .social-icon 3.5rem rounded-circle (sourced from b537845:src/App.css) */`.

        13. `.project-card` alternating-row rule per D-15 + RESEARCH.md Assumption A5. The Tailwind v4 arbitrary variant `md:[&:nth-child(even)]:flex-row-reverse` is the primary path in Plan 03-05, but in case the variant fails to emit (Assumption A5 LOW-risk failure mode), this plan pre-adds a plain CSS fallback so Plan 03-05 does NOT branch. Rule:
            `@media (min-width: 768px) { .project-card:nth-child(even) { flex-direction: row-reverse; } }`
            Inline comment: `/* D-15 alternating row direction: belt-and-suspenders for Tailwind v4 arbitrary variant md:[&:nth-child(even)]:flex-row-reverse in Projects.astro; both paths cover the same selector + property */`.
            Both the arbitrary variant and this rule resolve to the same `flex-direction: row-reverse` declaration on the even-indexed `.project-card` at ≥ 768px, so when both apply (the common case) the result is identical. If the variant fails to emit, this rule still provides correct behavior.

        Do NOT add `.nav-link:hover` styling — Tailwind v4 default hover behavior + the `.nav-link[aria-current='page']` active rule covers the spec; Phase 4 may add explicit hover during A11Y verification.
        Do NOT add `:focus-visible` outline overrides — recovered CRA had no `:focus-visible` rules; Tailwind v4 default focus ring is preserved per CONTEXT.md D-01 "focus rings honored" + .claude/CLAUDE.md § 5.
        Do NOT add an `<hr>` color override here — Wave 3 section markup uses `<hr class="m-0 border-black/10" />` per Pitfall A; setting `--default-border-color` globally has wider blast radius (RESEARCH.md Open Question 3 — per-`<hr>` utility chosen).
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              grep -q "^.subheading\b" src/styles/global.css &amp;&amp; \
              grep -qE "\.subheading\s*\{[^}]*font-size:\s*1\.5rem" src/styles/global.css &amp;&amp; \
              grep -qE "\.lead\s*\{[^}]*font-size:\s*1\.15rem" src/styles/global.css &amp;&amp; \
              grep -qE "\.resume-section\s*\{[^}]*max-width:\s*75rem" src/styles/global.css &amp;&amp; \
              grep -qE "min-width:\s*768px" src/styles/global.css &amp;&amp; \
              grep -qE "width:\s*19rem" src/styles/global.css &amp;&amp; \
              grep -qE "padding-left:\s*19rem" src/styles/global.css &amp;&amp; \
              grep -qE "padding-top:\s*3\.375rem" src/styles/global.css &amp;&amp; \
              grep -qE "\.nav-link\[aria-current='page'\]" src/styles/global.css &amp;&amp; \
              grep -qE "\.text-primary\s*\{[^}]*var\(--color-accent\)" src/styles/global.css &amp;&amp; \
              grep -qE "\.btn-primary\s*\{[^}]*background-color:\s*var\(--color-accent\)" src/styles/global.css &amp;&amp; \
              grep -qE "\.profile-pic\s*\{[^}]*width:\s*200px" src/styles/global.css &amp;&amp; \
              grep -qE "\.text-muted\s*\{[^}]*color:\s*var\(--color-muted\)" src/styles/global.css &amp;&amp; \
              grep -qE "\.social-icon\s*\{[^}]*border-radius:\s*9999px" src/styles/global.css &amp;&amp; \
              grep -qE "\.project-card:nth-child\(even\)\s*\{[^}]*flex-direction:\s*row-reverse" src/styles/global.css
        </automated>
    </verify>
    <acceptance_criteria>
        - Source: file contains `.subheading { ... }` rule with `font-family: var(--font-heading)`, `font-weight: 500`, `font-size: 1.5rem`, `text-transform: uppercase` (recovered).
        - Source: file contains `.lead { ... }` rule with `font-size: 1.15rem; font-weight: 400` (recovered).
        - Source: file contains `.resume-section { ... }` rule with `padding: 5rem 1rem; max-width: 75rem;` (recovered).
        - Source: file contains `@media (min-width: 768px) { .resume-section { ... min-height: 100vh; padding-left: 3rem; padding-right: 3rem; ... } }` (recovered behavior at widened breakpoint per D-10).
        - Source: file contains `.resume-section-content { width: 100%; }`.
        - Source: file contains `@media (min-width: 768px)` block setting `body { padding-left: 19rem; }` AND `.sidenav { position: fixed; ... width: 19rem; height: 100vh; ... }`.
        - Source: file contains `@media (max-width: 767px)` block setting `body { padding-top: 3.375rem; }` AND `.sidenav { position: sticky; top: 0; ... }`.
        - Source: file contains `.nav-link { ... font-weight: 800; letter-spacing: 0.05rem; text-transform: uppercase; padding: 0.5rem 1rem; color: var(--color-text); border-left: 4px solid transparent; ... }`.
        - Source: file contains `.nav-link[aria-current='page'] { color: var(--color-accent); border-left-color: var(--color-accent); ... }` — proving 3-signal active state per D-12.
        - Source: file contains `.profile-pic { width: 200px; height: 225px; object-fit: cover; }` (recovered).
        - Source: file contains `.text-primary { color: var(--color-accent); }` (CRA single-use accent span).
        - Source: file contains `.btn-primary { ... background-color: var(--color-accent); ... }` (NOT `#123c69`) AND `.btn-primary:hover { background-color: #a44e2f; }`.
        - Source: file contains inline comment near `.btn-primary` documenting the D-27 reconciliation (e.g., `/* D-27 reconciliation: UI-SPEC accent (#bd5d38), NOT recovered CRA navy (#123c69) */`).
        - Source: file contains `.text-muted { color: var(--color-muted); }` — fixes orphan consumption from Education/Work/Leadership right-column date span (closes Plan 03-04 decision-shift).
        - Source: file contains `.social-icon { ... width: 3.5rem; height: 3.5rem; border-radius: 9999px; ... }` AND `.social-icon:hover { background-color: var(--color-accent); }` — closes Plan 03-03 About-anchor orphan class.
        - Source: file contains `@media (min-width: 768px) { .project-card:nth-child(even) { flex-direction: row-reverse; } }` — pre-resolves Plan 03-05 Assumption A5 fallback (closes the Task 1/Task 2 conditional re-edit in Plan 03-05).
        - Source: file does NOT contain `outline: none` (CLAUDE.md § 5 / A11Y-05 — focus rings preserved).
        - Source: file does NOT contain `--default-border-color` (RESEARCH.md Open Question 3 — per-`<hr>` utility instead).
    </acceptance_criteria>
    <done>
        global.css contains all recovered semantic classes + sidebar layout media queries + .text-muted + .social-icon + alternating-row rule; total file length 95-130 lines.
    </done>
</task>

<task type="auto">
    <name>Task 3: Build verification — confirm Fontsource .woff2 files land in dist, marker remains, no Google Fonts request</name>
    <files>src/styles/global.css</files>
    <read_first>
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/src/styles/global.css
        @.planning/phases/03-sections-navigation/03-RESEARCH.md (Pitfall F — Fontsource imports not picked up by Vite; warning sign: FOUT + zero .woff2 in dist)
        @/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/tests/smoke.test.ts (assertion #5: `text-[#abc123]` marker must still appear in dist/_astro/*.css)
    </read_first>
    <action>
        Execute a clean rebuild and verify the Fontsource asset graph + Tailwind marker survive:

        1. `rm -rf dist .astro` to flush Phase 1/2 cached output.
        2. `npm run build` — should exit 0.
        3. Confirm dist contains the 6 expected Fontsource `.woff2` weights:
           - `dist/_astro/` should contain at least 1 file matching `saira-extra-condensed-*-500-*.woff2` and 1 matching `*-700-*.woff2` (2 Saira weights).
           - `dist/_astro/` should contain at least 1 file matching `mulish-*-400-normal*.woff2`, 1 matching `*-400-italic*.woff2`, 1 matching `*-800-normal*.woff2`, 1 matching `*-800-italic*.woff2` (4 Mulish weights).
           - Filename exact pattern is Fontsource-internal (subset hashes, latin/latin-ext); the check is "exactly 6 distinct Fontsource-shaped woff2 files".
        4. Confirm Tailwind marker survives: `grep -l "#abc123" dist/_astro/*.css` returns ≥ 1 file (Phase 1 smoke assertion #5).
        5. Confirm no Google Fonts URL anywhere in dist: `grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" dist/` exits 1.
        6. `npx vitest run tests/smoke.test.ts` — Phase 1 smoke test exits 0 (all 5 smoke assertions pass — section ids, marker, hydration fixture, JS chunk). This command runs ONLY the smoke suite (not content-validation), so the legacy "5 passed" sentinel still applies cleanly to the targeted assertion.

        If `npm run build` fails with `Cannot find module '@fontsource/saira-extra-condensed/500.css'`: confirm Plan 03-01 ran first (`npm ls @fontsource/saira-extra-condensed` must report a resolved version). If it ran and the import still fails, surface as a blocker — do NOT switch to `@fontsource-variable/*` silently.

        If `dist/_astro/*.css` does NOT contain `#abc123`: the marker has been shadowed (Pitfall 29 regression). Re-inspect the order of @theme tokens and base body rules — the Tailwind v4 source-detection must still see the `text-[#abc123]` class in `src/components/About.astro`. Do NOT remove the marker from About.astro to "clean up"; Plan 03-03 owns marker management when filling About.
    </action>
    <verify>
        <automated>
            cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &amp;&amp; \
              rm -rf dist .astro &amp;&amp; \
              npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; \
              [ "$(find dist/_astro -maxdepth 1 -name 'saira-extra-condensed-*.woff2' | wc -l | tr -d ' ')" -ge 2 ] &amp;&amp; \
              [ "$(find dist/_astro -maxdepth 1 -name 'mulish-*.woff2' | wc -l | tr -d ' ')" -ge 4 ] &amp;&amp; \
              grep -lE "#abc123" dist/_astro/*.css &amp;&amp; \
              ! grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" dist/ 2&gt;/dev/null &amp;&amp; \
              npx vitest run tests/smoke.test.ts
        </automated>
    </verify>
    <acceptance_criteria>
        - Behavior: `npm run build` exits 0 after `rm -rf dist .astro`.
        - Behavior: `find dist/_astro -name 'saira-extra-condensed-*.woff2'` returns ≥ 2 distinct filenames (one per locked weight 500/700 — D-34).
        - Behavior: `find dist/_astro -name 'mulish-*.woff2'` returns ≥ 4 distinct filenames (one per locked weight 400/400i/800/800i — D-34).
        - Behavior: `grep -l '#abc123' dist/_astro/*.css` returns at least one CSS file path (Phase 1 smoke assertion #5 still passes; Tailwind v4 source detection unbroken by the new @theme block — Pitfall 29 non-regression).
        - Behavior: `grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" dist/ 2>/dev/null` exits 1 (zero matches — D-36 still honored post-rebuild).
        - Behavior: `npx vitest run tests/smoke.test.ts` exits 0; all 5 Phase 1 smoke assertions pass (this is the smoke-only gate; the broader `npm test` runs both smoke + content-validation and is exercised at end-of-phase Plan 03-06 Task 3).
    </acceptance_criteria>
    <done>
        Clean build produces 6+ Fontsource woff2 files in dist; Tailwind marker still present; zero Google Fonts references; Phase 1 smoke green via scoped vitest run.
    </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @theme token values → Tailwind v4 utility generation | Brand colors flow from CSS into emitted utility classes |
| Fontsource @import → Vite asset graph | npm-shipped .woff2 files are copied into dist/_astro/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Fontsource per-weight CSS files served from dist | accept | All package code already vetted via Plan 03-01 (`[OK]` slopcheck per RESEARCH.md L177-191); CSS contains only `@font-face` declarations referencing local .woff2 files. No JS in font CSS. |
| T-03-05 | Information Disclosure | Inline color hex values in @theme | accept | Brand tokens are public design choices; no secret data. |
| T-03-06 | Denial of Service | Smooth-scroll CSS rule + prefers-reduced-motion guard | mitigate | Reduced-motion media query honors WCAG 2.3.3 user preference; no JS-driven animation introduced (CSS-only). |
</threat_model>

<verification>
After all 3 tasks complete:

1. `wc -l src/styles/global.css` reports 95-130 lines (was 9 pre-plan).
2. `grep -c "^@import '@fontsource" src/styles/global.css` returns 6 (per D-34 weight set).
3. `grep -c -- "--color-" src/styles/global.css` returns ≥ 6 (5 color tokens + alias).
4. `grep -c "prefers-reduced-motion: reduce" src/styles/global.css` returns 1 (NAV-02).
5. `grep -c "min-width: 768px" src/styles/global.css` returns ≥ 3 (.resume-section min-height + sidebar layout + project-card alternating).
6. `find dist/_astro -name '*.woff2' | wc -l` returns ≥ 6 after rebuild.
7. `grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com|fontawesome|iconify\.design|devicon\.com|cdnjs|stackpath|jsdelivr" dist/ 2>/dev/null` exits 1 (zero CDN refs after Plan 03-02 — strengthens gate from Plan 03-01).
8. `npx vitest run tests/smoke.test.ts` exits 0 (smoke suite green; broader `npm test` end-of-phase gate is owned by Plan 03-06 Task 3).
</verification>

<success_criteria>
- `src/styles/global.css` populated with @theme tokens (5 colors + 1 muted alias + 2 fonts), 6 Fontsource imports, base body + heading rules, smooth-scroll + reduced-motion guard, recovered semantic classes (.subheading, .lead, .resume-section, .nav-link, .nav-link[aria-current='page'], .profile-pic, .text-primary, .btn-primary, .text-muted, .social-icon), and the @media (min-width: 768px) .project-card:nth-child(even) alternating-row fallback.
- Clean rebuild produces ≥ 6 Fontsource .woff2 files in `dist/_astro/`.
- Tailwind marker `#abc123` still present in `dist/_astro/*.css` (no Pitfall 29 regression).
- Zero Google Fonts URLs in `dist/`.
- `npx vitest run tests/smoke.test.ts` exits 0 (scoped smoke gate).
</success_criteria>

<output>
Create `.planning/phases/03-sections-navigation/03-02-SUMMARY.md` documenting:
- Final line count of `global.css`.
- Output of `find dist/_astro -name '*.woff2'` (list of Fontsource files emitted).
- Confirmation `grep -l '#abc123' dist/_astro/*.css` returns ≥ 1 file (marker survives).
- Confirmation `grep -rE "fonts\.googleapis|fonts\.gstatic" dist/` exits 1 (zero CDN font refs).
- Confirmation `npx vitest run tests/smoke.test.ts` exits 0 with all 5 smoke assertions passing.
- Note the new helper rules added beyond the original target: `.text-muted`, `.social-icon` + `:hover`, and the project-card alternating-row fallback inside `@media (min-width: 768px)`.
</output>
</content>
</invoke>