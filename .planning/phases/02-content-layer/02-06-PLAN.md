---
phase: 02-content-layer
plan: 06
type: execute
wave: 4
depends_on:
    - 02-01
    - 02-02
    - 02-03
    - 02-04
    - 02-05
files_modified:
    - tests/content-validation.test.ts
    - tests/__fixtures__/malformed-project.md
autonomous: true
requirements:
    - CONTENT-08
must_haves:
    truths:
        - 'CONTENT-08: `npx astro check` validates content; a deliberately-malformed fixture missing the required `title` field fails the build with a Zod error referencing both the entry id (`__test__`) and the field path (`"path": ["title"]`).'
        - 'Phase 2 SC #1 (positive path): `await getCollection("projects")` over the real data returns length > 0; entries have typed (non-`any`) data — D-28 + RESEARCH.md Recipe R9 second describe block.'
        - 'T10: Body validation lives in `tests/content-validation.test.ts`; Astro schema cannot gate markdown body — D-20 + RESEARCH.md Open Question 1.'
        - 'D-26: Fixture at `tests/__fixtures__/malformed-project.md`, no `title` field.'
        - 'D-27: try/finally cleanup; temp file at `src/content/projects/__test__/index.md`; assert exit non-zero AND combined stdout/stderr contains BOTH `__test__` AND the regex `"path"\\s*:\\s*\\[\\s*"title"\\s*\\]`.'
        - 'D-28: positive-path assertion uses `expectTypeOf(projects[0]!.data.title).toEqualTypeOf<string>()` from Vitest.'
    artifacts:
        - path: 'tests/content-validation.test.ts'
          provides: 'Three Vitest assertions: (1) astro check fails on malformed fixture, (2) getCollection("projects") returns >0 typed entries, (3) every list-collection entry has non-empty body.'
          contains: "describe('CONTENT-08"
        - path: 'tests/__fixtures__/malformed-project.md'
          provides: 'Fixture with frontmatter missing the required `title` field — used by the spawn-astro-check sub-test.'
    key_links:
        - from: 'tests/content-validation.test.ts'
          to: 'tests/__fixtures__/malformed-project.md'
          via: 'copyFileSync to temp dir src/content/projects/__test__/index.md before spawnSync of npx astro check'
          pattern: 'copyFileSync\\(FIXTURE,'
---

<objective>
Author the Vitest test file that proves CONTENT-08 and the positive-path Phase 2 SC #1, plus the malformed fixture it consumes. The test runs three sub-tests per RESEARCH.md Recipe R9: (1) a malformed-fixture spawn of `npx astro check` that must exit non-zero with the entry id + `title` field path in stderr/stdout; (2) `await getCollection('projects')` returns 13 typed entries (positive baseline); (3) every list-collection entry's markdown body is non-empty (D-20 enforcement, since Astro's schema doesn't cover the body).

Purpose: This test gates CONTENT-08 ("deliberately-malformed test fixture fails the build with a useful Zod error referencing the field path") and the positive path of Phase 2 SC #1 ("`await getCollection('projects')` returns length > 0 and entries are typed"). It also closes the body-non-emptiness gap left by Astro's schema-validation-only-covers-frontmatter limitation (RESEARCH.md Open Question 1).

Output: 1 test file (`tests/content-validation.test.ts`, ~60 lines) + 1 fixture file (`tests/__fixtures__/malformed-project.md`, ~10 lines). The test runs as part of the existing Vitest suite (`vitest.config.ts` already includes `tests/**/*.test.ts`).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer/02-CONTEXT.md
@.planning/phases/02-content-layer/02-RESEARCH.md
@.planning/phases/02-content-layer/02-PATTERNS.md
@.planning/phases/02-content-layer/02-01-PLAN.md
@.planning/phases/02-content-layer/02-04-PLAN.md
@.planning/phases/02-content-layer/02-05-PLAN.md
@tests/smoke.test.ts
@tests/global-setup.ts
@tests/__fixtures__/HydrationCheck.tsx
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
    <name>Task 1: Author tests/__fixtures__/malformed-project.md (no title field)</name>
    <files>tests/__fixtures__/malformed-project.md</files>
    <read_first>
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 1033-1045 (the verbatim target fixture content) and Open Question 3 at line 1192 (recommendation to DROP the `cover` field so the only Zod error path is `["title"]` — easier to debug)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 573-602 — fixture conventions; header-comment style mirroring `tests/__fixtures__/HydrationCheck.tsx`
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-26 (fixture path; missing-title contract)
        - tests/__fixtures__/HydrationCheck.tsx (15 lines) — location/comment-style analog
        - src/content.config.ts (post-Plan-01) — projects schema requires `title: trimmedString()` (i.e., `z.string().trim().min(1)`); removing it surfaces the Zod error this test asserts on
    </read_first>
    <action>
        Create `tests/__fixtures__/malformed-project.md` with: (a) a YAML frontmatter block delimited by `---` lines containing exactly one inner field `order: 999` and intent comments above it; (b) a non-empty markdown body explaining the fixture's purpose. The frontmatter MUST NOT contain a `title` field (the absence is the assertion target). Apply Open Question 3 recommendation: also OMIT the `cover` field — having `title` be the sole missing required field keeps the assertion focused on a single Zod error path (`["title"]`).

        Required body text: a single sentence stating the fixture deliberately lacks a title. The body must be non-empty so that if (defensively) it ever gets scanned by the body-non-emptiness sub-test, that test does not compound failures with the title-missing test (RESEARCH.md line 601 caveat).

        Intent header inside the frontmatter (as YAML comments using `#`): note (a) deliberately missing title, (b) consumed by `tests/content-validation.test.ts` CONTENT-08, (c) the assertion is that `astro check` cites `path: ["title"]`. Pattern matches the `tests/__fixtures__/HydrationCheck.tsx` top-of-file comment style.

        The `order: 999` value is defensive — it sorts the fixture last if it ever leaks into rendering during a test run that races with cleanup. The whole file should be ~10 lines.

        Anti-patterns to avoid:
        - Do NOT include a `title:` line (the fixture's contract is its ABSENCE — D-26).
        - Do NOT include a `cover:` field (Open Question 3 — isolating to a single missing field).
        - Do NOT name the file `malformed.md` or anything ambiguous — the canonical path is `tests/__fixtures__/malformed-project.md` per D-26.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f tests/__fixtures__/malformed-project.md && ! grep -qE '^title:' tests/__fixtures__/malformed-project.md && ! grep -qE '^cover:' tests/__fixtures__/malformed-project.md && grep -qE '^order:\s*999' tests/__fixtures__/malformed-project.md && grep -q 'title' tests/__fixtures__/malformed-project.md && BODY="$(awk '/^---$/{n++; next} n==2' tests/__fixtures__/malformed-project.md | tr -d '[:space:]')" && test -n "$BODY"</automated>
    </verify>
    <acceptance_criteria>
        - `tests/__fixtures__/malformed-project.md` exists.
        - File contains NO line starting with `title:` (the missing-title invariant — D-26).
        - File contains NO line starting with `cover:` (Open Question 3 — single-error fixture).
        - File contains the line `order: 999` (defensive sort-last).
        - File body (content after the second `---`) is non-empty.
        - File mentions the word `title` SOMEWHERE (likely in the intent comment) so a future reader understands why the file is malformed — matches the intent of the fixture.
    </acceptance_criteria>
    <done>
        Fixture is in place with the single-missing-field contract. Task 2 can spawn `npx astro check` against a copy of it and assert on the Zod error.
    </done>
</task>

<task type="auto" tdd="false">
    <name>Task 2: Author tests/content-validation.test.ts (3 sub-tests: malformed spawn, positive getCollection, body non-empty)</name>
    <files>tests/content-validation.test.ts</files>
    <read_first>
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 974-1031 (Recipe R9 — the AUTHORITATIVE test scaffold; copy structure verbatim and adjust counts for the actual 13-project baseline), Open Question 1 (line 1066 — body validation must be in Vitest, not Zod), Open Question 2 (line 1187 — verify the `astro:content` static import works under Vitest 4 + getViteConfig; if it fails, fall back to spawning a child process)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 489-570 — full pattern reference for the test file (imports, REPO_ROOT/FIXTURE/TEMP_DIR constants, spawnSync convention copied from `tests/global-setup.ts`, two-describe-block structure, 60_000 ms timeout caveat, v6 stderr regex shape per Pitfall P2-V4 — DO NOT use a clean v5-style "title is required" assertion)
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-26 (fixture path), D-27 (try/finally cleanup, temp at `src/content/projects/__test__/index.md`, assertions: status !== 0, stderr contains `__test__` AND `"path": ["title"]`), D-28 (positive sub-test uses `expectTypeOf`)
        - tests/smoke.test.ts (55 lines) — STRONG analog for imports, describe/it structure, path resolution, `process.cwd()` root
        - tests/global-setup.ts (18 lines) — STRONG analog for the spawnSync pattern; reuse the `spawnSync('npx', [...])` shape but with `encoding: 'utf8'` instead of `stdio: 'inherit'` so stderr/stdout can be captured for assertions
        - vitest.config.ts — confirms tests/**/*.test.ts pickup AND that `getViteConfig` is wired (the latter is what makes `await import('astro:content')` resolvable inside test files)
        - .planning/research/PITFALLS.md lines 701-723 (Pitfall 28 — getCollection predicate pattern; the positive-path sub-test does NOT need a predicate since there are no drafts in M1, but the test should be future-compatible)
    </read_first>
    <action>
        Create `tests/content-validation.test.ts` modeled after `tests/smoke.test.ts` and Recipe R9 (RESEARCH.md lines 974-1031).

        Imports section: `import { spawnSync } from 'node:child_process'`, `import { copyFileSync, existsSync, mkdirSync, rmSync } from 'node:fs'`, `import { dirname, join } from 'node:path'`, `import { describe, expect, expectTypeOf, it } from 'vitest'`. (`existsSync` may be unused — drop it if so; lint will catch.)

        Top-level constants per PATTERNS.md lines 555-562:
        - `REPO_ROOT = process.cwd()` (mirrors `tests/smoke.test.ts:11`)
        - `FIXTURE = join(REPO_ROOT, 'tests/__fixtures__/malformed-project.md')`
        - `TEMP_DIR = join(REPO_ROOT, 'src/content/projects/__test__')`
        - `TEMP_FILE = join(TEMP_DIR, 'index.md')`

        Three describe/it blocks:

        **Block 1 — CONTENT-08 schema-validation gate (one `it` per D-27 + Recipe R9 first describe block):**
        - `describe('CONTENT-08: schema validation', () => { ... })`
        - One `it('astro check fails on a malformed project entry with a useful Zod error', () => { ... }, 60_000)` — 60-second timeout because `astro check` is slow (Recipe R9 line 1007 caveat).
        - Test body: `mkdirSync(dirname(TEMP_FILE), { recursive: true })` then `copyFileSync(FIXTURE, TEMP_FILE)` inside a `try { ... } finally { rmSync(TEMP_DIR, { recursive: true, force: true }); }`.
        - Inside try: `const result = spawnSync('npx', ['astro', 'check'], { cwd: REPO_ROOT, encoding: 'utf8' })`. Mirror the `spawnSync` shape from `tests/global-setup.ts:9-13` but use `encoding: 'utf8'` (not `stdio: 'inherit'`) so the output is captured.
        - Three assertions per D-27:
          (a) `expect(result.status).not.toBe(0)` (D-27 — non-zero exit on schema failure; RESEARCH.md Open Question 2)
          (b) Let `const combined = (result.stdout ?? '') + '\n' + (result.stderr ?? '')` — D-27 says "stderr" but Recipe R9 combines stdout+stderr because v6 may surface the error on either stream (Pitfall P2-V4).
          (c) `expect(combined).toContain('__test__')` — the entry id from the temp directory name (D-27).
          (d) `expect(combined).toMatch(/"path"\s*:\s*\[\s*"title"\s*\]/)` — the Zod issue path matching the missing field (D-27 + Recipe R9 line 1003). This v6 format target — do NOT use a clean v5-style `expect(stderr).toContain('title is required')` (would FAIL on v6 per Pitfall P2-V4).

        **Block 2 — Phase 2 SC #1 positive path (one `it` per D-28 + Recipe R9 second describe block, FIRST sub-test):**
        - `describe('CONTENT-08 positive path (SC #1)', () => { ... })`
        - One `it('await getCollection("projects") returns >0 typed entries', async () => { ... })` — `async` because `getCollection` is async.
        - Body: `const { getCollection } = await import('astro:content');` (dynamic import — Open Question 2 confirms this works under `getViteConfig`; if it fails at runtime, fall back to the child-process approach described in Open Question 2 — but try the static-import path first).
        - `const projects = await getCollection('projects');` then `expect(projects.length).toBeGreaterThan(0);` (Phase 2 SC #1 first half; Plan 02-04 authored 13 entries so this should be 13).
        - Type assertion per D-28: `expectTypeOf(projects[0]!.data.title).toEqualTypeOf<string>()`. The `!` non-null assertion is acceptable here because the prior `expect(projects.length).toBeGreaterThan(0)` proves index 0 is defined; the `!` satisfies `noUncheckedIndexedAccess` (Phase 1 D-10).

        **Block 2 (second `it`) — body non-emptiness across all list collections (D-20 + Recipe R9 third sub-test):**
        - Inside the same `describe('CONTENT-08 positive path (SC #1)', () => { ... })`.
        - `it('every list-collection entry has a non-empty markdown body (D-20)', async () => { ... })`.
        - Body: `const { getCollection } = await import('astro:content');` then iterate `for (const collection of ['projects', 'work', 'education', 'leadership', 'testimonials'] as const) { const entries = await getCollection(collection); for (const entry of entries) { expect(entry.body?.trim().length, `${collection}/${entry.id}`).toBeGreaterThan(0); } }`. The third argument to `expect` is the custom error message — names the failing entry path (e.g., `projects/face-detection`) for fast debug.
        - Assumption A2 in RESEARCH.md line 1173: Astro 6's glob loader exposes `entry.body` directly (no need for `render()`). If this assumption fails at runtime, the test changes to `(await render(entry)).body` — one-line tweak.

        Top-of-file comment header (mirror `tests/smoke.test.ts:1-5`): cite (a) CONTENT-08 + D-26..D-28, (b) Recipe R9 as the source pattern, (c) the v6 stderr regex caveat (Pitfall P2-V4 — do not use v5-clean assertions).

        Anti-patterns to avoid:
        - Do NOT assert on a clean v5-style error message (would fail on v6 — Pitfall P2-V4 + RESEARCH.md State-of-the-Art table line 1159).
        - Do NOT skip the try/finally cleanup — leaving `src/content/projects/__test__/index.md` in place would break the next `astro check` / `astro build` run (Recipe R9 line 1004).
        - Do NOT use `stdio: 'inherit'` on the spawnSync — that pipes output to the parent process and leaves `result.stdout`/`result.stderr` empty (Recipe R9 line 994 + PATTERNS.md line 524).
        - Do NOT lower the timeout below `60_000` ms — `astro check` is slow (Recipe R9 line 1007).
        - Do NOT inline the malformed fixture's contents in the test source — use the file at `tests/__fixtures__/malformed-project.md` per D-26 (so the file's intent is discoverable in the fixtures dir).
        - Do NOT use `import { getCollection } from 'astro:content'` as a top-level static import — Recipe R9 uses the dynamic `await import` form inside the test body to keep the import isolated to the `async` test (Open Question 2 mitigation).
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f tests/content-validation.test.ts && grep -q "import { spawnSync } from 'node:child_process'" tests/content-validation.test.ts && grep -q "import { copyFileSync" tests/content-validation.test.ts && grep -q "import { describe, expect, expectTypeOf, it } from 'vitest'" tests/content-validation.test.ts && grep -q "tests/__fixtures__/malformed-project.md" tests/content-validation.test.ts && grep -q "src/content/projects/__test__" tests/content-validation.test.ts && grep -q "spawnSync('npx', \['astro', 'check'\]" tests/content-validation.test.ts && grep -qE "encoding:\s*'utf8'" tests/content-validation.test.ts && grep -q "60_000" tests/content-validation.test.ts && grep -qE "\.not\.toBe\(0\)" tests/content-validation.test.ts && grep -q "'__test__'" tests/content-validation.test.ts && grep -qE '/"path".*\\\\[.*"title".*\\\\]/' tests/content-validation.test.ts && grep -q "await import('astro:content')" tests/content-validation.test.ts && grep -q "getCollection('projects')" tests/content-validation.test.ts && grep -q "expectTypeOf" tests/content-validation.test.ts && grep -q "toEqualTypeOf<string>" tests/content-validation.test.ts && grep -q "entry.body" tests/content-validation.test.ts && grep -qE "try\s*\{" tests/content-validation.test.ts && grep -qE "finally\s*\{" tests/content-validation.test.ts && grep -q "rmSync" tests/content-validation.test.ts && npx vitest run tests/content-validation.test.ts 2>&1 | tee /tmp/vitest-content.log; TEST_STATUS="${PIPESTATUS[0]}"; if [ "$TEST_STATUS" != "0" ]; then echo "vitest failed (exit $TEST_STATUS)"; cat /tmp/vitest-content.log; exit 1; fi</automated>
    </verify>
    <acceptance_criteria>
        - `tests/content-validation.test.ts` exists.
        - File imports `spawnSync` from `node:child_process`, `copyFileSync`/`mkdirSync`/`rmSync` from `node:fs`, `dirname`/`join` from `node:path`, and `describe`/`expect`/`expectTypeOf`/`it` from `vitest`.
        - File contains the constants `FIXTURE`, `TEMP_DIR`, `TEMP_FILE` referring to the paths in D-27.
        - File invokes `spawnSync('npx', ['astro', 'check'], ...)` with `encoding: 'utf8'` (NOT `stdio: 'inherit'`).
        - File contains a timeout of `60_000` on the malformed-fixture test.
        - File contains the assertion `result.status).not.toBe(0)` (D-27 part 1).
        - File contains the literal substring `'__test__'` in an assertion (D-27 part 2 — entry id).
        - File contains a regex assertion of the shape `"path"\s*:\s*\[\s*"title"\s*\]` (D-27 part 3 — Zod issue path).
        - File contains `await import('astro:content')` (Open Question 2 — dynamic import inside test).
        - File contains `getCollection('projects')` AND `expectTypeOf` with `toEqualTypeOf<string>` (D-28 — positive path + type assertion).
        - File contains `entry.body` reference (D-20 third sub-test).
        - File contains BOTH `try {` and `finally {` (D-27 cleanup invariant).
        - `npx vitest run tests/content-validation.test.ts` exits with code 0 — all three sub-tests pass.
    </acceptance_criteria>
    <done>
        `tests/content-validation.test.ts` exercises the CONTENT-08 schema gate, the Phase 2 SC #1 positive path, and the D-20 body-non-emptiness invariant. Plan 02-07 (parity verification) can rely on these tests as the build-gate baseline.
    </done>
</task>

</tasks>

<verification>
    <automated>
        cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &&
        test -f tests/__fixtures__/malformed-project.md &&
        test -f tests/content-validation.test.ts &&
        ! grep -qE '^title:' tests/__fixtures__/malformed-project.md &&
        npx vitest run tests/content-validation.test.ts
    </automated>
</verification>

<success_criteria>
    - Fixture exists with no `title:` field and a non-empty body.
    - Test file imports, structure, assertions, and timeout match D-26..D-28 + Recipe R9.
    - All three sub-tests pass when `npx vitest run tests/content-validation.test.ts` is invoked.
    - `src/content/projects/__test__/index.md` does NOT remain after the test run (try/finally cleanup verified).
</success_criteria>

<output>
    Create `.planning/phases/02-content-layer/02-06-SUMMARY.md` when done. Summary must list: (a) the three test outcomes (status, stderr/stdout excerpts for the malformed test, count of projects returned by getCollection, count of body-non-empty entries verified per collection), (b) whether the dynamic `astro:content` import worked under the existing `vitest.config.ts` OR whether a fallback was needed (Open Question 2), (c) confirmation that `src/content/projects/__test__/` does not exist after the test run, (d) the actual stderr regex match for the `path: ["title"]` assertion (for Plan 02-07 / future debug).
</output>
</content>
</invoke>