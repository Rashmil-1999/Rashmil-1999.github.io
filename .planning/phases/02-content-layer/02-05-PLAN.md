---
phase: 02-content-layer
plan: 05
type: execute
wave: 3
depends_on:
    - 02-01
files_modified:
    - src/content/work/phionike-solutions/index.md
    - src/content/work/pear-technologies/index.md
    - src/content/education/dj-sanghvi-college-of-engineering/index.md
    - src/content/education/st-rocks-college-of-science-and-commerce/index.md
    - src/content/education/st-francis-school/index.md
    - src/content/leadership/dj-unicode/index.md
    - src/content/testimonials/roopam-mishra/index.md
autonomous: true
requirements:
    - CONTENT-02
must_haves:
    truths:
        - 'CONTENT-02 (non-projects): The four image-free list collections (work, education, leadership, testimonials) use `glob({ pattern: "**/*.md", base: "./src/content/<name>" })` and are each populated with per-item markdown.'
        - 'D-02: No `slug:` field in any frontmatter (Astro 6 auto-derives `entry.id` from the directory name).'
        - 'D-19: Long-form descriptions (work `description`, leadership `description`, testimonials `text`) live in the markdown BODY.'
        - 'D-22: `peAR Technologies.` casing preserved verbatim (user voice).'
        - 'D-22: School name casing preserved (`DWARKADAS J. SANGHVI COLLEGE OF ENGINEERING`, `ST. Rocks College Of Science and Commerce`, `ST. Francis School`).'
        - 'T10 prerequisite (data side): every list-collection entry`s body is non-empty so the Vitest body-non-emptiness assertion in Plan 02-06 passes (D-20 + RESEARCH.md Recipe R9 third sub-test).'
        - 'Testimonials user/role/org split: snapshot `user: "Roopam Mishra, Founder of Phionike Solutions: Design-Tech Studio."` is split into `user: Roopam Mishra`, `role: Founder`, `org: "Phionike Solutions: Design-Tech Studio"` per RESEARCH.md schema lines 673-679.'
        - 'D-04: Every entry has `order:` set to its step-10 position (10, 20, 30 for education; 10, 20 for work; 10 for leadership/testimonials).'
    artifacts:
        - path: 'src/content/work/<slug>/index.md'
          provides: '2 work entries (phionike-solutions, pear-technologies) with company/title/duration in frontmatter, description in body'
        - path: 'src/content/education/<slug>/index.md'
          provides: '3 education entries with name/degree/graduated/score? in frontmatter'
        - path: 'src/content/leadership/<slug>/index.md'
          provides: '1 leadership entry (dj-unicode)'
        - path: 'src/content/testimonials/<slug>/index.md'
          provides: '1 testimonial entry (roopam-mishra)'
    key_links:
        - from: 'src/content/<collection>/<slug>/index.md'
          to: 'projects schema or other static schema in src/content.config.ts'
          via: 'glob() loader scans **/*.md under base; frontmatter validated by Zod schema'
          pattern: 'src/content/(work|education|leadership|testimonials)/.+/index\\.md'
---

<objective>
Author 7 markdown entry files across 4 image-free list collections: 2 work entries, 3 education entries, 1 leadership entry, 1 testimonial. Each file's frontmatter matches the collection's static schema (set in Plan 02-01); the long-form description (or testimonial text) lives in the markdown body per D-19. Education entries — whose snapshot bodies are empty — get a one-sentence body per PATTERNS.md line 390 so the body-non-emptiness gate (D-20 / Plan 02-06 third sub-test) passes.

Purpose: With the singletons (Plan 02-02) and projects (Plan 02-04) covered, this plan completes the migration of every remaining JSON top-level branch (`resumeData.work`, `resumeData.education`, `leaderships`, `testimonials`). Together with the project entries from Plan 02-04, every snapshot entry is now reachable via `getCollection()`.

Output: 7 `.md` files in 4 directories. Total file count post-plan: 13 (projects) + 2 (work) + 3 (education) + 1 (leadership) + 1 (testimonials) = 20 markdown entries across 5 list collections.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer/02-CONTEXT.md
@.planning/phases/02-content-layer/02-RESEARCH.md
@.planning/phases/02-content-layer/02-PATTERNS.md
@.planning/phases/02-content-layer/02-01-PLAN.md
@.planning/snapshots/m1-source/resumeData.json
@.planning/research/PITFALLS.md
@src/content.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
    <name>Task 1: Author 2 work entries + 3 education entries</name>
    <files>src/content/work/phionike-solutions/index.md, src/content/work/pear-technologies/index.md, src/content/education/dj-sanghvi-college-of-engineering/index.md, src/content/education/st-rocks-college-of-science-and-commerce/index.md, src/content/education/st-francis-school/index.md</files>
    <read_first>
        - .planning/snapshots/m1-source/resumeData.json lines 42-74 — `resumeData.education[]` (3 entries, lines 42-61) + `resumeData.work[]` (2 entries, lines 62-74). Source data.
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 332-390 — work section (lines 332-359) + education section (lines 361-390) with slug tables and the critical "body non-emptiness" note for education
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 329-340 (Pattern 2 — static schema for image-free collections), lines 642-655 (education schema with `score: trimmedString().optional()`), Recipe R6 lines 893-915 (snapshot → destination map)
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-02 (slug = kebab-case from company/school name), D-04 (step-10 order), D-19 (description in body), D-22 (preserve `peAR Technologies.` casing, school name casing)
        - src/content.config.ts (post-Plan-01) — `work` schema fields: `company`, `title`, `duration` (all trimmed), `url` (optional), `order`, `draft`; `education` schema fields: `name`, `degree`, `graduated` (all trimmed), `score` (optional trimmed), `order`, `draft`
    </read_first>
    <action>
        Author 5 markdown files. Frontmatter delimited by `---`, body below.

        **Work (2 entries — `src/content/work/<slug>/index.md`):**
        - `phionike-solutions/index.md` — frontmatter:
            - `company: "Phionike Solutions: Design-Tech Studio"` (quote — contains colon which is a YAML special char in unquoted scalars)
            - `title: Research and Development Intern`
            - `duration: "May 2019 - July 2019"` (quote — contains hyphen+space which YAML can parse as a flow indicator)
            - `order: 10`
            - Body: snapshot `description` field from `resumeData.json:67` (full paragraph about VUI research, Alexa skill, blog reader; ~600 chars). Trim leading/trailing whitespace per D-21.
        - `pear-technologies/index.md` — frontmatter:
            - `company: "peAR Technologies."` (preserve `peAR` casing AND the trailing period per D-22 — RESEARCH.md line 915 + PATTERNS.md line 350)
            - `title: Professional Photographer`
            - `duration: "January 2019 - February 2019"`
            - `order: 20`
            - Body: snapshot description from `resumeData.json:73`.

        **Education (3 entries — `src/content/education/<slug>/index.md`):**
        - `dj-sanghvi-college-of-engineering/index.md` — frontmatter:
            - `name: DWARKADAS J. SANGHVI COLLEGE OF ENGINEERING` (preserve UPPERCASE per D-22)
            - `degree: Bachelors in Computer Engineering`
            - `graduated: "August 2017 - May 2021"`
            - `score: "CGPA: 9.963 / 10"` (quote — colons)
            - `order: 10`
            - Body: one-sentence summary (snapshot has no body text). Per PATTERNS.md line 390 recommendation: write a single line like `Bachelor's in Computer Engineering with a CGPA of 9.963 / 10.` This satisfies D-20 body-non-emptiness without inventing content (it's a restatement of frontmatter).
        - `st-rocks-college-of-science-and-commerce/index.md` — frontmatter:
            - `name: ST. Rocks College Of Science and Commerce` (preserve casing per D-22)
            - `degree: HSC Maharashtra 11th-12th`
            - `graduated: "July 2015 - June 2017"`
            - `score: "Finals: 89.4% , JEE mains :22000 rank, JEE Advanced :7500 rank."` (snapshot has irregular spacing — preserve per D-22; quote — colons)
            - `order: 20`
            - Body: one-sentence summary like `Higher Secondary Certificate, Maharashtra Board, completed July 2015 - June 2017.`
        - `st-francis-school/index.md` — frontmatter:
            - `name: ST. Francis School`
            - `degree: ICSE Board`
            - `graduated: May 2015`
            - `score: "Finals: 92.5%"`
            - `order: 30`
            - Body: one-sentence summary like `ICSE Board schooling, graduated May 2015.`

        YAML scalar quoting rule: any value containing `:`, `%`, leading/trailing whitespace, leading hyphen, or characters that could be parsed as YAML flow indicators MUST be double-quoted. Apply consistently.

        Anti-patterns to avoid:
        - Do NOT add a `slug:` field (D-02).
        - Do NOT add a `description:` field (D-19 — descriptions are in body).
        - Do NOT leave education bodies empty (D-20 — the body-non-emptiness Vitest assertion in Plan 02-06 third sub-test would fail; PATTERNS.md line 390 explicitly calls this out). Each education entry MUST have at least one non-empty paragraph after the frontmatter.
        - Do NOT add a `draft:` field (defaults to false per schema; omitted is correct).
        - Do NOT change `peAR Technologies.` casing or strip the trailing period (D-22).
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && for path in src/content/work/phionike-solutions/index.md src/content/work/pear-technologies/index.md src/content/education/dj-sanghvi-college-of-engineering/index.md src/content/education/st-rocks-college-of-science-and-commerce/index.md src/content/education/st-francis-school/index.md; do test -f "$path" || { echo "MISSING: $path"; exit 1; }; done && grep -q 'peAR Technologies' src/content/work/pear-technologies/index.md && grep -q 'DWARKADAS J. SANGHVI COLLEGE OF ENGINEERING' src/content/education/dj-sanghvi-college-of-engineering/index.md && grep -q '^order: 10' src/content/work/phionike-solutions/index.md && grep -q '^order: 20' src/content/work/pear-technologies/index.md && grep -q '^order: 10' src/content/education/dj-sanghvi-college-of-engineering/index.md && grep -q '^order: 20' src/content/education/st-rocks-college-of-science-and-commerce/index.md && grep -q '^order: 30' src/content/education/st-francis-school/index.md && ! grep -rqE '^slug:' src/content/work/ src/content/education/ && ! grep -rqE '^description:' src/content/work/ src/content/education/ && for f in src/content/work/*/index.md src/content/education/*/index.md; do BODY="$(awk '/^---$/{n++; next} n==2' "$f" | tr -d '[:space:]')"; test -n "$BODY" || { echo "EMPTY BODY: $f"; exit 1; }; done</automated>
    </verify>
    <acceptance_criteria>
        - All 5 markdown files exist at the specified paths.
        - `peAR Technologies.` (lowercase 'pe', uppercase 'AR', trailing period) appears verbatim in `src/content/work/pear-technologies/index.md` — D-22.
        - `DWARKADAS J. SANGHVI COLLEGE OF ENGINEERING` (full uppercase) appears verbatim in the DJ Sanghvi education entry.
        - Step-10 orders: work entries `10, 20`; education entries `10, 20, 30`.
        - No `slug:` field anywhere in `src/content/work/` or `src/content/education/`.
        - No `description:` field anywhere in those collections.
        - Every body (content after the second `---`) is non-empty — verified by an awk-based grep that extracts the post-frontmatter body and asserts non-empty per file.
    </acceptance_criteria>
    <done>
        2 work + 3 education entries authored; descriptions live in body; education bodies populated with summary sentences (D-20 satisfied without inventing content); casing preserved per D-22.
    </done>
</task>

<task type="auto" tdd="false">
    <name>Task 2: Author 1 leadership entry + 1 testimonial entry</name>
    <files>src/content/leadership/dj-unicode/index.md, src/content/testimonials/roopam-mishra/index.md</files>
    <read_first>
        - .planning/snapshots/m1-source/resumeData.json lines 328-341 — `testimonials[]` (1 entry, lines 328-333) + `leaderships[]` (1 entry, lines 334-341)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 392-433 — leadership section (lines 392-413) + testimonials section (lines 415-433) with slug, target shapes, and the testimonial user/role/org split note
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 658-679 — leadership schema (org/title/duration/url?/order/draft) + testimonials schema (user trimmed; role optional; org optional)
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-02 (slug from org/user), D-19 (text → body), D-21 (trim), D-22 (preserve org name casing)
        - src/content.config.ts (post-Plan-01) — leadership schema: `org`, `title`, `duration` (trimmed), `url` (optional), `order`, `draft`; testimonials schema: `user` (trimmed), `role` (optional trimmed), `org` (optional trimmed), `order`, `draft`
    </read_first>
    <action>
        Author 2 markdown files.

        **Leadership (1 entry — `src/content/leadership/dj-unicode/index.md`):**
        - Frontmatter:
            - `org: DJ Unicode`
            - `title: Full Stack Mentor & Events Head` (the `&` should be quoted if unsafe in unquoted scalar — YAML 1.2 allows `&` inside scalars EXCEPT at the start; here `&` is mid-string and safe, but quoting `"Full Stack Mentor & Events Head"` is the conservative call)
            - `duration: "August 2018 - Present"`
            - `order: 10`
        - Body: snapshot `description` from `resumeData.json:339` (the full ~600-char paragraph about knowledge transfer, DJ Unicode, project lead role, workshops, growth from 45→70 developers). Trim per D-21.

        **Testimonials (1 entry — `src/content/testimonials/roopam-mishra/index.md`):**

        Snapshot stores `user: "Roopam Mishra, Founder of Phionike Solutions: Design-Tech Studio."` as one combined string and the testimonial text in the `text` field. The new schema splits this into separate fields per RESEARCH.md schema (lines 673-679 + PATTERNS.md line 437).

        - Frontmatter:
            - `user: Roopam Mishra`
            - `role: Founder` (extracted from the snapshot combined string)
            - `org: "Phionike Solutions: Design-Tech Studio"` (extracted; quote because of the colon; preserve `Design-Tech Studio` casing per D-22)
            - `order: 10`
        - Body: snapshot `text` field from `resumeData.json:330` (the full testimonial text — "His sincere contribution and innovative thinking...great asset to the team."). Trim per D-21.

        Anti-patterns to avoid:
        - Do NOT keep the snapshot's combined string in a single `user:` field — split into `user` / `role` / `org` per PATTERNS.md line 437.
        - Do NOT add a `text:` or `description:` frontmatter field — the testimonial body IS the text (D-19).
        - Do NOT add a `slug:` field.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f src/content/leadership/dj-unicode/index.md && test -f src/content/testimonials/roopam-mishra/index.md && grep -q '^org: DJ Unicode' src/content/leadership/dj-unicode/index.md && grep -qE '^title:.*Full Stack Mentor.*Events Head' src/content/leadership/dj-unicode/index.md && grep -q '^order: 10' src/content/leadership/dj-unicode/index.md && grep -q '^user: Roopam Mishra' src/content/testimonials/roopam-mishra/index.md && grep -q '^role: Founder' src/content/testimonials/roopam-mishra/index.md && grep -qE '^org:.*Phionike Solutions: Design-Tech Studio' src/content/testimonials/roopam-mishra/index.md && grep -q '^order: 10' src/content/testimonials/roopam-mishra/index.md && ! grep -qE '^text:' src/content/testimonials/roopam-mishra/index.md && ! grep -qE '^description:' src/content/leadership/dj-unicode/index.md && ! grep -rqE '^slug:' src/content/leadership/ src/content/testimonials/ && for f in src/content/leadership/dj-unicode/index.md src/content/testimonials/roopam-mishra/index.md; do BODY="$(awk '/^---$/{n++; next} n==2' "$f" | tr -d '[:space:]')"; test -n "$BODY" || { echo "EMPTY BODY: $f"; exit 1; }; done</automated>
    </verify>
    <acceptance_criteria>
        - Both files exist at the specified paths.
        - Leadership entry has `org: DJ Unicode`, `title:` containing both `Full Stack Mentor` and `Events Head`, `duration:`, `order: 10`.
        - Testimonial entry has separate `user: Roopam Mishra`, `role: Founder`, and `org:` containing `Phionike Solutions: Design-Tech Studio` — verifies the user/role/org split was applied.
        - Neither file has a `text:` or `description:` frontmatter field (D-19 — text is body).
        - Neither file has a `slug:` field (D-02).
        - Both file bodies are non-empty (D-20).
    </acceptance_criteria>
    <done>
        Leadership + testimonials authored; the user/role/org split is applied to the testimonial; descriptions/texts live in body; all 7 non-project markdown entries from this plan + the 13 from Plan 02-04 = 20 total list-collection entries.
    </done>
</task>

<task type="auto" tdd="false">
    <name>Task 3: Run astro check to validate the full content tree against schemas</name>
    <files></files>
    <read_first>
        - All entries authored in Tasks 1 + 2 above
        - Plan 02-04 (project entries) — also covered by this validation pass
        - Plan 02-02 (singleton YAMLs) — also covered
        - src/content.config.ts (post-Plan-01) — the schemas being validated
        - .planning/research/PITFALLS.md lines 20-103 (Pitfall 1 / 2 / 3 / 4 — common config issues to debug if check fails)
    </read_first>
    <action>
        Run `npx astro check` from the repo root. This pass validates:
        - `src/content.config.ts` compiles (Plan 02-01 already ran this gate; included again for full-tree coverage).
        - All 3 singleton YAML files (`about.yaml`, `skills.yaml`, `links.yaml`) parse and validate against their `defineCollection` schemas (Plan 02-02 content).
        - All 13 projects entries + 2 work + 3 education + 1 leadership + 1 testimonial = 20 list-collection entries validate against their schemas — `cover` paths resolve via `image()`, `tech_stack` is an array of non-empty strings, urls satisfy `z.url()`, etc.
        - Strictest tsconfig (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`) is satisfied by Astro's generated `astro:content` types.

        If `astro check` exits non-zero, the executor MUST fix the underlying entries (do NOT relax the schemas in `src/content.config.ts`). Common failure modes per Pitfall 1/4 + RESEARCH.md Pitfall Inventory:
        - YAML parse error in a singleton → check quoting around colons/percentages/special chars in `about.yaml`/`skills.yaml`/`links.yaml`.
        - `image()` validation failure → check the `cover` path is `./<filename>` (NOT `/<filename>`) and the file actually exists in the same directory (Plan 02-03 colocated images).
        - `z.url()` failure → check the url is a real URL with scheme (`https://` or `http://`; the snapshot uses `http://` for github which 02-02 should have normalized).
        - Unknown field error → check for stray `description:` / `slug:` / `text:` in frontmatter (D-19 / D-02 / D-19).
        - Empty-string failure → check for trimmed-to-empty fields (D-21 trim applied; `.min(1)` then fires).

        Do NOT silence errors with `// @ts-ignore` or schema `.passthrough()` or `.optional()` band-aids. Fix the underlying data.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && npx astro check 2>&1 | tee /tmp/astro-check-final.log; STATUS="${PIPESTATUS[0]}"; if [ "$STATUS" != "0" ]; then echo "astro check FAILED (exit $STATUS)"; cat /tmp/astro-check-final.log; exit 1; fi; echo "astro check OK"</automated>
    </verify>
    <acceptance_criteria>
        - `npx astro check` exits with code 0 from the repo root.
        - `stdout`/`stderr` contains no occurrences of `InvalidContentEntryDataError` (the Astro 6 schema-validation error class — RESEARCH.md Open Question 2).
        - `stdout`/`stderr` contains no occurrences of `error TS` (TypeScript errors from strictest tsconfig).
        - The validation covers all 20 list-collection entries + 3 singletons by virtue of the loaders scanning their `base` directories.
    </acceptance_criteria>
    <done>
        The full content tree (3 singletons + 20 list-collection entries) validates against the Phase 2 schemas. Plan 02-06 can now author the malformed-fixture test confident the baseline is clean.
    </done>
</task>

</tasks>

<verification>
    <automated>
        cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &&
        test "$(find src/content/work -name 'index.md' | wc -l | tr -d ' ')" = '2' &&
        test "$(find src/content/education -name 'index.md' | wc -l | tr -d ' ')" = '3' &&
        test "$(find src/content/leadership -name 'index.md' | wc -l | tr -d ' ')" = '1' &&
        test "$(find src/content/testimonials -name 'index.md' | wc -l | tr -d ' ')" = '1' &&
        grep -q 'peAR Technologies' src/content/work/pear-technologies/index.md &&
        grep -q '^role: Founder' src/content/testimonials/roopam-mishra/index.md &&
        npx astro check
    </automated>
</verification>

<success_criteria>
    - 7 markdown entries authored across 4 collections (2 work + 3 education + 1 leadership + 1 testimonial).
    - Casings preserved per D-22 (peAR Technologies., uppercase school names).
    - Testimonial split applied: separate `user`, `role`, `org` fields.
    - Education bodies populated to satisfy D-20.
    - `npx astro check` exits 0 — schema validation passes against the full migrated content tree.
</success_criteria>

<output>
    Create `.planning/phases/02-content-layer/02-05-SUMMARY.md` when done. Summary must list: (a) each entry's slug + order + frontmatter fields used, (b) the education-body one-liners written (so the parity diff in Plan 02-07 can flag these as "synthesized, not in snapshot"), (c) the testimonial user/role/org split verbatim, (d) full `astro check` output (truncated to 50 lines if verbose).
</output>
</content>
</invoke>