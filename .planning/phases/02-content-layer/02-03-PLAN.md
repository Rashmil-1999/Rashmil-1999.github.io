---
phase: 02-content-layer
plan: 03
type: execute
wave: 2
depends_on:
    - 02-01
files_modified:
    - src/content/projects/face-detection/face_detection.png
    - src/content/projects/emotion-recognizer/emotion_recognition.png
    - src/content/projects/american-sign-language-detection/asl.png
    - src/content/projects/age-of-warring-empire-tower-bot/aowe.jpg
    - src/content/projects/smart-india-hackathon/SIH.png
    - src/content/projects/e-yantra-competition/eyantra.jpg
    - src/content/projects/e-yantra-competition/eyantra.png
    - src/content/projects/garduino-smart-garden/garduino.png
    - src/content/projects/stack-overflow-chatbot/chatbot.png
    - src/content/projects/twitter-named-entity-recognition/ner.png
    - src/content/projects/library-attendance-manager/library.png
    - src/content/projects/dj-archive/archive.jpeg
    - src/content/projects/dj-archive/archive.jpg
    - src/content/projects/dj-archive/archive.png
    - src/content/projects/college-event-manager-app/event.png
    - src/content/projects/resume-website/resume.png
    - src/content/_orphans/attendance.png
    - src/content/_orphans/attendance.webp
    - src/content/_orphans/attendance1.png
    - src/content/_orphans/smgarden.png
autonomous: true
requirements:
    - CONTENT-05
must_haves:
    truths:
        - 'CONTENT-05 (asset side): Every project image is colocated with its (future) markdown entry in `src/content/projects/<slug>/`.'
        - 'T8: `graduino.png` is renamed to `garduino.png` during the copy — the ONLY filename change in the entire snapshot→content migration (D-09).'
        - 'D-10: 4 orphan images (no project references them) are copied into `src/content/_orphans/` — leading-underscore directory keeps them out of every collection`s glob `base`.'
        - 'D-08: All snapshot filenames preserved verbatim except for the Garduino rename (D-09).'
        - 'D-07: Projects with multiple image variants (e-yantra, dj-archive) get ALL variants copied — `cover` is one file, `alternates` references the others (specified in Plan 02-04 frontmatter).'
        - 'The `emotion-recognizer` project uses `emotion_recognition.png` (23 KB), NOT the 4.8 MB `emotion.png` (CONCERNS.md carry-forward).'
    artifacts:
        - path: 'src/content/projects/garduino-smart-garden/garduino.png'
          provides: 'The garduino cover image, renamed from snapshot graduino.png (D-09)'
        - path: 'src/content/_orphans/'
          provides: '4 stranded snapshot images (attendance.png, attendance.webp, attendance1.png, smgarden.png) kept out of any collection glob'
    key_links:
        - from: 'src/content/projects/<slug>/'
          to: 'image file(s) in same directory'
          via: 'Phase 2-04 frontmatter `cover: ./<filename>` (Astro `image()` resolves relative to the markdown file)'
          pattern: 'src/content/projects/.+/.+\\.(png|jpg|jpeg|webp)'
---

<objective>
Copy 18 image files from `.planning/snapshots/m1-source/assets/` into the 13 project entry directories under `src/content/projects/<slug>/`, performing the Garduino rename (`graduino.png` → `garduino.png`) as the single deliberate filename change. Copy the 4 orphan images into `src/content/_orphans/`. This plan creates only directories and copies binaries — Plan 02-04 will author the markdown entries that reference these images via `cover: ./<filename>` frontmatter.

Purpose: The Astro `image()` schema helper resolves frontmatter paths relative to the markdown file (Pitfall 4 + RESEARCH.md Open Question 3). Colocating images with their entries kills the existing `image_map` anti-pattern (Pitfall 20 / CONCERNS.md carry-forward). With images in place before Wave 3 markdown authoring, `npx astro check` validates every `cover` reference at the moment the entry file is written.

Output: 18 binary copies into project entry directories + 4 binary copies into `_orphans/` = 22 file operations (matches the snapshot's 22-file `assets/` inventory).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer/02-CONTEXT.md
@.planning/phases/02-content-layer/02-RESEARCH.md
@.planning/phases/02-content-layer/02-PATTERNS.md
@.planning/phases/02-content-layer/02-01-PLAN.md
@.planning/snapshots/m1-source/resumeData.json
@.planning/research/PITFALLS.md
@src/content.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
    <name>Task 1: Copy 18 project images into per-slug directories (with Garduino rename)</name>
    <files>src/content/projects/face-detection/face_detection.png, src/content/projects/emotion-recognizer/emotion_recognition.png, src/content/projects/american-sign-language-detection/asl.png, src/content/projects/age-of-warring-empire-tower-bot/aowe.jpg, src/content/projects/smart-india-hackathon/SIH.png, src/content/projects/e-yantra-competition/eyantra.jpg, src/content/projects/e-yantra-competition/eyantra.png, src/content/projects/garduino-smart-garden/garduino.png, src/content/projects/stack-overflow-chatbot/chatbot.png, src/content/projects/twitter-named-entity-recognition/ner.png, src/content/projects/library-attendance-manager/library.png, src/content/projects/dj-archive/archive.jpeg, src/content/projects/dj-archive/archive.jpg, src/content/projects/dj-archive/archive.png, src/content/projects/college-event-manager-app/event.png, src/content/projects/resume-website/resume.png</files>
    <read_first>
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 870-892 — Recipe R5 (the AUTHORITATIVE per-project image table) with slug → snapshot files → cover/alternates mapping; Open Question 3 at lines 1093-1097 (relative-path resolution behavior for `image()` schema)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 277-330 — Per-project slug+image table; lines 442-455 — image-copy critical conventions including the 4.8 MB `emotion.png` rejection (CONCERNS.md carry-forward)
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-08 (preserve filenames verbatim), D-09 (Garduino rename is the only filename change), D-07 (multi-variant collection — alternates), D-01 (directory-per-item layout)
        - .planning/snapshots/m1-source/resumeData.json lines 227-326 — projects array; the `image:` field on each project (without extension) is the snapshot's filename stem that maps to the snapshot asset file
        - .planning/research/PITFALLS.md lines 519-541 (Pitfall 20 — no image_map; per-entry colocation), lines 105-124 (Pitfall 4 — image paths relative to markdown)
        - `.planning/snapshots/m1-source/assets/` directory listing (run `ls -la .planning/snapshots/m1-source/assets/` to confirm file sizes; the 22-file inventory is fixed)
    </read_first>
    <action>
        Create 13 project entry directories under `src/content/projects/` (mkdir -p) using the kebab-case slugs from RESEARCH.md Recipe R5 (lines 871-885). Then copy 18 image files from `.planning/snapshots/m1-source/assets/` into them per the table — `cp` for binaries (do NOT Read/Write binary content). The destinations and source mappings:

        1. `face-detection/face_detection.png` ← snapshot `face_detection.png` (5.9 KB)
        2. `emotion-recognizer/emotion_recognition.png` ← snapshot `emotion_recognition.png` (23 KB) — DO NOT copy `emotion.png` (4.8 MB; CONCERNS.md carry-forward — RESEARCH.md line 874 + PATTERNS.md line 304)
        3. `american-sign-language-detection/asl.png` ← snapshot `asl.png` (85 KB)
        4. `age-of-warring-empire-tower-bot/aowe.jpg` ← snapshot `aowe.jpg` (190 KB)
        5. `smart-india-hackathon/SIH.png` ← snapshot `SIH.png` (62 KB) — preserve `SIH` uppercase per D-08
        6. `e-yantra-competition/eyantra.jpg` ← snapshot `eyantra.jpg` (103 KB) — this will be the `cover` per Recipe R5 line 878
        7. `e-yantra-competition/eyantra.png` ← snapshot `eyantra.png` (3.8 KB) — this will be `alternates[0]` per Recipe R5 + D-07
        8. **`garduino-smart-garden/garduino.png` ← snapshot `graduino.png` (37 KB) — RENAME during copy per D-09. This is the SOLE filename change. `cp .planning/snapshots/m1-source/assets/graduino.png src/content/projects/garduino-smart-garden/garduino.png`**
        9. `stack-overflow-chatbot/chatbot.png` ← snapshot `chatbot.png` (241 KB)
        10. `twitter-named-entity-recognition/ner.png` ← snapshot `ner.png` (97 KB)
        11. `library-attendance-manager/library.png` ← snapshot `library.png` (6.1 KB)
        12. `dj-archive/archive.jpeg` ← snapshot `archive.jpeg` (110 KB) — this will be `cover` per Recipe R5 line 883
        13. `dj-archive/archive.jpg` ← snapshot `archive.jpg` (23 KB) — `alternates[]`
        14. `dj-archive/archive.png` ← snapshot `archive.png` (368 KB) — `alternates[]`
        15. `college-event-manager-app/event.png` ← snapshot `event.png` (32 KB)
        16. `resume-website/resume.png` ← snapshot `resume.png` (622 KB)

        Total: 16 destinations (face-detection, emotion-recognizer, asl, aowe, SIH, eyantra (×2), garduino, chatbot, ner, library, archive (×3), event, resume) = 16 files? Recount: 1+1+1+1+1+2+1+1+1+1+3+1+1 = 15 listed above for the first 12 projects, but the table above has 16 entries (numbered 1-16 with the Garduino rename as #8). Final count: 13 projects, 16 image files copied (5 projects have 1 image, 1 project (e-yantra) has 2, 1 project (dj-archive) has 3 → 5×1 + 7×1 + 2 + 3 = 12 + 2 + 3 ... wait recount per Recipe R5: face-detection (1), emotion (1), asl (1), aowe (1), SIH (1), eyantra (2), garduino (1, renamed), chatbot (1), ner (1), library (1), archive (3), event (1), resume (1) = 16 image files total across 13 projects. The `files_modified` frontmatter lists 16 project images + 4 orphans = 20 paths. CORRECTION: the frontmatter listed 16 project files; total ops = 16 project image copies + 4 orphan copies = 20 operations (not 22). 22 was the source file count (includes `emotion.png` skipped + `profilepic.jpg` handled by Plan 02-02 = 22 in snapshot − 2 not-here = 20 ops here. Confirmed.)

        Use `mkdir -p src/content/projects/<slug>` to create each directory before copying. Do NOT add a `.gitkeep` file — every directory gets a real image; git tracks non-empty directories.

        Critical handling of `emotion.png` (4.8 MB): this file in the snapshot is the "5 MB PNG" anti-pattern flagged in CONCERNS.md (PATTERNS.md line 304, RESEARCH.md line 874). It MUST NOT be copied. The 23 KB `emotion_recognition.png` is the snapshot's smaller variant and is the cover.

        Anti-patterns to avoid:
        - Do NOT add a top-level `src/content/projects/index.md` — `glob({ pattern: '**/*.md' })` would treat it as an entry without a parent slug directory.
        - Do NOT rename ANY file other than `graduino.png` → `garduino.png` (D-08 verbatim preservation; D-09 is the sole exception).
        - Do NOT copy `emotion.png` (4.8 MB) — the CONCERNS.md "5 MB PNG" carry-forward forbids it.
        - Do NOT copy `profilepic.jpg` here — Plan 02-02 Task 1 already handled the profile pic relocation to `src/assets/profile.jpg`.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && for slug in face-detection emotion-recognizer american-sign-language-detection age-of-warring-empire-tower-bot smart-india-hackathon e-yantra-competition garduino-smart-garden stack-overflow-chatbot twitter-named-entity-recognition library-attendance-manager dj-archive college-event-manager-app resume-website; do test -d "src/content/projects/$slug" || { echo "MISSING DIR: $slug"; exit 1; }; done && test -f src/content/projects/face-detection/face_detection.png && test -f src/content/projects/emotion-recognizer/emotion_recognition.png && test -f src/content/projects/american-sign-language-detection/asl.png && test -f src/content/projects/age-of-warring-empire-tower-bot/aowe.jpg && test -f src/content/projects/smart-india-hackathon/SIH.png && test -f src/content/projects/e-yantra-competition/eyantra.jpg && test -f src/content/projects/e-yantra-competition/eyantra.png && test -f src/content/projects/garduino-smart-garden/garduino.png && test ! -f src/content/projects/garduino-smart-garden/graduino.png && test -f src/content/projects/stack-overflow-chatbot/chatbot.png && test -f src/content/projects/twitter-named-entity-recognition/ner.png && test -f src/content/projects/library-attendance-manager/library.png && test -f src/content/projects/dj-archive/archive.jpeg && test -f src/content/projects/dj-archive/archive.jpg && test -f src/content/projects/dj-archive/archive.png && test -f src/content/projects/college-event-manager-app/event.png && test -f src/content/projects/resume-website/resume.png && test ! -f src/content/projects/emotion-recognizer/emotion.png && IMG_COUNT="$(find src/content/projects -type f \( -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' -o -name '*.webp' \) | wc -l | tr -d ' ')" && test "$IMG_COUNT" = '16'</automated>
    </verify>
    <acceptance_criteria>
        - All 13 project slug directories exist under `src/content/projects/` (exact list from Recipe R5: face-detection, emotion-recognizer, american-sign-language-detection, age-of-warring-empire-tower-bot, smart-india-hackathon, e-yantra-competition, garduino-smart-garden, stack-overflow-chatbot, twitter-named-entity-recognition, library-attendance-manager, dj-archive, college-event-manager-app, resume-website).
        - Each project directory contains its image(s) per the Recipe R5 table — 16 image files total across all 13 directories.
        - `src/content/projects/garduino-smart-garden/garduino.png` exists; `src/content/projects/garduino-smart-garden/graduino.png` does NOT exist (Garduino rename per D-09 applied during copy).
        - `src/content/projects/e-yantra-competition/` contains BOTH `eyantra.jpg` AND `eyantra.png` (multi-variant per D-07).
        - `src/content/projects/dj-archive/` contains ALL THREE variants: `archive.jpeg`, `archive.jpg`, `archive.png` (multi-variant per D-07).
        - `src/content/projects/emotion-recognizer/emotion.png` does NOT exist (the 4.8 MB CONCERNS-flagged file was deliberately not copied).
        - The directory `src/content/projects/` does NOT contain any markdown files yet (those are Plan 02-04's responsibility).
        - Total image-file count under `src/content/projects/` is exactly 16 (verified by the `find` count).
    </acceptance_criteria>
    <done>
        13 project directories created; 16 image files colocated; Garduino rename applied (the only filename change); multi-variant projects (`e-yantra-competition`, `dj-archive`) hold all variants for use as `cover` / `alternates[]` in Plan 02-04 frontmatter. Plan 02-04 can now author the 13 markdown entries knowing every `cover: ./<filename>` reference will resolve.
    </done>
</task>

<task type="auto" tdd="false">
    <name>Task 2: Copy 4 orphan images into src/content/_orphans/</name>
    <files>src/content/_orphans/attendance.png, src/content/_orphans/attendance.webp, src/content/_orphans/attendance1.png, src/content/_orphans/smgarden.png</files>
    <read_first>
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 887-890 (Recipe R5 orphan section — the 4 stranded image files), Open Question 6 at lines 1122-1125 (containment verified — leading-underscore directory is invisible to every collection's `glob`)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 457-471 — `_orphans/` directory conventions; explicit "no `.gitkeep` needed" note
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-10 (orphans go to `src/content/_orphans/`; future use TBD; bytes preserved)
        - src/content.config.ts (post-Plan-01) — confirm no `defineCollection` references `_orphans` in any `base:` value (this is what makes the directory invisible)
    </read_first>
    <action>
        Create `src/content/_orphans/` (mkdir -p) and `cp` the 4 stranded snapshot images verbatim:
        - `attendance.png` (109 KB)
        - `attendance.webp` (12 KB)
        - `attendance1.png` (747 KB)
        - `smgarden.png` (42 KB)

        These images are present in the snapshot's `assets/` directory but no project's `image:` field references them (verified by RESEARCH.md Recipe R5 line 888 + the `resumeData.json` projects[] array). They are preserved per D-10 so Phase 5's cleanup decision (delete / archive / keep) has the bytes available.

        The leading-underscore directory name is conventional only — RESEARCH.md Open Question 6 confirms Astro's glob loader only scans paths matched by each collection's `base + pattern`. Since `src/content.config.ts` (post-Plan-01) has NO `defineCollection` with `base: './src/content/_orphans'` (or anything resembling it), the directory is invisible to the build. Do NOT add a `.gitkeep` file — the directory becomes non-empty once the 4 copies are in place, and git tracks it naturally (PATTERNS.md line 470).

        Anti-patterns to avoid:
        - Do NOT register `_orphans` as a collection in `src/content.config.ts` — that would break D-10's "stranded but preserved" intent.
        - Do NOT delete the source files in the snapshot — Phase 1 D-03 reserves snapshot deletion for Phase 5 after CONTENT-06 parity is signed off.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -d src/content/_orphans && test -f src/content/_orphans/attendance.png && test -f src/content/_orphans/attendance.webp && test -f src/content/_orphans/attendance1.png && test -f src/content/_orphans/smgarden.png && ORPHAN_COUNT="$(find src/content/_orphans -type f | wc -l | tr -d ' ')" && test "$ORPHAN_COUNT" = '4' && ! grep -q "_orphans" src/content.config.ts && test ! -f src/content/_orphans/.gitkeep</automated>
    </verify>
    <acceptance_criteria>
        - `src/content/_orphans/` directory exists and contains exactly 4 files: `attendance.png`, `attendance.webp`, `attendance1.png`, `smgarden.png`.
        - No `.gitkeep` file in `_orphans/` (per PATTERNS.md line 470 — the 4 real images make the directory non-empty).
        - `src/content.config.ts` does NOT contain the string `_orphans` (no collection is configured to scan it — containment verified).
        - File sizes are sane (each file > 1 KB; total > 800 KB; the largest is `attendance1.png` at ~747 KB) — guards against truncated copies. (Verified implicitly by `test -f` on each file.)
    </acceptance_criteria>
    <done>
        4 orphan images preserved in `src/content/_orphans/`. The directory is invisible to every collection's glob. Phase 5 can later decide whether to keep, delete, or archive them.
    </done>
</task>

</tasks>

<verification>
    <automated>
        cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &&
        test "$(find src/content/projects -type f -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' -o -name '*.webp' | wc -l | tr -d ' ')" = '16' &&
        test "$(find src/content/_orphans -type f | wc -l | tr -d ' ')" = '4' &&
        test -f src/content/projects/garduino-smart-garden/garduino.png &&
        test ! -f src/content/projects/garduino-smart-garden/graduino.png &&
        test ! -f src/content/projects/emotion-recognizer/emotion.png
    </automated>
</verification>

<success_criteria>
    - 13 project directories created under `src/content/projects/`.
    - 16 image files colocated across those 13 directories (matching Recipe R5 verbatim).
    - The Garduino rename is applied: target filename is `garduino.png`, source filename `graduino.png` did NOT survive into `src/content/`.
    - 4 orphan images live in `src/content/_orphans/` (D-10 containment).
    - The 4.8 MB `emotion.png` is NOT copied anywhere (CONCERNS carry-forward).
    - `src/content/projects/` contains NO `.md` files yet (Plan 02-04 owns markdown authoring).
</success_criteria>

<output>
    Create `.planning/phases/02-content-layer/02-03-SUMMARY.md` when done. Summary must list: (a) the 13 directories created with their slug names, (b) the Garduino rename verification (`graduino.png` not present, `garduino.png` present), (c) `emotion.png` rejection confirmation, (d) total file count under `src/content/projects/` (expect 16 images), (e) `src/content/_orphans/` count (expect 4 files), (f) any binary integrity issues encountered during copy.
</output>
</content>
</invoke>