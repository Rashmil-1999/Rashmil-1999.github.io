---
phase: 02-content-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - src/content.config.ts
autonomous: true
requirements:
    - CONTENT-01
    - CONTENT-04
    - CONTENT-07
must_haves:
    truths:
        - 'T1: Astro 6 config file location is src/content.config.ts (NOT src/content/config.ts) — Pitfall 1.'
        - 'T2: Every collection uses defineCollection({ loader: glob() | file(), schema }) — never the legacy `type:` key — Pitfall 2/3.'
        - 'T3: projects schema uses the function form `({ image }) => z.object({ cover: image() })` — Pitfall 4.'
        - 'T5: Schemas compile under strictest tsconfig (noUncheckedIndexedAccess, exactOptionalPropertyTypes).'
        - 'T6: Iconify identifier shape `prefix:name` is enforced by a regex iconSchema (D-16).'
        - 'CONTENT-01: src/content.config.ts defines all 8 collections (projects, work, education, leadership, testimonials, about, skills, links) with Zod schemas.'
        - 'CONTENT-04: every list-collection schema has `order: z.number().int().default(0)`.'
        - 'CONTENT-07: every schema uses CMS-neutral field names (no `_id`, `_ref`, `sys.*`, raw CSS class names).'
        - 'Zod 4 idioms — `z.email()`, `z.url()`, `.regex(..., { error: ... })` — used throughout (not v3 `.string().email()` / `.string().url()` / `{ message: ... }`).'
        - 'Zod `z` is imported from `astro:content`, NEVER from `zod` directly.'
    artifacts:
        - path: 'src/content.config.ts'
          provides: '8 defineCollection exports + `export const collections = {...}` for all 8 collections'
          contains: 'defineCollection, glob, file, image()'
    key_links:
        - from: 'src/content.config.ts'
          to: 'astro:content + astro/loaders'
          via: 'import { defineCollection, z } from "astro:content"; import { glob, file } from "astro/loaders";'
          pattern: 'from .astro:content.|from .astro/loaders.'
---

<objective>
Fill the Phase 1 D-11 placeholder at `src/content.config.ts` with the full 8-collection schema definition. Five list collections (`projects`, `work`, `education`, `leadership`, `testimonials`) use the `glob()` loader; three singletons (`about`, `skills`, `links`) use the `file()` loader. Schemas use Zod 4 idioms and are CMS-neutral. The `projects` schema uses the schema-function form so `image()` is in scope.

Purpose: Establish the type contract before any entries or images exist. With schemas in place, Wave 2 (singletons + image colocation) and Wave 3 (entry markdown files) can be written against known types — Astro auto-generates `.astro/content.d.ts` once the file compiles, and downstream tasks can rely on typed `getCollection`/`getEntry` calls.

Output: One modified file (`src/content.config.ts`, ~120 lines including comments) that `npx astro check` accepts cleanly under the strictest tsconfig — even though no entries exist yet (the schemas describe the shape, not the data).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-layer/02-CONTEXT.md
@.planning/phases/02-content-layer/02-RESEARCH.md
@.planning/phases/02-content-layer/02-PATTERNS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/PITFALLS.md
@src/content.config.ts
@tsconfig.json
@astro.config.mjs
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
    <name>Task 1: Replace placeholder src/content.config.ts with all 8 defineCollection exports</name>
    <files>src/content.config.ts</files>
    <read_first>
        - src/content.config.ts (current 4-line placeholder — entirely replaced)
        - .planning/phases/02-content-layer/02-RESEARCH.md (Recipe R1 + the "Full src/content.config.ts" code block at lines 600-751 is the canonical source — copy structure from there; Pattern 1 at lines 295-321; Pattern 2 at lines 323-340; Pattern 3 at lines 342-371; Pattern 4 at lines 398-410; Open Question 4 at lines 1099-1111 for `file()` loader semantics; Open Question 5 at lines 1113-1120 for strictest-tsconfig caveats)
        - .planning/phases/02-content-layer/02-PATTERNS.md (lines 45-150: "src/content.config.ts (config, build-time)" section — anti-patterns to avoid: never `import { z } from 'zod'`, never `type: 'content'`, never `z.string().email()` / `.url()` / `{ message: ... }` — use Zod 4 idioms)
        - .planning/phases/02-content-layer/02-CONTEXT.md (D-01 through D-25 — all schema decisions are locked here)
        - .planning/research/PITFALLS.md lines 20-103 (Pitfall 1 — file location; Pitfall 2 — loader API; Pitfall 3 — file() shape; Pitfall 4 — image() schema-function form) and lines 489-541 (Pitfall 19 — single nav structure; Pitfall 20 — no image_map) and lines 701-723 (Pitfall 28 — getCollection predicate pattern)
        - .planning/phases/01-foundation/01-CONTEXT.md (D-10 strictest tsconfig — schemas must compile under noUncheckedIndexedAccess + exactOptionalPropertyTypes; D-11 the empty placeholder Phase 2 fills; D-23 lowercase ids)
        - tsconfig.json (5 lines — confirms `astro/tsconfigs/strictest`)
        - astro.config.mjs (confirms output: 'static', no base)
    </read_first>
    <action>
        Replace the entire contents of `src/content.config.ts` (file is currently a 4-line comment-only placeholder from Phase 1 D-11) with the schema source verbatim from RESEARCH.md lines 600-751 ("Full src/content.config.ts"). Required imports at top of file: `import { defineCollection, z } from 'astro:content';` and `import { glob, file } from 'astro/loaders';` — never `import { z } from 'zod'` (Pitfall 2/3 anti-pattern from PATTERNS.md line 67).

        Define shared helpers:
        - `iconSchema = z.string().regex(/^[a-z0-9-]+:[a-z0-9-]+$/, { error: 'icon must be in Iconify "prefix:name" form (e.g., "devicon:python")' });` — per D-16.
        - `trimmedString = (min = 1) => z.string().trim().min(min);` — per D-21; applied to ~24 string fields, so the helper pays for itself (CONTEXT.md Claude's Discretion).

        Five list collections via `glob()` per D-03 (Pitfall 2 — never legacy `type:`):
        - `projects` — schema-function form `({ image }) => z.object({ ... })` per D-07 + Pattern 1. Fields: `title: trimmedString()`, `tech_stack: z.array(trimmedString()).default([])`, `url: z.url().optional()`, `cover: image()`, `alternates: z.array(image()).optional()`, `order: z.number().int().default(0)`, `draft: z.boolean().default(false)`. Loader: `glob({ pattern: '**/*.md', base: './src/content/projects' })`.
        - `work` — static schema (no image). Fields: `company`, `title`, `duration` (all `trimmedString()`), `url: z.url().optional()`, `order`, `draft`. Loader: `glob({ pattern: '**/*.md', base: './src/content/work' })`.
        - `education` — static schema. Fields: `name`, `degree`, `graduated` (all `trimmedString()`), `score: trimmedString().optional()`, `order`, `draft`. Loader base: `./src/content/education`.
        - `leadership` — static schema. Fields: `org`, `title`, `duration` (`trimmedString()`), `url: z.url().optional()`, `order`, `draft`. Loader base: `./src/content/leadership`.
        - `testimonials` — static schema. Fields: `user: trimmedString()`, `role: trimmedString().optional()`, `org: trimmedString().optional()`, `order`, `draft`. Loader base: `./src/content/testimonials`.

        Three singletons via `file()` per Pitfall 3 + Open Question 4 (each schema describes ONE entry — never the wrapping array/map):
        - `about` — `loader: file('src/content/about.yaml')`. Fields per D-13: `first_name`, `last_name`, `current_status`, `contact_message`, `description`, `resume_download`, `profile_image` (all `trimmedString()`), `email: z.email()`, `social: z.array(z.object({ name: trimmedString(), url: z.url(), icon: iconSchema })).min(1)`. NOTE: `profile_image` is a string, not `image()` — D-11 + RESEARCH.md line 359.
        - `skills` — `loader: file('src/content/skills.yaml')`. Schema per D-15: `categories: z.array(z.object({ name: trimmedString(), order: z.number().int().default(0), items: z.array(z.object({ name: trimmedString(), icon: iconSchema })).min(1) })).min(1)`.
        - `links` — `loader: file('src/content/links.yaml')` (array form). Schema describes ONE nav entry per D-06 + D-14: `id: z.string().regex(/^[a-z][a-z0-9-]*$/, { error: 'id must be lowercase kebab-case, starting with a letter' })`, `label: trimmedString()`, `order: z.number().int().default(0)`. (The array iteration is handled by the loader; the schema is the per-entry shape — Pitfall 3.)

        End the file with `export const collections = { projects, work, education, leadership, testimonials, about, skills, links };` — RESEARCH.md lines 741-750.

        Top-of-file comment header documenting (a) Phase 2 fills the Phase 1 D-11 placeholder, (b) Zod 4 idioms (`z.email()`, `z.url()`, `{ error: '...' }`) per RESEARCH.md State of the Art line 1159, (c) `z` import from `astro:content` not `zod` — Pitfall 2/3 anti-pattern. Keep `src/content.config.ts` total under 130 lines.

        Do NOT install any new npm packages (RESEARCH.md "Package Legitimacy Audit" — Phase 2 adds zero deps). Do NOT add icon-pack devDependencies — those are Phase 3 per CONTEXT.md `<deferred>` block (T11).
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f src/content.config.ts && grep -c 'defineCollection' src/content.config.ts | awk '{ if ($1 == 8) print "OK 8 collections"; else { print "FAIL count=" $1; exit 1 } }' && grep -q "import { defineCollection, z } from 'astro:content'" src/content.config.ts && grep -q "import { glob, file } from 'astro/loaders'" src/content.config.ts && ! grep -q "from 'zod'" src/content.config.ts && ! grep -qE "type:\s*'content'|type:\s*\"content\"|type:\s*'data'|type:\s*\"data\"" src/content.config.ts && grep -q "z.email()" src/content.config.ts && grep -q "z.url()" src/content.config.ts && grep -q "export const collections" src/content.config.ts && grep -q "schema: ({ image }) =>" src/content.config.ts && npx astro check 2>&1 | tee /tmp/astro-check.log; test "${PIPESTATUS[0]}" = 0 || (echo "astro check failed"; cat /tmp/astro-check.log; exit 1)</automated>
    </verify>
    <acceptance_criteria>
        - `src/content.config.ts` exists and contains exactly 8 `defineCollection(` occurrences.
        - File imports `{ defineCollection, z }` from `'astro:content'` (NOT from `'zod'`).
        - File imports `{ glob, file }` from `'astro/loaders'`.
        - File does NOT contain the strings `type: 'content'`, `type: "content"`, `type: 'data'`, or `type: "data"` (Pitfall 2 — legacy `type:` keys are forbidden).
        - File does NOT contain the string `from 'zod'` (Pitfall — Zod must come from `astro:content`).
        - File contains the regex string `^[a-z0-9-]+:[a-z0-9-]+$` (icon regex, D-16).
        - File contains the regex string `^[a-z][a-z0-9-]*$` (links id regex, D-06).
        - File contains the substring `schema: ({ image }) =>` exactly once (only `projects` uses the schema-function form — D-07).
        - File contains `z.email()` and `z.url()` (Zod 4 idioms — not `.string().email()` / `.string().url()`).
        - File contains `order: z.number().int().default(0)` at least 5 times (one per list collection — CONTENT-04).
        - File contains `draft: z.boolean().default(false)` at least 5 times (one per list collection — D-05).
        - File contains `export const collections = {` (single export).
        - File contains all 8 collection identifiers (`projects`, `work`, `education`, `leadership`, `testimonials`, `about`, `skills`, `links`) inside the `export const collections = {...}` object literal.
        - `npx astro check` (run from repo root) exits with code 0 — no type errors under `astro/tsconfigs/strictest`. (No content entries exist yet; this gate verifies the schema file itself type-checks; downstream waves will produce content errors that fail the gate until they author entries.)
    </acceptance_criteria>
    <done>
        `src/content.config.ts` defines all 8 collections with Zod 4 idiomatic schemas; `npx astro check` exits 0; no legacy `type:` keys or `from 'zod'` imports; the `projects` schema is the only one using `({ image }) =>`. Wave 2 plans (02-02 singletons, 02-03 image colocation) can now reference the typed schemas.
    </done>
</task>

</tasks>

<verification>
    <automated>
        cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &&
        test "$(grep -c 'defineCollection' src/content.config.ts)" = '8' &&
        grep -q "image()" src/content.config.ts &&
        grep -q "iconSchema" src/content.config.ts &&
        grep -q "trimmedString" src/content.config.ts &&
        npx astro check
    </automated>
</verification>

<success_criteria>
    - `src/content.config.ts` no longer matches the Phase 1 D-11 placeholder (file size > 100 lines; contains `defineCollection` 8 times).
    - All 8 collections (`projects`, `work`, `education`, `leadership`, `testimonials`, `about`, `skills`, `links`) appear in the `export const collections` object literal.
    - Zod 4 idioms used throughout: `z.email()`, `z.url()`, `.regex(..., { error: ... })` — no Zod 3 deprecation forms.
    - `npx astro check` exits 0 (schema file compiles under strictest tsconfig).
    - No new npm packages installed in this plan (verify with `git diff package.json` — should be empty).
</success_criteria>

<output>
    Create `.planning/phases/02-content-layer/02-01-SUMMARY.md` when done. Summary must list: (a) which patterns from RESEARCH.md were applied verbatim vs adjusted, (b) the result of `npx astro check`, (c) any deviations from CONTEXT.md decisions (expect none), (d) whether `trimmedString` and `iconSchema` were factored as helpers vs inlined.
</output>
</content>
</invoke>