---
phase: 02-content-layer
plan: 02
type: execute
wave: 2
depends_on:
    - 02-01
files_modified:
    - src/content/about.yaml
    - src/content/skills.yaml
    - src/content/links.yaml
    - src/assets/profile.jpg
autonomous: true
requirements:
    - CONTENT-03
must_haves:
    truths:
        - 'CONTENT-03: Every singleton (`about`, `skills`, `links`) uses `file()` over one YAML file each.'
        - '`about.yaml` and `skills.yaml` use the object-map form (top-level key `about:` / `skills:` whose value is the entry data) — RESEARCH.md Open Question 4.'
        - '`links.yaml` uses the array form — entries are consumed via `getCollection("links")`.'
        - 'T6: Every `icon` string matches the Iconify regex `^[a-z0-9-]+:[a-z0-9-]+$` — D-16.'
        - 'T8 (singleton side): `profilepic.jpg` relocates to `src/assets/profile.jpg` — D-11.'
        - 'T9: `links.yaml` uses id `work` / label `Work` (NOT `experience`/`Experience`) — D-24 reconciles Phase 1 D-23.'
        - 'D-17: every snapshot CDN class is mapped to the audited Iconify identifier from RESEARCH.md Recipe R7 (corrections: `simple-icons:django`, `devicon:react`, `devicon:apple`, `devicon:windows8`, `devicon:amazonwebservices`, `devicon:nodejs`).'
        - 'D-21: trailing space on `first_name` ("Rashmil ") is trimmed.'
        - 'D-22: user voice preserved — `Postgre SQL`, `peAR Technologies.` (in other plans), `Tensorflow` casing, school name casing.'
    artifacts:
        - path: 'src/content/about.yaml'
          provides: 'Single about entry under top-level key `about:` (object-map form).'
          contains: 'about:'
        - path: 'src/content/skills.yaml'
          provides: 'Single skills entry under top-level key `skills:` (object-map form) with 6 categories and ~32 items.'
          contains: 'skills:'
        - path: 'src/content/links.yaml'
          provides: '7 nav entries as a top-level array: about, education, work, skills, projects, leadership, testimonials.'
          contains: 'id: about'
        - path: 'src/assets/profile.jpg'
          provides: 'Profile picture, relocated from snapshot `profilepic.jpg`.'
    key_links:
        - from: 'src/content/about.yaml'
          to: 'src/assets/profile.jpg'
          via: 'profile_image: ~/assets/profile.jpg (string path, NOT image() per D-11)'
          pattern: 'profile_image:'
        - from: 'src/content/skills.yaml'
          to: 'every items[].icon string'
          via: 'Iconify regex prefix:name'
          pattern: 'icon: [a-z0-9-]+:[a-z0-9-]+'
---

<objective>
Author the three singleton YAML files (`about.yaml`, `skills.yaml`, `links.yaml`) and relocate the profile picture from the snapshot to `src/assets/profile.jpg`. All data is migrated from `.planning/snapshots/m1-source/resumeData.json` with conservative normalization per D-21/D-22 and the audited Iconify identifier mapping per Recipe R7.

Purpose: Phase 2 ships the singleton data needed by Phase 3 SideNav / About / Skills rendering. The `file()` loader's object-map vs array form is honored per RESEARCH.md Open Question 4. The nav reconciliation `Experience → Work` (D-24) is enforced here because Phase 1 D-23 already pinned the lowercase id `work` in the smoke test and stub component.

Output: 3 YAML files in `src/content/` + 1 binary (`src/assets/profile.jpg`).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer/02-CONTEXT.md
@.planning/phases/02-content-layer/02-RESEARCH.md
@.planning/phases/02-content-layer/02-PATTERNS.md
@.planning/phases/02-content-layer/02-01-PLAN.md
@.planning/snapshots/m1-source/resumeData.json
@.planning/research/PITFALLS.md
@src/content.config.ts
@src/components/Work.astro
</context>

<tasks>

<task type="auto" tdd="false">
    <name>Task 1: Author src/content/about.yaml + relocate src/assets/profile.jpg</name>
    <files>src/content/about.yaml, src/assets/profile.jpg</files>
    <read_first>
        - .planning/snapshots/m1-source/resumeData.json lines 2-22 — source `about` block with the trailing-space `first_name: "Rashmil "` (D-21 trim target) and the two `social[]` entries with `className` fields that map to Iconify ids
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 153-202 — "src/content/about.yaml" pattern + the exact target YAML body
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 343-396 (Pattern 3 — `file()` loader object-map form for singletons), lines 877-915 (Recipe R5/R6 — profile pic relocation note + snapshot inventory)
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-11 (profile.jpg lives at `src/assets/profile.jpg`, referenced as a string path NOT `image()`), D-13 (about.yaml schema), D-17 (icon mapping — Devicon brand → simple-icons for social), D-21 (trim trailing space), D-22 (preserve user voice)
        - src/content.config.ts (post-Plan-01) — about schema lines: confirm `email: z.email()`, `profile_image: trimmedString()`, `social[].icon` is iconSchema (`^[a-z0-9-]+:[a-z0-9-]+$`)
        - .planning/snapshots/m1-source/assets/profilepic.jpg (1 MB binary — the file to copy/rename)
    </read_first>
    <action>
        Create `src/content/about.yaml` using the object-map form per RESEARCH.md Open Question 4 + Pattern 3. Top-level key MUST be `about:` (this becomes the entry id; consumption is `getEntry('about', 'about')` per RESEARCH.md line 396). Indented under `about:` the following fields from `.planning/snapshots/m1-source/resumeData.json` lines 2-21, with D-21 trim applied to `first_name`:

        - `first_name: Rashmil` — trimmed from snapshot `"Rashmil "` (D-21)
        - `last_name: Panchani`
        - `current_status: "Undergraduate BE Computer Engineering @Dwarkadas J. Sanghvi College of Engineering"` — quoted to preserve `@` symbol
        - `email: rashmilp833@gmail.com`
        - `contact_message: "Have a project for me? Think I'd be a good fit for your team? I'd love to hear from you, reach me out through my email."` — full string from snapshot, single-quoted-with-escapes acceptable
        - `description: "I am an athlete at heart, ... gallery soon."` — full ~600-char string from snapshot line 8 verbatim
        - `resume_download: Rashmil_Panchani.pdf` — D-13 (filename only; the file lives in `public/Rashmil_Panchani.pdf` and survived the Phase 1 wipe)
        - `profile_image: ~/assets/profile.jpg` — D-11 (Astro path alias; string path NOT `image()`; the file will be created by Task 1 second step)
        - `social:` — D-17 mapping converts each snapshot `className` to an Iconify id:
            - LinkedIn entry: name `LinkedIn`, url `https://www.linkedin.com/in/rashmil-panchani-67587a14b/` (verbatim from snapshot line 13), `icon: simple-icons:linkedin` (D-17 prefers simple-icons over devicon for brand social — RESEARCH.md Recipe R7 line 943)
            - Github entry: name `Github`, url `https://github.com/Rashmil-1999` (NORMALIZE `http://` from snapshot line 18 to `https://` per PATTERNS.md line 191 — the snapshot has `http:` which fails `z.url()` only if Zod 4 rejects http; it accepts http, but normalize to https because GitHub redirects), `icon: simple-icons:github` (Recipe R7 line 944).

        After writing the YAML, copy `.planning/snapshots/m1-source/assets/profilepic.jpg` to `src/assets/profile.jpg` (preserve binary content; the rename is the only change — D-11). Use `cp` (do not use Read/Write for binary files): `cp .planning/snapshots/m1-source/assets/profilepic.jpg src/assets/profile.jpg`. Leave the Phase 1 `src/assets/.gitkeep` in place (surgical-change principle — it is harmless).

        Anti-patterns to avoid (per PATTERNS.md line 196 + Pitfall 3): do NOT use a flat root object (e.g., starting the file with `first_name: ...` at column 0). The `file()` loader REQUIRES the object-map form (top-level key = entry id) per RESEARCH.md Open Question 4 — without the `about:` wrapper key, `astro check` will fail with `expected object, received <something>`.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f src/content/about.yaml && test -f src/assets/profile.jpg && head -1 src/content/about.yaml | grep -qE '^about:' && grep -qE '^\s+first_name:\s*Rashmil\s*$' src/content/about.yaml && grep -qE '^\s+email:\s*rashmilp833@gmail.com\s*$' src/content/about.yaml && grep -q 'simple-icons:linkedin' src/content/about.yaml && grep -q 'simple-icons:github' src/content/about.yaml && grep -q 'profile_image: ~/assets/profile.jpg' src/content/about.yaml && grep -q 'resume_download: Rashmil_Panchani.pdf' src/content/about.yaml && test "$(stat -f%z src/assets/profile.jpg 2>/dev/null || stat -c%s src/assets/profile.jpg)" -gt 100000</automated>
    </verify>
    <acceptance_criteria>
        - `src/content/about.yaml` exists and its first non-blank line is `about:` (object-map form).
        - The file's `first_name` value is exactly `Rashmil` (NO trailing space — D-21 trim applied; matched by regex `^\s+first_name:\s*Rashmil\s*$`).
        - The file contains `email: rashmilp833@gmail.com`.
        - The file contains `profile_image: ~/assets/profile.jpg` (path alias form, NOT `/src/assets/profile.jpg`).
        - The file contains `resume_download: Rashmil_Panchani.pdf` (filename only — D-13).
        - The file contains EXACTLY two `social:` entries with `icon: simple-icons:linkedin` and `icon: simple-icons:github` (D-17 — devicon brand glyphs replaced with simple-icons per RESEARCH.md Recipe R7).
        - The GitHub `url:` line uses `https://github.com/Rashmil-1999` (NOT `http://...` from the snapshot).
        - `src/assets/profile.jpg` exists and is > 100 KB (the snapshot file is ~1 MB; sanity check that the copy worked).
        - The `about.yaml` YAML is parseable: no unquoted special characters cause YAML parse errors (the `contact_message` and `description` fields contain apostrophes and question marks — wrap in double quotes per YAML 1.2).
    </acceptance_criteria>
    <done>
        `src/content/about.yaml` is a valid object-map singleton with all 8 about fields migrated from the snapshot (one trim applied, two icon strings mapped to Iconify, one URL normalized https). `src/assets/profile.jpg` is a verbatim copy of the snapshot's `profilepic.jpg` at its new path. Wave 3 plans can author project entries; Phase 3 will consume `getEntry('about', 'about')`.
    </done>
</task>

<task type="auto" tdd="false">
    <name>Task 2: Author src/content/skills.yaml (object-map singleton, 6 categories, 32 items, audited Iconify ids)</name>
    <files>src/content/skills.yaml</files>
    <read_first>
        - .planning/snapshots/m1-source/resumeData.json lines 76-225 — source `skill_array` (6 categories in order) + `skills` (keyed-by-category items with `class` and occasional `data-icon` fields)
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 772-825 (the FULL target `skills.yaml` body — copy this verbatim) + lines 917-953 (Recipe R7 — audited Iconify identifier table with 6 corrections from D-17 first pass)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 204-235 — "src/content/skills.yaml" pattern + the 6 critical Iconify corrections enumerated
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-15 (skills.yaml schema collapses parallel `skill_array[]` + `skills{}` into ordered `categories[].items[]`), D-17 (Iconify mapping), D-22 (preserve user voice — `Postgre SQL` spelling, `Tensorflow` casing kept verbatim)
        - src/content.config.ts (post-Plan-01) — skills schema requires `categories[].name`, `order`, `items[].name`, `items[].icon` (matching the iconSchema regex)
    </read_first>
    <action>
        Create `src/content/skills.yaml` using the object-map form (top-level key `skills:`). Under it, `categories:` is an array of 6 category objects matching the snapshot's `skill_array` order (D-15 collapses the parallel arrays):

        1. `name: "Programming Languages & Operating Systems"`, `order: 10`, items (9 in snapshot order): C → `devicon:c`, C++ → `devicon:cplusplus`, Java → `devicon:java`, Python → `devicon:python`, Go → `devicon:go`, JavaScript → `devicon:javascript`, Windows → `devicon:windows8` (Iconify drops `-original` suffix per Recipe R7 line 927), Linux → `devicon:linux`, macOS → `devicon:apple` (Iconify drops `-original` per Recipe R7 line 929).
        2. `name: "Database Technologies"`, `order: 20`, items (5): MySQL → `devicon:mysql` (snapshot says `My SQL`; D-22 says preserve user voice but RESEARCH.md skills.yaml line 792 normalizes to `MySQL` — keep snapshot name `My SQL` per D-22 strict reading — DEVIATION: PATTERNS.md and RESEARCH.md both write `MySQL`; use `MySQL` per researcher recommendation since this matches the standard product name, not a stylistic preference. Document the choice in the SUMMARY), `Postgre SQL` → `devicon:postgresql` (preserve `Postgre SQL` spelling per D-22 — RESEARCH.md line 793), MongoDB → `devicon:mongodb`, Redis → `devicon:redis`, GraphQL → `logos:graphql` (already canonical from snapshot).
        3. `name: "Web Development"`, `order: 30`, items (6): HTML5 → `devicon:html5`, CSS3 → `devicon:css3`, Django → `simple-icons:django` (CRITICAL: D-17 correction — `devicon:django` does NOT exist on Iconify; Recipe R7 line 936), NodeJS → `devicon:nodejs` (Iconify drops `-plain-wordmark` per Recipe R7 line 937), ReactJS → `devicon:react` (Iconify drops `-original` per Recipe R7 line 938), Bootstrap → `devicon:bootstrap`.
        4. `name: "Dev Ops"`, `order: 40`, items (3): Heroku → `devicon:heroku`, AWS → `devicon:amazonwebservices` (Iconify drops `-original` per Recipe R7 line 941), "Google Cloud Platform" → `logos:google-cloud` (normalize from snapshot `logos-google-cloud-platform` per Recipe R7 line 946).
        5. `name: "Tools and Frameworks"`, `order: 50`, items (6): NumPy → `simple-icons:numpy`, Pandas → `simple-icons:pandas`, Tensorflow → `simple-icons:tensorflow` (preserve `Tensorflow` casing per D-22 — snapshot says `Tensorflow` not `TensorFlow`), Keras → `simple-icons:keras`, NLTK → `lucide:code` (placeholder — D-17 + Recipe R7 line 951 — no brand glyph exists in any set), OpenCV → `simple-icons:opencv`.
        6. `name: "Version Control"`, `order: 60`, items (1): Git → `devicon:git`.

        Use the compact YAML flow form for items: `- { name: C, icon: devicon:c }` per RESEARCH.md skills.yaml example line 780. Wrap category names with `&` (e.g., `"Programming Languages & Operating Systems"`) in double quotes because the `&` character is a YAML anchor indicator and must be quoted. Wrap any item name containing spaces (`"Google Cloud Platform"`, `"Postgre SQL"`) in double quotes in the flow form to avoid YAML parser ambiguity.

        Total: 6 categories × ~5 items each = 30 items (verified count: 9+5+6+3+6+1 = 30 items).

        Anti-patterns to avoid: do NOT preserve the `class:` field from the snapshot — replace EVERY `class:` with `icon:` and map the value per Recipe R7. The snapshot's `data-icon:` field is also dropped — its value (after normalization) becomes the `icon:` value (RESEARCH.md Recipe R7 lines 945-952).
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f src/content/skills.yaml && head -1 src/content/skills.yaml | grep -qE '^skills:' && test "$(grep -cE 'name:\s*"' src/content/skills.yaml)" -ge 4 && test "$(grep -c '^\s*-\s*\{' src/content/skills.yaml)" -ge 25 && grep -q 'simple-icons:django' src/content/skills.yaml && grep -q 'devicon:react' src/content/skills.yaml && grep -q 'devicon:apple' src/content/skills.yaml && grep -q 'devicon:windows8' src/content/skills.yaml && grep -q 'devicon:amazonwebservices' src/content/skills.yaml && grep -q 'devicon:nodejs' src/content/skills.yaml && grep -q 'logos:graphql' src/content/skills.yaml && grep -q 'logos:google-cloud' src/content/skills.yaml && grep -q 'lucide:code' src/content/skills.yaml && ! grep -qE 'devicon:django|devicon:react-original|devicon:apple-original|devicon:windows8-original|devicon:amazonwebservices-original|devicon:nodejs-plain-wordmark' src/content/skills.yaml && ! grep -qE '^\s*class:' src/content/skills.yaml</automated>
    </verify>
    <acceptance_criteria>
        - `src/content/skills.yaml` exists and its first non-blank line is `skills:` (object-map form).
        - File contains 6 `name:` lines for categories (the regex `name:\s*"` matches the 4 multi-word category names; the 2 single-word categories `"Dev Ops"` and `"Version Control"` both contain spaces and must be quoted).
        - File contains at least 25 `- {` lines (compact flow-form items; total target is 30 items).
        - File contains ALL of the corrected Iconify identifiers: `simple-icons:django`, `devicon:react` (NOT `devicon:react-original`), `devicon:apple` (NOT `devicon:apple-original`), `devicon:windows8` (NOT `devicon:windows8-original`), `devicon:amazonwebservices` (NOT `devicon:amazonwebservices-original`), `devicon:nodejs` (NOT `devicon:nodejs-plain-wordmark`), `logos:graphql`, `logos:google-cloud`, `lucide:code`.
        - File does NOT contain the 6 INCORRECT identifiers from D-17's first pass (`devicon:django`, `devicon:react-original`, `devicon:apple-original`, `devicon:windows8-original`, `devicon:amazonwebservices-original`, `devicon:nodejs-plain-wordmark`).
        - File does NOT contain any line starting with `class:` (the snapshot's CDN class field has been replaced with `icon:`).
        - YAML parses without error (covered indirectly by the `astro check` gate run after Wave 3).
    </acceptance_criteria>
    <done>
        `src/content/skills.yaml` lists all 6 skill categories with all 30 items, each item's `icon` value matches the iconSchema regex AND the audited Iconify identifier from Recipe R7. The 6 D-17 corrections are applied. User-voice spellings preserved (`Postgre SQL`, `Tensorflow`). Phase 3 will consume `getEntry('skills', 'skills')`.
    </done>
</task>

<task type="auto" tdd="false">
    <name>Task 3: Author src/content/links.yaml (array singleton, 7 nav entries, Experience→Work reconciliation)</name>
    <files>src/content/links.yaml</files>
    <read_first>
        - .planning/snapshots/m1-source/resumeData.json lines 23-40 — source `sections[]` (labels — note `Experience` is index 2) and `links[]` (anchors — note `#experience` is index 2)
        - .planning/phases/02-content-layer/02-PATTERNS.md lines 237-275 — "src/content/links.yaml" pattern + critical conventions (array form, NOT object-map; `id` field becomes Astro entry id; `Experience → Work` reconciliation per D-24)
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 413-436 (the FULL target `links.yaml` body — copy verbatim), lines 398-410 (Pattern 4 — `file()` loader array form)
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-06 (id regex `^[a-z][a-z0-9-]*$`), D-14 (collapse parallel sections/links arrays into single shape — Pitfall 19), D-24 (canonical id `work` / label `Work`, reconciling snapshot `Experience` per Phase 1 D-23), D-25 (the 7 entries with their step-10 orders)
        - src/components/Work.astro lines 1-8 — confirms Phase 1 already uses `id="work"` (this is the reconciliation target — links.yaml must match)
        - src/content.config.ts (post-Plan-01) — links schema requires `id: z.string().regex(/^[a-z][a-z0-9-]*$/)`, `label`, `order`
        - .planning/research/PITFALLS.md lines 489-516 (Pitfall 19 — single nav structure, no parallel arrays)
    </read_first>
    <action>
        Create `src/content/links.yaml` as a YAML array (top-level is an array, NOT an object map — Pattern 4). 7 entries in step-10 order matching D-25:

        ```
        - id: about
          label: About
          order: 10
        - id: education
          label: Education
          order: 20
        - id: work
          label: Work
          order: 30
        - id: skills
          label: Skills
          order: 40
        - id: projects
          label: Projects
          order: 50
        - id: leadership
          label: Leadership
          order: 60
        - id: testimonials
          label: Testimonials
          order: 70
        ```

        Critical: the snapshot's index-2 entry is `Experience` / `#experience`. This entry is reconciled to `id: work` / `label: Work` per D-24 because (a) Phase 1 D-23 already chose `work` as the canonical id, (b) `src/components/Work.astro` stub uses `id="work"`, (c) `tests/smoke.test.ts` asserts `id="work"`. The reconciliation flows snapshot → links.yaml, not the reverse. Drop the `#` prefix from every entry per D-06 (the id is the bare anchor; Phase 3 prepends `#` at render).

        Every `id` value MUST satisfy the regex `^[a-z][a-z0-9-]*$`:
        - `about` (5 chars, all lowercase): OK
        - `education`: OK
        - `work`: OK
        - `skills`: OK
        - `projects`: OK
        - `leadership`: OK
        - `testimonials`: OK

        Anti-patterns to avoid (Pitfall 19 — explicit carry-forward from CONCERNS.md): do NOT preserve the snapshot's parallel `sections[]` + `links[]` arrays. The two parallel arrays in `resumeData.json:23-40` are intentionally collapsed into a single 7-entry array here so reordering a section never misaligns its label.
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f src/content/links.yaml && head -1 src/content/links.yaml | grep -qE '^-' && test "$(grep -c '^- id:' src/content/links.yaml)" = '7' && grep -q '^- id: work$' src/content/links.yaml && grep -q '^  label: Work$' src/content/links.yaml && ! grep -qE '^- id: experience' src/content/links.yaml && ! grep -qE '^  label: Experience' src/content/links.yaml && grep -q 'order: 10' src/content/links.yaml && grep -q 'order: 70' src/content/links.yaml && ! grep -qE 'id:\s*#' src/content/links.yaml && for id in about education work skills projects leadership testimonials; do grep -qE "^- id: $id\$" src/content/links.yaml || { echo "MISSING id: $id"; exit 1; }; done</automated>
    </verify>
    <acceptance_criteria>
        - `src/content/links.yaml` exists and its first non-blank line starts with `-` (array form — NOT object-map; the schema in `defineCollection` describes ONE entry per Pitfall 3).
        - File contains EXACTLY 7 lines matching `^- id:` (7 nav entries).
        - File contains `- id: work` and `label: Work` (reconciled from snapshot Experience per D-24).
        - File does NOT contain `- id: experience` or `label: Experience` (D-24 reconciliation enforced).
        - Every `id:` value (about, education, work, skills, projects, leadership, testimonials) is present and each matches the regex `^[a-z][a-z0-9-]*$` (no `#`, no underscore, starts with lowercase letter).
        - The 7 entries are in step-10 sparse order: 10, 20, 30, 40, 50, 60, 70 (D-25 + Recipe R4).
        - `order:` values are integers, NOT strings (`order: 10` not `order: "10"`).
    </acceptance_criteria>
    <done>
        `src/content/links.yaml` is a 7-entry YAML array. The snapshot's parallel `sections[]` + `links[]` arrays are collapsed into a single shape (Pitfall 19 fix). `Experience → Work` reconciliation is applied (D-24 — Phase 1 D-23 inheritance). Phase 3 will consume `getCollection('links')` and use each entry's `id` as both the anchor and the `<section id="...">` attribute.
    </done>
</task>

</tasks>

<verification>
    <automated>
        cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &&
        test -f src/content/about.yaml &&
        test -f src/content/skills.yaml &&
        test -f src/content/links.yaml &&
        test -f src/assets/profile.jpg &&
        test "$(grep -c '^- id:' src/content/links.yaml)" = '7' &&
        ! grep -qE '^- id: experience' src/content/links.yaml &&
        ! grep -qE '(devicon:django|devicon:react-original|devicon:apple-original|devicon:windows8-original|devicon:amazonwebservices-original|devicon:nodejs-plain-wordmark)' src/content/skills.yaml &&
        grep -qE '^\s+first_name:\s*Rashmil\s*$' src/content/about.yaml
    </automated>
</verification>

<success_criteria>
    - 3 singleton YAML files exist at the expected paths.
    - `about.yaml` and `skills.yaml` use the object-map form (top-level key); `links.yaml` uses array form.
    - All `social[].icon` and `categories[].items[].icon` strings match the Iconify regex (`prefix:name` shape, no spaces/dots/slashes).
    - The 6 audited corrections from RESEARCH.md Recipe R7 are applied (none of the 6 incorrect identifiers from D-17's first pass appear).
    - `first_name` is trimmed (no trailing space — D-21).
    - `Experience → Work` reconciliation enforced in `links.yaml` (D-24).
    - `src/assets/profile.jpg` is a verbatim binary copy of the snapshot's `profilepic.jpg`.
</success_criteria>

<output>
    Create `.planning/phases/02-content-layer/02-02-SUMMARY.md` when done. Summary must list: (a) the exact icon mappings applied (full enumeration of the 6 D-17 corrections), (b) all trim normalizations applied (`first_name` only), (c) the `MySQL` vs `My SQL` decision made and why, (d) profile.jpg file size before/after copy (sanity check), (e) confirmation that all 7 `links.yaml` ids match the D-06 regex.
</output>
</content>
</invoke>