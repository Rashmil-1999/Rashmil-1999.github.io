---
phase: 02-content-layer
plan: 07
type: execute
wave: 5
depends_on:
    - 02-02
    - 02-03
    - 02-04
    - 02-05
    - 02-06
files_modified:
    - .planning/phases/02-content-layer/02-PARITY.md
autonomous: false
requirements:
    - CONTENT-06
must_haves:
    truths:
        - 'CONTENT-06: Every resume item from the old `src/resumeData.json` is reachable through the new collections — manual field-by-field diff shows zero data loss.'
        - 'Recipe R6 (snapshot inventory) is the authoritative checklist — every JSON path enumerated there has a documented destination in the new content tree.'
        - 'Phase 2 SC #3: Manual diff documents zero data loss. The PARITY.md artifact IS the evidence.'
        - 'Acceptable losses (documented explicitly): trailing space on `first_name` (D-21 trim), `Numpy`/`Tensorflow` casing fixes in tech_stack arrays (D-21 lossless casing fix), `http://github.com` normalized to `https://github.com` (Plan 02-02 Task 1 — defensive URL normalization).'
        - 'Acceptable additions: synthesized one-sentence bodies for the 3 education entries (Plan 02-05 Task 1 — D-20 enforcement; PATTERNS.md line 390 recommendation). These additions are noted in PARITY.md so they are not mistaken for data loss in reverse.'
    artifacts:
        - path: '.planning/phases/02-content-layer/02-PARITY.md'
          provides: 'Manual diff checklist of every snapshot key (resumeData.json) → new content destination, with status (migrated / transformed / synthesized / orphan) per row.'
          contains: 'CONTENT-06'
    key_links:
        - from: '.planning/phases/02-content-layer/02-PARITY.md'
          to: '.planning/snapshots/m1-source/resumeData.json'
          via: 'one row per snapshot key with the JSON path on the left'
          pattern: 'about\\.first_name|projects\\['
---

<objective>
Produce the CONTENT-06 parity-verification artifact: a manual field-by-field diff between `.planning/snapshots/m1-source/resumeData.json` and the new Content Layer files. The artifact is `.planning/phases/02-content-layer/02-PARITY.md` and uses Recipe R6 (RESEARCH.md lines 893-915) as the authoritative checklist. Every JSON path in the snapshot is enumerated; for each, the new destination is named and a status is assigned: `migrated` (verbatim), `transformed` (documented normalization per D-21/D-22), `synthesized` (added by Phase 2 for schema/test compliance), or `orphan` (snapshot file with no project ref — `_orphans/`).

Purpose: Phase 2 SC #3 ("manual diff of fields ... shows zero data loss") cannot be discharged by `astro check` alone — the schema only validates that data is well-formed, not that it matches the snapshot. PARITY.md is the human-readable evidence that the migration is complete and any divergences are intentional. Phase 5 will reference this artifact when deciding to delete the snapshot.

Output: One markdown file (`02-PARITY.md`, ~150 lines) + one checkpoint (the user reviews the artifact and confirms zero unexpected losses).
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-layer/02-CONTEXT.md
@.planning/phases/02-content-layer/02-RESEARCH.md
@.planning/phases/02-content-layer/02-PATTERNS.md
@.planning/snapshots/m1-source/resumeData.json
@.planning/phases/02-content-layer/02-02-PLAN.md
@.planning/phases/02-content-layer/02-04-PLAN.md
@.planning/phases/02-content-layer/02-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
    <name>Task 1: Author .planning/phases/02-content-layer/02-PARITY.md (snapshot → destination diff)</name>
    <files>.planning/phases/02-content-layer/02-PARITY.md</files>
    <read_first>
        - .planning/snapshots/m1-source/resumeData.json — full file (342 lines); read end-to-end to enumerate every key, including top-level `sections[]` and `links[]` (which collapse into `links.yaml`) and nested arrays
        - .planning/phases/02-content-layer/02-RESEARCH.md lines 893-915 — Recipe R6 (the authoritative snapshot-inventory table); reproduce its rows in PARITY.md with execution-time status added per row
        - .planning/phases/02-content-layer/02-CONTEXT.md — D-21 (trims) + D-22 (preserved spellings) + D-09 (Garduino rename) + D-10 (orphans) + D-13/D-14/D-15 (singleton collapses) — every normalization here gets a row in PARITY.md
        - src/content/about.yaml — confirm migrated keys
        - src/content/skills.yaml — confirm migrated keys
        - src/content/links.yaml — confirm 7 entries (Experience→Work reconciliation per D-24 noted)
        - src/content/projects/*/index.md — 13 project entries (Plan 02-04 output)
        - src/content/work/*/index.md, src/content/education/*/index.md, src/content/leadership/*/index.md, src/content/testimonials/*/index.md — 7 entries (Plan 02-05 output)
        - src/assets/profile.jpg, src/content/_orphans/* — non-text artifacts; note their existence and size in PARITY.md
        - .planning/phases/02-content-layer/02-02-SUMMARY.md, 02-03-SUMMARY.md, 02-04-SUMMARY.md, 02-05-SUMMARY.md — the per-plan summaries already document specific decisions; cite them when explaining rows
    </read_first>
    <action>
        Create `.planning/phases/02-content-layer/02-PARITY.md` with the following sections (use markdown tables; one row per snapshot key/value pair):

        **Header section.** Top of file: title `# Phase 2 Parity Verification (CONTENT-06)`, the date the diff was performed, the explicit claim under verification ("Every resume item in `.planning/snapshots/m1-source/resumeData.json` is reachable through the new Content Layer collections — zero data loss"), a legend for status values (`migrated`, `transformed`, `synthesized`, `orphan`, `dropped`), and a TL;DR row count summary (e.g., "X migrated, Y transformed, Z synthesized, 4 orphans, 0 dropped").

        **Section 1: `about` (snapshot lines 2-22).** One table with 8 rows + 2 sub-rows for social entries.
        - `about.first_name "Rashmil "` → `about.yaml: about.first_name "Rashmil"` — status: transformed (D-21 trim).
        - `about.last_name "Panchani"` → `about.yaml: about.last_name "Panchani"` — migrated.
        - `about.current_status` → migrated verbatim.
        - `about.email` → migrated.
        - `about.contact_message` → migrated.
        - `about.description` → migrated.
        - `about.resume_download "Rashmil_Panchani.pdf"` → migrated (file is in `public/` per Phase 1 retention).
        - `about.social[0]` (LinkedIn): `name`, `url`, `className: "devicon-linkedin-plain colored"` → name + url migrated, `className` transformed to `icon: simple-icons:linkedin` (D-17 mapping — RESEARCH.md Recipe R7).
        - `about.social[1]` (Github): name + url migrated (url normalized `http://` → `https://`, transformed), className transformed to `icon: simple-icons:github`.
        - `(synthesized)` row: `about.profile_image` = `~/assets/profile.jpg` — not present in snapshot JSON (the snapshot's profile pic was an asset file, not a JSON field). Status: synthesized (D-11 — links the relocated binary).

        **Section 2: `sections[]` + `links[]` (snapshot lines 23-40).** Pitfall 19 collapse + D-24 reconciliation.
        - Snapshot `sections[]` (7 strings) + `links[]` (7 anchors) → `links.yaml` (7 entries, single source). Status: transformed (collapsed parallel arrays per D-14; Pitfall 19 fix).
        - Highlight the Experience→Work reconciliation row: `sections[2] = "Experience"` + `links[2] = "#experience"` → `links.yaml [{id: work, label: Work, order: 30}]`. Status: transformed (D-24 — reconciliation to Phase 1 D-23 canonical).

        **Section 3: `resumeData.education[]` (snapshot lines 42-61).** 3 entries.
        - Each entry's `name`, `degree`, `graduated`, `score` → corresponding fields in `src/content/education/<slug>/index.md` frontmatter. All status: migrated (casing preserved per D-22). Note: education `name` strings preserved verbatim including UPPERCASE / `ST. Rocks` / `ST. Francis`.
        - 3 synthesized rows: each education entry has a `(body)` synthesized one-sentence summary per Plan 02-05 Task 1 (D-20 enforcement; the snapshot has no body text for education). Status: synthesized — flagged so the parity diff is honest about additions.

        **Section 4: `resumeData.work[]` (snapshot lines 62-74).** 2 entries.
        - `company`, `title`, `duration` → frontmatter; `description` → body. All status: migrated (`peAR Technologies.` casing/period preserved per D-22).

        **Section 5: `resumeData.skill_array[]` + `resumeData.skills{}` (snapshot lines 76-225).** Collapsed per D-15.
        - One row per category × items. 6 categories × variable items = ~30 rows.
        - For each item: status migrated for `name`, status transformed for `class:` (CDN form) → `icon:` (Iconify identifier per D-17 + Recipe R7). Explicitly enumerate the 6 D-17 corrections in their own sub-section ("Iconify mapping audit — corrections applied"):
            - `devicon-django-plain colored` → `simple-icons:django` (devicon:django does not exist)
            - `devicon-react-original colored` → `devicon:react` (drop -original)
            - `devicon-apple-original colored` → `devicon:apple` (drop -original)
            - `devicon-windows8-original colored` → `devicon:windows8` (drop -original)
            - `devicon-amazonwebservices-original colored` → `devicon:amazonwebservices` (drop -original)
            - `devicon-nodejs-plain-wordmark colored` → `devicon:nodejs` (drop -plain-wordmark)
            - `logos-google-cloud-platform` → `logos:google-cloud` (normalized canonical id)
            - 5 `iconify` + `bx:bx-code-alt` data-icon entries (NumPy/Pandas/Tensorflow/Keras/OpenCV) → 5 `simple-icons:<name>` ids (D-17 — brand glyphs).
            - 1 `iconify` + `bx:bx-code-alt` (NLTK) → `lucide:code` (D-17 placeholder; no brand glyph exists in any set — documented in PARITY.md as accepted shortcoming).

        **Section 6: `projects[]` (snapshot lines 227-326).** 13 entries.
        - One sub-section per project. For each: title (migrated), tech_stack (migrated, with the casing fixes for `Numpy`→`NumPy` and `Tensorflow`→`TensorFlow` flagged as transformed per Plan 02-04 documentation), description (migrated, to body), url (migrated), image (transformed: snapshot `image: "garduino"` (no extension, references `graduino.png` in assets) → frontmatter `cover: ./garduino.png` per D-09 rename). Highlight the Garduino row explicitly.
        - For multi-variant projects (e-yantra-competition, dj-archive), document the alternates rows: snapshot has multiple files in `assets/` with the same stem; new entries declare them via `alternates: [...]` per D-07.
        - `(asset side)` rows: project images colocated. Reference the 16 image files copied + the Garduino rename + the 4.8 MB `emotion.png` rejection (CONCERNS carry-forward — accepted dropped: status `dropped` with rationale).

        **Section 7: `testimonials[]` (snapshot lines 328-333).** 1 entry.
        - Snapshot `user: "Roopam Mishra, Founder of Phionike Solutions: Design-Tech Studio."` → 3 fields in the new entry: `user: Roopam Mishra`, `role: Founder`, `org: "Phionike Solutions: Design-Tech Studio"`. Status: transformed (user-role-org split per RESEARCH.md schema lines 673-679).
        - Snapshot `text` → body. Status: migrated.

        **Section 8: `leaderships[]` (snapshot lines 334-341).** 1 entry.
        - `org`, `title`, `duration` → frontmatter (migrated). `description` → body (migrated).

        **Section 9: Snapshot `assets/` directory (22 files).** Each file accounted for:
        - 16 → `src/content/projects/<slug>/<filename>` (migrated; Garduino renamed)
        - 4 → `src/content/_orphans/` (orphan — D-10)
        - 1 (`profilepic.jpg`) → `src/assets/profile.jpg` (migrated, renamed — D-11)
        - 1 (`emotion.png`, 4.8 MB) → dropped intentionally (CONCERNS.md carry-forward; the 23 KB `emotion_recognition.png` is the cover). Status: dropped with rationale.

        **Footer.** Total counts: rows by status (migrated / transformed / synthesized / orphan / dropped). Explicit verdict: "CONTENT-06 satisfied: zero unexpected data loss; all transformations and additions are documented and traceable to a CONTEXT.md decision (D-NN) or a RESEARCH.md Recipe (R-NN)."

        Anti-patterns to avoid:
        - Do NOT skip any snapshot row to keep PARITY.md short — exhaustiveness is the whole point.
        - Do NOT label any row "migrated" if a transformation was applied (use `transformed` with a citation to the deciding D-NN).
        - Do NOT introduce new transformations during the parity check — if a discrepancy is found that has no D-NN justification, STOP and surface it as a gap (the executor should not silently fix data drift during the parity pass; this is the gate before Phase 3 begins).
    </action>
    <verify>
        <automated>cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io && test -f .planning/phases/02-content-layer/02-PARITY.md && grep -q '# Phase 2 Parity' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'CONTENT-06' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'about\.first_name' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'D-21' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'D-09|garduino' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'D-17' .planning/phases/02-content-layer/02-PARITY.md && grep -qE '(simple-icons:django|devicon:nodejs)' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'D-24|Experience' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'Roopam' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'role: Founder|user-role-org' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'emotion\.png' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'orphan' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'synthesized' .planning/phases/02-content-layer/02-PARITY.md && grep -qE 'profilepic\.jpg|profile\.jpg' .planning/phases/02-content-layer/02-PARITY.md && WC="$(wc -l < .planning/phases/02-content-layer/02-PARITY.md)" && test "$WC" -gt 100</automated>
    </verify>
    <acceptance_criteria>
        - `.planning/phases/02-content-layer/02-PARITY.md` exists; file is > 100 lines.
        - Header references `CONTENT-06`.
        - Body cites at least 5 distinct decision IDs (`D-09`, `D-11`, `D-17`, `D-21`, `D-22`, `D-24`) — every transformation is traced to a CONTEXT.md decision.
        - Body enumerates the Garduino rename (`graduino.png` → `garduino.png`, D-09).
        - Body enumerates all 6 Iconify-mapping corrections from Recipe R7 (`simple-icons:django`, `devicon:react`, `devicon:apple`, `devicon:windows8`, `devicon:amazonwebservices`, `devicon:nodejs`).
        - Body enumerates the testimonial user/role/org split.
        - Body enumerates the Experience→Work reconciliation (D-24).
        - Body enumerates the 4 orphan images and the deliberately-dropped `emotion.png` (4.8 MB).
        - Body enumerates the 3 synthesized education bodies (D-20 + Plan 02-05 Task 1).
        - Body uses the 5-status legend (`migrated`, `transformed`, `synthesized`, `orphan`, `dropped`).
        - Footer contains a concrete row-count summary and an explicit CONTENT-06 verdict.
    </acceptance_criteria>
    <done>
        `02-PARITY.md` is the artifact of record for CONTENT-06; every snapshot row is accounted for; the 5 status categories are populated; every transformation cites the D-NN that authorized it. Task 2 (the human checkpoint) reads this artifact and decides whether Phase 2 is closed or whether a regression has been surfaced.
    </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
    <name>Task 2: User confirms PARITY.md shows zero unexpected data loss</name>
    <files>(none — human review only; consults 02-PARITY.md and runs build gate)</files>
    <action>Pause execution and prompt the user to manually review `.planning/phases/02-content-layer/02-PARITY.md` per the steps in `<how-to-verify>`. Do not advance the phase until the user responds. This is a `checkpoint:human-verify` task — the work was automated by Plans 02-01..02-06 + this plan's Task 1; the human only verifies the artifact and signs off (CONTENT-06 acceptance per Phase 2 SC #3).</action>
    <verify><human-check>User types `approved` after reviewing 02-PARITY.md and running the build gate (`npx astro check && npx vitest run` both exit 0).</human-check></verify>
    <done>User confirms zero unexpected data loss by typing `approved`. CONTENT-06 marked satisfied; Phase 2 closed.</done>
    <what-built>
        Plans 02-01 through 02-06 collectively produced: `src/content.config.ts` with 8 collections, 3 singleton YAML files (`about.yaml`, `skills.yaml`, `links.yaml`), 20 list-collection markdown entries (13 projects + 2 work + 3 education + 1 leadership + 1 testimonial), 16 colocated project images + 4 orphans + 1 relocated profile image, and `tests/content-validation.test.ts` with passing assertions for CONTENT-08 and Phase 2 SC #1. Task 1 of this plan produced `02-PARITY.md` — the human-readable diff that documents every snapshot key's destination and status.
    </what-built>
    <how-to-verify>
        1. Open `.planning/phases/02-content-layer/02-PARITY.md` in an editor.
        2. Read every row of every section. For each `migrated` row, spot-check 2-3 against the snapshot at `.planning/snapshots/m1-source/resumeData.json` and the new file (e.g., open `src/content/about.yaml` and confirm `email: rashmilp833@gmail.com` matches the snapshot at line 6).
        3. For every `transformed` row, confirm the cited D-NN actually authorizes that transformation. Open `.planning/phases/02-content-layer/02-CONTEXT.md` and verify the D-NN says what PARITY.md claims.
        4. For every `synthesized` row, confirm the addition was necessary (e.g., the 3 education one-sentence bodies — open one of the education `index.md` files, read the body, confirm it is a non-fabricating summary of the frontmatter fields, not invented content).
        5. For every `orphan` row, confirm the 4 files are in `src/content/_orphans/` (`ls src/content/_orphans/` should show exactly attendance.png, attendance.webp, attendance1.png, smgarden.png).
        6. For the `dropped` row (`emotion.png` 4.8 MB), confirm the file is NOT under `src/content/projects/` (`find src/content/projects -name 'emotion.png'` returns nothing).
        7. Run the full build gate one final time: `npx astro check && npx vitest run`. Both must exit 0.
        8. Verify Garduino: `ls src/content/projects/garduino-smart-garden/` should list `garduino.png` (NOT `graduino.png`) and `index.md`. Open `index.md` and confirm `cover: ./garduino.png`.
        9. Verify Experience→Work: `grep '^- id: work' src/content/links.yaml` returns a match; `grep '^- id: experience' src/content/links.yaml` returns nothing.

        If everything above checks out, type `approved` to mark Phase 2 complete. If anything is missing or incorrect, describe what is missing or wrong — the executor will open a gap-closure plan to address it.
    </how-to-verify>
    <resume-signal>
        Type "approved" to mark Phase 2 CONTENT-06 satisfied and close the phase. Otherwise, describe gaps; the executor will surface them for `/gsd-plan-phase --gaps`.
    </resume-signal>
</task>

</tasks>

<verification>
    <automated>
        cd /Users/rashmilpanchani/Documents/Rashmil-1999.github.io &&
        test -f .planning/phases/02-content-layer/02-PARITY.md &&
        npx astro check &&
        npx vitest run
    </automated>
</verification>

<success_criteria>
    - `02-PARITY.md` exists, exceeds 100 lines, and enumerates every snapshot key.
    - Every transformation/synthesized/dropped row cites a D-NN.
    - User confirms (or surfaces gaps for) `approved` on the checkpoint.
    - Full build gate (`npx astro check && npx vitest run`) exits 0 at phase close.
</success_criteria>

<output>
    Create `.planning/phases/02-content-layer/02-07-SUMMARY.md` when done. Summary must list: (a) the final row-count summary from PARITY.md (migrated / transformed / synthesized / orphan / dropped totals), (b) the user's checkpoint response verbatim, (c) the final `astro check` and `vitest run` exit codes at phase close, (d) any open issues surfaced during the human review (none expected; record explicitly if so).
</output>
</content>
</invoke>