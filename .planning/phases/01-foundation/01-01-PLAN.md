---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - .claude/
    - .planning/snapshots/m1-source/resumeData.json
    - .planning/snapshots/m1-source/assets/
    - .planning/snapshots/m1-source/favicon.ico
    - .planning/snapshots/m1-source/logo192.png
    - .planning/snapshots/m1-source/logo512.png
    - src/
    - public/
    - yarn.lock
    - package.json
    - package-lock.json
    - README.md
    - .gitignore
autonomous: false
requirements:
    - FOUND-01
    - FOUND-07
must_haves:
    truths:
        - "`.claude/` is committed to git and survives the wipe"
        - "Branch is renamed from `master` to `main` locally and pushed to origin"
        - "`.planning/snapshots/m1-source/resumeData.json` exists and matches the pre-wipe `src/resumeData.json` byte-for-byte"
        - "`.planning/snapshots/m1-source/assets/` contains every file that was in `src/assets/` before wipe"
        - "After wipe, `src/` and `public/` contain only the new Astro scaffold; `public/Rashmil_Panchani.pdf` is the single survivor in `public/`"
        - "`yarn.lock` is deleted before any `npm create`/`npx astro add` runs"
        - "Astro 6 scaffold is in place at the repo root (`astro.config.mjs`, `src/pages/index.astro`, `package.json` with `astro` dep present)"
    artifacts:
        - path: ".planning/snapshots/m1-source/resumeData.json"
          provides: "Pre-wipe resume data for Phase 2 to consume"
          contains: "first_name"
        - path: ".planning/snapshots/m1-source/assets/"
          provides: "Pre-wipe project images for Phase 3 to consume"
        - path: "public/Rashmil_Panchani.pdf"
          provides: "Résumé PDF (only public/ survivor per D-02)"
        - path: "astro.config.mjs"
          provides: "Astro config (skeleton; integrations wired in plan 02)"
        - path: "package.json"
          provides: "npm scripts and Astro dep tree"
          contains: "astro"
    key_links:
        - from: "Phase 2 content migration"
          to: ".planning/snapshots/m1-source/resumeData.json"
          via: "filesystem read"
          pattern: "snapshots/m1-source/resumeData.json"
        - from: "About.astro (Phase 3)"
          to: "public/Rashmil_Panchani.pdf"
          via: "href in download link"
          pattern: "Rashmil_Panchani.pdf"
---

<objective>
Snapshot pre-wipe source data, commit `.claude/`, rename `master`→`main`, delete `yarn.lock`, then wipe the CRA tree and run `npm create astro@latest` to land a baseline Astro 6 scaffold at the repo root.

Purpose: Establish a clean greenfield Astro foundation while preserving the resume content + assets for downstream phases (D-01..D-06). Sequencing here is irreversible-on-disk, so the snapshot and branch rename MUST happen before any deletion.

Output: Pre-wipe snapshot at `.planning/snapshots/m1-source/`, `main` branch on local + origin, fresh Astro scaffold (no integrations yet — Plan 02 adds React/Tailwind), `yarn.lock` deleted, one commit covering "wipe + scaffold" rollup per D-04.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit .claude/ and snapshot pre-wipe source data</name>
  <files>.claude/, .planning/snapshots/m1-source/resumeData.json, .planning/snapshots/m1-source/assets/, .planning/snapshots/m1-source/favicon.ico, .planning/snapshots/m1-source/logo192.png, .planning/snapshots/m1-source/logo512.png</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-03 snapshot scope; D-04 sequencing; D-05 `.claude/` preservation)
    - .planning/phases/01-foundation/01-RESEARCH.md (Runtime State Inventory — snapshot list; Pitfall D — `yarn.lock` must be deleted BEFORE scaffold not in this task)
    - src/resumeData.json (verify it exists and is the file being snapshotted)
    - .gitignore (confirm `.planning/snapshots/` is NOT ignored — snapshot must be committable)
  </read_first>
  <action>
    Step A — Ensure `.claude/` is in working tree and staged for commit. Currently `.claude/` is untracked (per gitStatus). Run `git status .claude/` to confirm; then `git add .claude/`.

    Step B — Create the snapshot directory and copy these specific items VERBATIM (no transformation, no rename):
    - `src/resumeData.json` → `.planning/snapshots/m1-source/resumeData.json`
    - `src/assets/` (recursive, preserving filenames and casing) → `.planning/snapshots/m1-source/assets/`
    - `public/favicon.ico` → `.planning/snapshots/m1-source/favicon.ico`
    - `public/logo192.png` → `.planning/snapshots/m1-source/logo192.png`
    - `public/logo512.png` → `.planning/snapshots/m1-source/logo512.png`

    Use `cp -R` (or equivalent) — do NOT use `git mv` (these files are being copied, not moved; the originals are deleted in Task 3).

    DO NOT alter snapshot contents (per D-03: "must be preserved verbatim"). The `id="experience"` divergence in `resumeData.json links[]` is a Phase 2 concern (PATTERNS.md notes it explicitly) — Phase 1 must NOT silently rename `experience`→`work` in the snapshot.

    Step C — Stage the snapshot files: `git add .planning/snapshots/m1-source/`.

    Step D — Do NOT commit yet. Tasks 1+2+3+4 commit together at the end of Task 4 per D-04 (single rollup commit).
  </action>
  <verify>
    <automated>test -f .planning/snapshots/m1-source/resumeData.json && test -d .planning/snapshots/m1-source/assets && test -f .planning/snapshots/m1-source/favicon.ico && test -f .planning/snapshots/m1-source/logo192.png && test -f .planning/snapshots/m1-source/logo512.png && diff -q src/resumeData.json .planning/snapshots/m1-source/resumeData.json && diff -rq src/assets .planning/snapshots/m1-source/assets && git ls-files --error-unmatch .claude/CLAUDE.md > /dev/null 2>&1 || git diff --cached --name-only | grep -q '^\.claude/' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/snapshots/m1-source/resumeData.json` exists and `diff -q` against `src/resumeData.json` reports identical
    - `.planning/snapshots/m1-source/assets/` exists and `diff -rq` against `src/assets/` reports no differences
    - `.planning/snapshots/m1-source/favicon.ico`, `logo192.png`, `logo512.png` all exist
    - `.claude/` content is either tracked already OR staged via `git add` (verify with `git diff --cached --name-only | grep '^\.claude/'` returning at least one path)
    - No commit has been created yet — verify `git log -1 --pretty=%s` does NOT mention "snapshot" or "wipe"
  </acceptance_criteria>
  <done>Snapshot directory populated with byte-identical copies of pre-wipe source; `.claude/` staged for inclusion in the upcoming rollup commit; no commit created yet (per D-04 rollup ordering).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify snapshot integrity and approve branch rename master→main</name>
  <files>.planning/snapshots/m1-source/</files>
  <what-built>Pre-wipe snapshot is staged at `.planning/snapshots/m1-source/`. `.claude/` is staged for commit. Nothing committed yet; nothing deleted yet. This checkpoint exists because Task 3 is destructive and Task 4 renames the branch — both are easier to reverse before the commit than after.</what-built>
  <how-to-verify>
    1. Run `ls -la .planning/snapshots/m1-source/` and confirm `resumeData.json` plus `assets/` directory plus three favicon/logo files are present.
    2. Run `diff -q src/resumeData.json .planning/snapshots/m1-source/resumeData.json` — must print nothing (files identical).
    3. Run `du -sh .planning/snapshots/m1-source/` — expect roughly 6–8 MB (the assets are the bulk — `emotion.png` is 4.8 MB, `profilepic.jpg` is 1 MB per RESEARCH.md Runtime State Inventory).
    4. Run `git diff --cached --stat | head -50` — verify `.claude/` files and `.planning/snapshots/m1-source/` files are both in the staged set.
    5. Run `git branch --show-current` — confirm it is currently `master`. (The next task renames it to `main`.)
    6. Confirm you want to proceed with: (a) rename `master`→`main` locally, (b) push `main` to origin and set upstream, (c) delete `src/` (except scaffold-to-come) and `public/` (except `Rashmil_Panchani.pdf`) on the next task.

    NOTE: GitHub's default branch stays as `gh-pages` (the live-site branch) per CONTEXT.md specifics block — Phase 1 does NOT change the GitHub default branch. That cutover is Phase 5's responsibility. The push of `main` here just creates the branch on origin alongside the existing `master` and `gh-pages`.
  </how-to-verify>
  <action>Human-only verification gate. Operator inspects the snapshot integrity via the six steps in `<how-to-verify>` and answers the resume-signal prompt. No file modifications. No commands run by the executor — this task pauses execution.</action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Operator typed "approved" (or the resume-signal handshake). Execution resumes at Task 3 (destructive wipe + branch rename).</done>
  <resume-signal>Type "approved" to proceed with branch rename + destructive wipe + Astro scaffold (Tasks 3–4). Type "stop" to abort — `git reset` will unstage everything and the snapshot directory can be removed with `rm -rf .planning/snapshots/m1-source/`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Rename master→main, delete yarn.lock, then wipe CRA tree</name>
  <files>yarn.lock, src/components/, src/App.css, src/index.js, src/setupTests.js, src/serviceWorker.js, src/resumeData.json, src/assets/, public/CNAME, public/favicon.ico, public/index.html, public/logo192.png, public/logo512.png, public/manifest.json, public/robots.txt, node_modules/, build/</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-02 wipe scope; D-04 sequencing; D-06 `public/CNAME` permanently dropped)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall D — `yarn.lock` MUST be deleted BEFORE `npm create astro` and `npx astro add`; Runtime State Inventory — `rm -rf node_modules build/` between wipe and reinstall)
    - .planning/phases/01-foundation/01-PATTERNS.md (Greenfield Note — list of wipe-survivors)
    - public/ (visually inspect what is currently there to confirm only `Rashmil_Panchani.pdf` should remain after this task)
    - src/ (visually inspect current contents — everything except a moments-later-created Astro scaffold goes)
  </read_first>
  <action>
    Step A — Rename branch locally: `git branch -m master main`. Then push `main` to origin and set upstream: `git push -u origin main`. Then delete the remote `master` branch: `git push origin --delete master`. (Local `master` is already gone after the rename.) Per RESEARCH.md Runtime State Inventory + Pitfall H. Do NOT touch GitHub's default branch setting — `gh-pages` stays as the default branch through Phase 5.

    Step B — Delete `yarn.lock` BEFORE any scaffold step. `git rm yarn.lock`. This is critical per RESEARCH.md Pitfall D — if `yarn.lock` is still present when `npm create astro@latest` or `npx astro add` runs, Astro's auto-detection installs against Yarn and regenerates `yarn.lock` alongside `package-lock.json`, violating FOUND-07.

    Step C — Delete the CRA file/dir set per D-02. Use `git rm -r` for tracked files and `rm -rf` for untracked. Concrete targets:
    - `src/components/` (whole directory — all 8 `.jsx` files)
    - `src/App.css`, `src/index.js`, `src/setupTests.js`, `src/serviceWorker.js`, `src/resumeData.json`
    - `src/assets/` (whole directory — already snapshotted in Task 1)
    - `public/CNAME` (D-06: permanently dropped, NOT re-authored)
    - `public/favicon.ico`, `public/index.html`, `public/logo192.png`, `public/logo512.png`, `public/manifest.json`, `public/robots.txt`
    - DO NOT delete `public/Rashmil_Panchani.pdf` — this is the single `public/` survivor per D-02.

    Step D — Also remove leftover build outputs and old install (untracked, no git rm needed): `rm -rf node_modules/ build/`. RESEARCH.md flags `node_modules/` as a CRA + React 18 + react-scripts tree that will conflict with the fresh Astro install.

    Step E — At this point `src/` should be empty (or non-existent) and `public/` should contain only `Rashmil_Panchani.pdf`. Verify: `find src public -type f | sort` should print only `public/Rashmil_Panchani.pdf`.

    DO NOT commit yet — Task 4 finishes the scaffold and Tasks 1+3+4 commit together per D-04.
  </action>
  <verify>
    <automated>test ! -f yarn.lock && test ! -d src/components && test ! -f src/App.css && test ! -f src/index.js && test ! -f src/serviceWorker.js && test ! -f src/setupTests.js && test ! -f src/resumeData.json && test ! -d src/assets && test ! -f public/CNAME && test ! -f public/favicon.ico && test ! -f public/index.html && test ! -f public/logo192.png && test ! -f public/logo512.png && test ! -f public/manifest.json && test ! -f public/robots.txt && test -f public/Rashmil_Panchani.pdf && test ! -d node_modules && test ! -d build && [ "$(git branch --show-current)" = "main" ] && git ls-remote --heads origin main | grep -q main && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` returns `main`
    - `git ls-remote --heads origin main` shows `main` on origin (i.e., push succeeded)
    - `git ls-remote --heads origin master` returns no `master` ref (remote master deleted)
    - `yarn.lock` does NOT exist on disk
    - None of the wipe-targeted files/dirs exist: `src/components/`, `src/App.css`, `src/index.js`, `src/setupTests.js`, `src/serviceWorker.js`, `src/resumeData.json`, `src/assets/`, `public/CNAME`, `public/favicon.ico`, `public/index.html`, `public/logo192.png`, `public/logo512.png`, `public/manifest.json`, `public/robots.txt`
    - `public/Rashmil_Panchani.pdf` DOES exist
    - `node_modules/` and `build/` do not exist
    - The GitHub default branch is still `gh-pages` (Phase 5 changes this; verify via `gh api repos/Rashmil-1999/Rashmil-1999.github.io --jq '.default_branch'` if `gh` is configured, or inspect the GitHub Settings UI)
  </acceptance_criteria>
  <done>Branch is `main` locally + on origin (with origin/master deleted); `yarn.lock` is gone; the CRA tree is wiped; `public/Rashmil_Panchani.pdf` is the only survivor in `public/`; `node_modules/`/`build/` cleared; nothing committed yet.</done>
</task>

<task type="auto">
  <name>Task 4: Scaffold Astro 6 at repo root and commit the wipe+scaffold rollup</name>
  <files>package.json, package-lock.json, astro.config.mjs, src/pages/index.astro, src/, public/, tsconfig.json, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 single rollup commit for deletions + scaffold; D-11 src/ layout — Plan 03 actually authors stub files, Task 4 here just lays the baseline scaffold)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 — `astro.config.mjs` shape; Standard Stack — Astro 6.3.8 is the target; Pitfall D — already mitigated by Task 3 deleting `yarn.lock`)
    - .planning/phases/01-foundation/01-PATTERNS.md (`package.json` analog block — what must be deleted from the current CRA `package.json` once Astro takes over)
    - package.json (current — most CRA fields will be replaced by the Astro scaffold; some need manual cleanup after `npm create astro` runs)
  </read_first>
  <action>
    Step A — Run `npm create astro@latest -- --template minimal --typescript strict --install --no-git --skip-houston .` from the repo root. Flags rationale:
    - `--template minimal`: smallest scaffold (we author our own stubs in Plan 03)
    - `--typescript strict`: scaffolds with TypeScript and writes a minimal `tsconfig.json` (we tighten it to `astro/tsconfigs/strictest` in Plan 02)
    - `--install`: runs `npm install` so the lockfile is generated
    - `--no-git`: don't initialize a fresh repo over the existing one
    - `--skip-houston`: skip the welcome animation
    - `.`: scaffold into the current directory (the existing `package.json` is replaced; remaining fields are scrubbed below)

    If the prompt still appears, force-accept overwriting `package.json` (the wipe in Task 3 already cleared the conflicting source dirs). The scaffold creates:
    - `package.json` (Astro-shaped)
    - `package-lock.json`
    - `astro.config.mjs` (basic — Plan 02 wires React + Tailwind)
    - `src/pages/index.astro` (default "Welcome to Astro" page — Plan 03 replaces it)
    - `src/env.d.ts` (Astro types reference)
    - `public/favicon.svg` (Astro's default — keep; matches BaseHead's `<link rel="icon" href="/favicon.svg">` per D-13)
    - `tsconfig.json` (Plan 02 tightens this)
    - `.gitignore` (already exists with sensible entries; the scaffold may add `dist/` and `.astro/` — verify and merge if needed)

    Step B — Manually scrub any residual CRA artifacts in `package.json` that the scaffold did NOT overwrite (per PATTERNS.md `package.json` analog):
    - If `eslintConfig: { extends: "react-app" }` remains, delete it.
    - If `browserslist` block remains, delete it.
    - If CRA-era `scripts` block (start/build/test/eject/predeploy/deploy) remains, delete those entries — keep only the Astro-provided ones (`dev`, `build`, `preview`, `astro`). Plan 02 adds `lint`/`format`/`format:check`/`test`/`prepare`.
    - If `gh-pages`, `react-script-tag`, `react-scripts`, `react-scroll` are still in `dependencies` (because the scaffold didn't touch them), remove them.
    - Keep: `homepage: "https://Rashmil-1999.github.io"` (will also appear as `site` in `astro.config.mjs` in Plan 02).
    - Keep `"private": true`.

    Do NOT add `engines.node` or `lint-staged` or `prepare` script in this task — Plan 02 (config) and Plan 04 (lint/format/husky) own those.

    Step C — Verify the scaffold by running `npm run build` from the repo root. This produces `dist/index.html` containing the Astro "Welcome" placeholder. Expected exit code 0. (This is the "Hello-world placeholder is acceptable at this phase" clause of ROADMAP Phase 1 SC #1.)

    Step D — Single rollup commit per D-04. Stage everything: `git add -A` (this picks up the staged `.claude/`, the staged snapshot, the deletions from Task 3, and the new Astro scaffold). Then commit with a Conventional Commits message:
    `chore(01): greenfield wipe to Astro 6 scaffold; rename master→main; snapshot pre-wipe source`

    Body of commit (use the heredoc pattern from the parent agent's git commit protocol):
    - Mention D-01..D-06 in the commit body
    - List the moved-to-snapshot items
    - Note `.claude/` preservation
    - Note `yarn.lock` deletion
    - Conventional Commits scope is `01` (the phase number) per existing repo history (`docs(01): ...`).

    Step E — Push `main` if not already pushed (Task 3 pushed it; verify with `git push origin main` — fast-forward, no force).
  </action>
  <verify>
    <automated>test -f astro.config.mjs && test -f package.json && test -f package-lock.json && test -f tsconfig.json && test -f src/pages/index.astro && test -f public/Rashmil_Panchani.pdf && grep -q '"astro"' package.json && ! grep -q '"react-scripts"' package.json && ! grep -q '"react-scroll"' package.json && ! grep -q '"gh-pages"' package.json && ! grep -q '"react-script-tag"' package.json && ! grep -q '"eslintConfig"' package.json && ! grep -q '"browserslist"' package.json && test ! -f yarn.lock && npm run build > /dev/null 2>&1 && test -f dist/index.html && git log -1 --pretty=%s | grep -qi 'greenfield\|wipe\|scaffold'</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` exists at repo root
    - `package.json` contains `"astro"` in dependencies and does NOT contain `"react-scripts"`, `"react-scroll"`, `"react-script-tag"`, `"gh-pages"`, `"eslintConfig"`, or `"browserslist"`
    - `package-lock.json` exists and `yarn.lock` does NOT
    - `tsconfig.json` exists (Plan 02 tightens it)
    - `npm run build` exits 0 and produces `dist/index.html` (Astro "Welcome" placeholder is acceptable here per ROADMAP SC #1)
    - `git log -1 --pretty=%B` shows a Conventional Commits message with the scope `(01)` and references to wipe/scaffold/snapshot
    - `git diff HEAD~1 HEAD --stat` shows: deleted CRA files (Task 3), added Astro scaffold files (Task 4), added snapshot files (Task 1), added `.claude/` (Task 1) — all in ONE commit per D-04
    - `git rev-parse --abbrev-ref HEAD` returns `main` and `git rev-parse --abbrev-ref @{u}` returns `origin/main`
  </acceptance_criteria>
  <done>One rollup commit on `main` contains the wipe, the snapshot, the `.claude/` preservation, and the baseline Astro scaffold. `npm run build` produces `dist/index.html`. Plan 02 wires React + Tailwind + tightens tsconfig next.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local disk → git remote | Wipe + branch rename + commit push must not destroy live-site source-of-truth (`gh-pages` branch on GitHub) |
| Pre-wipe state → post-wipe state | Once `src/resumeData.json` is deleted on disk, the only copy is the snapshot — must verify before deletion |
| Existing CRA `node_modules/` → fresh Astro install | Stale React-18 + react-scripts deps must not influence the new install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | snapshot copy | mitigate | Task 1 uses `cp -R` (no transformation); checkpoint at Task 2 runs `diff -q` against original before wipe in Task 3 |
| T-01-02 | Denial of Service | live site (gh-pages branch) | mitigate | Phase 1 does NOT touch the `gh-pages` branch or GitHub default branch setting — both remain in Phase 5 cutover (RESEARCH.md Pitfall H + Runtime State Inventory) |
| T-01-03 | Tampering | dependency tree | mitigate | yarn.lock deleted BEFORE `npm create astro` (Pitfall D); `rm -rf node_modules/` before scaffold so React-18 + react-scripts don't pollute fresh install |
| T-01-04 | Repudiation | branch identity | mitigate | Single rollup commit per D-04 has full provenance — wipe + scaffold + snapshot + `.claude/` all attributable to one SHA |
| T-01-05 | Information Disclosure | resume PDF | accept | `public/Rashmil_Panchani.pdf` is intentionally public — it's the entire site's purpose |
| T-01-SC | Tampering | npm install | mitigate | Package legitimacy audit in RESEARCH.md ran slopcheck against all 24 Phase 1 packages — zero `[SLOP]`/`[SUS]` flags. No human checkpoint needed for the Astro scaffold install since this Plan only installs the minimal template; Plan 02 + Plan 04 + Plan 05 install additional packages but all are in the approved audit. |

| Category | Applies | Rationale |
|----------|---------|-----------|
| Spoofing | no | No authentication in Phase 1 (static site, no logins) |
| Tampering | yes | Supply-chain (slopcheck), lockfile integrity (FOUND-07), commit history (single rollup) |
| Repudiation | no (low) | Single-developer repo; git history is sufficient |
| Information Disclosure | no | Resume PDF is public by design; no secrets in repo |
| Denial of Service | yes (DEFERRED to Phase 5) | Live site cutover sequencing — Phase 1 keeps gh-pages branch + default-branch untouched |
| Elevation of Privilege | no | CI added in Plan 05 uses default read-only `GITHUB_TOKEN`; deploy.yml (Phase 5) handles `pages: write` |
</threat_model>

<verification>
- `npm run build` exits 0 and produces `dist/index.html`
- `git branch --show-current` → `main`; `git ls-remote --heads origin main` shows `main` exists on origin; `git ls-remote --heads origin master` is empty
- `.planning/snapshots/m1-source/resumeData.json` byte-identical to pre-wipe `src/resumeData.json` (verified via the diff in Task 1)
- One commit on `main` since Phase 1 start contains the wipe + scaffold + snapshot + `.claude/` per D-04
- `yarn.lock` does not exist; `package-lock.json` does
- `public/Rashmil_Panchani.pdf` survives the wipe (per D-02)
</verification>

<success_criteria>
- All four artifacts in `must_haves.artifacts` exist on disk
- All seven truths in `must_haves.truths` are demonstrably true (commands in `<verify>` blocks pass)
- The single rollup commit per D-04 exists on `main` and on `origin/main`
- No subsequent plan can proceed until this plan's commit lands (Plan 02 depends_on `01-01`)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done. Summary must record:
- Snapshot location and total size
- The commit SHA of the rollup commit
- Confirmation that branch is `main` on local + origin
- Confirmation that `yarn.lock` is gone and `package-lock.json` is the sole lockfile
- Note that no requirements are satisfied yet (this plan is preparatory infrastructure for FOUND-01..08 which Plans 02–05 close out)
</output>
