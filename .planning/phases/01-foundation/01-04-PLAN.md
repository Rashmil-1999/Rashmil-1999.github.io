---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
    - 01-03-PLAN.md
files_modified:
    - eslint.config.js
    - .prettierrc.json
    - .prettierignore
    - .husky/pre-commit
    - package.json
    - package-lock.json
autonomous: true
requirements:
    - FOUND-05
must_haves:
    truths:
        - "ESLint 9 (NOT 10) is installed (`npm ls eslint` shows `eslint@9.x.x`)"
        - "`eslint.config.js` composes (in order): @eslint/js recommended → typescript-eslint recommended-type-checked → eslint-plugin-astro recommended → jsx-a11y scoped to **/*.tsx → eslint-config-prettier last"
        - "`eslint.config.js` ignores `dist/`, `node_modules/`, `.astro/`, `coverage/`, `.planning/`, `.claude/`"
        - "`.prettierrc.json` has `printWidth: 100`, `tabWidth: 4`, `singleQuote: true`, `trailingComma: \"all\"`, plugins `[\"prettier-plugin-astro\", \"prettier-plugin-tailwindcss\"]` in that order (tailwindcss last)"
        - "`.prettierignore` ignores the same 6 paths as ESLint plus `package-lock.json`"
        - "`package.json` scripts include `lint`, `format`, `format:check`, `prepare`, plus a `lint-staged` config block"
        - "`.husky/pre-commit` runs `npx lint-staged`"
        - "`npm run lint` exits 0 against the current file set (no ESLint errors)"
        - "`npm run format:check` exits 0 against the current file set (no Prettier diffs)"
        - "`npx astro check` still exits 0"
    artifacts:
        - path: "eslint.config.js"
          provides: "ESLint 9 flat config per RESEARCH.md Pattern 5"
          contains: "tseslint.config"
        - path: ".prettierrc.json"
          provides: "Prettier config per D-17"
          contains: "prettier-plugin-tailwindcss"
        - path: ".prettierignore"
          provides: "Prettier ignore list per D-18"
          contains: ".planning/"
        - path: ".husky/pre-commit"
          provides: "Pre-commit hook running lint-staged per D-19"
          contains: "lint-staged"
        - path: "package.json"
          provides: "Lint/format/prepare scripts + lint-staged config"
          contains: "lint-staged"
    key_links:
        - from: "eslint.config.js"
          to: "eslint-config-prettier"
          via: "last entry in tseslint.config(...) array"
          pattern: "prettier"
        - from: ".husky/pre-commit"
          to: "lint-staged"
          via: "shell invocation"
          pattern: "npx lint-staged"
        - from: "package.json scripts"
          to: "eslint . && prettier --check ."
          via: "npm run lint && npm run format:check"
          pattern: "\"format:check\":"
---

<objective>
Install ESLint 9 + Prettier + husky + lint-staged. Author `eslint.config.js` (flat config), `.prettierrc.json`, `.prettierignore`, and `.husky/pre-commit`. Add `lint`, `format`, `format:check`, `prepare` scripts to `package.json`. Verify `npm run lint` and `npm run format:check` both exit 0 against the current file set.

Purpose: Closes FOUND-05 (ESLint 9 flat config + Prettier configured for .astro/.ts/.tsx/.md; `npm run lint` passes). Honors all of D-15..D-20 (ESLint composition, Prettier separate, Prettier rules, ignore lists, enforcement layers, Conventional Commits documented not enforced).

Output: 4 new config files + `package.json` script section updates + a husky pre-commit hook + an installed dev-dep set, committed together.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lint+format+husky dep set, author eslint.config.js + Prettier configs</name>
  <files>package.json, package-lock.json, eslint.config.js, .prettierrc.json, .prettierignore</files>
  <read_first>
    - package.json (current — verify the ESLint 8.57.1 entry seen earlier is replaced, NOT left in place)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5 — eslint.config.js shape; Pattern 6 — Prettier config + ignore; Standard Stack table — pin versions; Pitfall A — ESLint 9 NOT 10; Pitfall C — parserOptions.project required; Pitfall F — tailwindcss plugin MUST be last)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-15 ESLint composition; D-16 Prettier separate; D-17 Prettier rules; D-18 ignore lists)
    - .planning/phases/01-foundation/01-PATTERNS.md (Shared Patterns — ignore lists MUST include `.planning/` and `.claude/`)
  </read_first>
  <action>
    Step A — Install the lint+format dep set with EXACT version pins per RESEARCH.md Standard Stack:

    `npm install -D eslint@^9.38 @eslint/js@^9.38 typescript-eslint@^8.60 eslint-plugin-astro@^1.7 eslint-plugin-jsx-a11y@^6.10 eslint-config-prettier@^10.1 globals@^17 eslint-plugin-react-hooks@^7.1 prettier@^3.8 prettier-plugin-astro@^0.14 prettier-plugin-tailwindcss@^0.8`

    CRITICAL — Pin `eslint@^9.38`, NOT `^9` or floating. Per RESEARCH.md Pitfall A: `eslint-plugin-jsx-a11y@6.10.2` peer caps at ESLint 9. Installing ESLint 10 (released Feb 2026, available on npm) would cause jsx-a11y rules to silently no-op or `npm ci` to fail on CI.

    Note — the existing `package.json` (per the snapshot from Plan 02) shows `eslint@^8.57.1`. This install MUST overwrite that pin to `^9.38`. After install run `npm ls eslint` and verify exactly one ESLint version, in the 9.38+ range.

    Step B — Author `eslint.config.js` per RESEARCH.md Pattern 5 (lines 592–642). Exact composition order in the `tseslint.config(...)` call:
    1. Source-attribution comment header naming `eslint.org/docs/latest/use/configure/configuration-files`, `typescript-eslint.io/getting-started`, `eslint-plugin-astro`, `eslint-plugin-jsx-a11y`, plus `CONTEXT.md D-15, D-18`.
    2. Imports (ESM): `js from '@eslint/js'`, `tseslint from 'typescript-eslint'`, `astro from 'eslint-plugin-astro'`, `jsxA11y from 'eslint-plugin-jsx-a11y'`, `reactHooks from 'eslint-plugin-react-hooks'`, `prettier from 'eslint-config-prettier'`, `globals from 'globals'`.
    3. `tseslint.config(...)` entries in this order:
       a. An `{ ignores: [...] }` entry with the D-18 list — exactly: `'dist/'`, `'node_modules/'`, `'.astro/'`, `'coverage/'`, `'.planning/'`, `'.claude/'`. The `.planning/` and `.claude/` entries are non-negotiable per PATTERNS.md Shared Patterns.
       b. `js.configs.recommended`
       c. spread `...tseslint.configs.recommendedTypeChecked`
       d. A `languageOptions` entry setting `parserOptions: { project: true, tsconfigRootDir: import.meta.dirname }` and `globals: { ...globals.browser, ...globals.node }`. Pitfall C mitigation — without this, every type-checked rule errors with "parserServices to be generated".
       e. spread `...astro.configs.recommended`
       f. A jsx-a11y/react-hooks block scoped to `files: ['**/*.{jsx,tsx}']` per D-15 (jsx-a11y is JSX-only — NOT for `.astro` files). Plugins: `{ 'jsx-a11y': jsxA11y, 'react-hooks': reactHooks }`. Rules: `{ ...jsxA11y.flatConfigs.recommended.rules, ...reactHooks.configs.recommended.rules }`.
       g. `prettier` — MUST be the LAST entry per RESEARCH.md Pattern 5 note: "MUST be last — turns off stylistic conflicts with Prettier."

    Step C — Author `.prettierrc.json` per RESEARCH.md Pattern 6 (lines 657–678). Exact field values:
    - `printWidth: 100`
    - `tabWidth: 4` (D-17 — user-specific override)
    - `useTabs: false`
    - `semi: true`
    - `singleQuote: true`
    - `trailingComma: "all"`
    - `bracketSameLine: false`
    - `arrowParens: "always"`
    - `plugins: ["prettier-plugin-astro", "prettier-plugin-tailwindcss"]` — tailwindcss plugin MUST be LAST per Pitfall F. (Optionally add `"tailwindStylesheet": "./src/styles/global.css"` at top level; this is a recommendation in RESEARCH.md Pattern 6 to help v4 class-sorting resolve `@theme` definitions. Include it.)
    - An `overrides` entry mapping `*.astro` files to `parser: "astro"`.

    Step D — Author `.prettierignore` per RESEARCH.md Pattern 6 (lines 683–691). Contents (one per line):
    ```
    dist/
    node_modules/
    .astro/
    coverage/
    .planning/
    .claude/
    package-lock.json
    ```

    `package-lock.json` is in Prettier ignore only (not ESLint ignore) — large generated JSON, formatting it churns the diff.

    Step E — DO NOT run `npm run lint` or `npm run format:check` yet — Task 2 of this plan adds the scripts to `package.json` first.
    Do NOT commit yet — Task 3 commits the whole plan rollup.
  </action>
  <verify>
    <automated>npm ls eslint 2>&1 | grep -qE 'eslint@9\.(3[89]|[4-9][0-9])' && npm ls eslint-plugin-jsx-a11y 2>&1 | grep -q 'eslint-plugin-jsx-a11y@6\.' && npm ls prettier 2>&1 | grep -q 'prettier@3\.' && test -f eslint.config.js && grep -q "from 'typescript-eslint'" eslint.config.js && grep -q "from 'eslint-plugin-astro'" eslint.config.js && grep -q "from 'eslint-plugin-jsx-a11y'" eslint.config.js && grep -q "from 'eslint-config-prettier'" eslint.config.js && grep -q "parserOptions" eslint.config.js && grep -q "project: true" eslint.config.js && grep -q "'\.planning/'" eslint.config.js && grep -q "'\.claude/'" eslint.config.js && tail -3 eslint.config.js | grep -q 'prettier' && grep -q "'\*\*/\*\.{jsx,tsx}'" eslint.config.js && test -f .prettierrc.json && grep -q '"tabWidth": 4' .prettierrc.json && grep -q '"singleQuote": true' .prettierrc.json && grep -q '"trailingComma": "all"' .prettierrc.json && python3 -c "import json,sys; c=json.load(open('.prettierrc.json')); p=c['plugins']; sys.exit(0 if p[-1]=='prettier-plugin-tailwindcss' else 1)" && test -f .prettierignore && grep -q '^\.planning/$' .prettierignore && grep -q '^\.claude/$' .prettierignore && grep -q '^package-lock\.json$' .prettierignore</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls eslint` reports exactly one ESLint and the version is in the `9.38.x..9.99.x` range (NOT 10.x, NOT 8.x)
    - `npm ls eslint-plugin-jsx-a11y` reports `6.10.x`
    - `npm ls prettier` reports `3.8.x`
    - `eslint.config.js` exists and contains imports for typescript-eslint, eslint-plugin-astro, eslint-plugin-jsx-a11y, eslint-config-prettier
    - `eslint.config.js` contains `parserOptions: { project: true, tsconfigRootDir: import.meta.dirname }` (Pitfall C mitigation) — verified by greps for `parserOptions` AND `project: true`
    - `eslint.config.js` `ignores` array contains both `.planning/` and `.claude/` (D-18 + PATTERNS.md Shared Patterns)
    - `eslint.config.js` last config object is `prettier` (Pitfall F adjacent rule — eslint-config-prettier MUST be last in config array)
    - `eslint.config.js` has a jsx-a11y block scoped to `**/*.{jsx,tsx}` (D-15)
    - `.prettierrc.json` exists with `tabWidth: 4`, `singleQuote: true`, `trailingComma: "all"`
    - `.prettierrc.json` `plugins[last]` is `prettier-plugin-tailwindcss` (Pitfall F — must be last) — verified via the python3 JSON inspector in the verify command
    - `.prettierignore` contains `.planning/`, `.claude/`, and `package-lock.json`
  </acceptance_criteria>
  <done>Lint+format tooling installed with the exact ESLint 9 / jsx-a11y peer-compatible set; flat config + Prettier config + ignore list authored per RESEARCH.md Patterns 5 and 6 and CONTEXT.md D-15..D-18.</done>
</task>

<task type="auto">
  <name>Task 2: Install husky+lint-staged, author .husky/pre-commit, add package.json scripts, run lint+format gates</name>
  <files>package.json, package-lock.json, .husky/pre-commit</files>
  <read_first>
    - package.json (current — after Task 1)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 9 — husky v9 + lint-staged; Standard Stack — husky@^9.1.7, lint-staged@^17.0.5; Pitfall G — lint-staged engines require Node >=22.22.1 — dev env is 23.x and Plan 05 CI uses Node 22)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-16 lint/format script names; D-19 enforcement layers — CI gate + husky pre-commit + no .vscode requirement)
    - .planning/phases/01-foundation/01-PATTERNS.md (`package.json` analog — script block target shape)
  </read_first>
  <action>
    Step A — Install husky + lint-staged:
    `npm install -D husky@^9 lint-staged@^17`

    Versions: husky `^9.1.7`, lint-staged `^17.0.5` per RESEARCH.md Standard Stack.

    Step B — Run `npx husky init`. This is the canonical husky v9 setup per RESEARCH.md Pattern 9. It performs two side-effects:
    1. Creates `.husky/pre-commit` containing `npm test` (default).
    2. Adds `"prepare": "husky"` to `package.json` scripts (this is what runs `husky install` on `npm install` in fresh clones).

    Step C — Overwrite `.husky/pre-commit` so it contains:
    ```
    npx lint-staged
    ```
    Per RESEARCH.md Pattern 9 line 836. husky v9's modern hook is a one-line shell script (no `_/husky.sh` shebang per v9 release notes).

    Step D — Update `package.json` `scripts` and add a `lint-staged` block. The target shape per RESEARCH.md Pattern 9 (lines 839–862):

    `scripts`:
    - `prepare`: `husky` (added by `npx husky init` in Step B)
    - `dev`: `astro dev` (from scaffold — keep)
    - `build`: `astro build` (from scaffold — keep)
    - `preview`: `astro preview` (from scaffold — keep)
    - `lint`: `eslint .`
    - `format`: `prettier --write .`
    - `format:check`: `prettier --check .`
    - `test`: `vitest run` — ADD this stub now even though Plan 05 installs vitest. Plan 05's first task asserts the script exists; we want a single `package.json` edit not two. (Yes, this script will fail until Plan 05 installs Vitest — that's fine; it's not invoked in this plan's verify steps.)

    Also add `"astro": "astro"` if the scaffold included it (most do).

    Add a top-level `lint-staged` block per RESEARCH.md Pattern 9 (lines 852–860):
    ```
    "lint-staged": {
        "*.{ts,tsx,astro,js,jsx}": ["eslint --fix", "prettier --write"],
        "*.{md,json,yml,yaml,css}": ["prettier --write"]
    }
    ```

    Optionally (per RESEARCH.md Open Question 2 + Pitfall G), add `"engines": { "node": ">=22.13.0" }`. Recommend INCLUDING this — matches `actions/setup-node@v4` Node 22 in CI (Plan 05) and `lint-staged@17`'s `>=22.22.1`. Cheap insurance.

    Step E — Run the lint and format gates to verify the configs work against the current file set:

    1. `npm run lint` — Expected exit 0. If failures: read the error output. Common issues:
       - jsx-a11y errors on `HydrationCheck.tsx`: the `type="button"` attribute is already set (Plan 03 Task 3), so this should be clean.
       - `typescript-eslint` type-checked rules complaining about cross-tree imports: the tsconfig include `**/*` covers `tests/`, so this should be clean.
       - Unused-import rules from strictest: fix in place (every import must be consumed).
       Do NOT mass-disable rules. Fix the specific issue.

    2. `npm run format:check` — Expected exit 0. If failures: run `npm run format` ONCE to apply Prettier, then re-run `format:check` to confirm 0 diffs. Stage the formatted files. Per PATTERNS.md Indentation: files were authored at 4-space indent so Prettier should be a no-op.

    Re-run `npx astro check` to verify it still exits 0 after any prettier rewrites.

    Do NOT commit yet.
  </action>
  <verify>
    <automated>npm ls husky 2>&1 | grep -q 'husky@9\.' && npm ls lint-staged 2>&1 | grep -q 'lint-staged@17\.' && test -f .husky/pre-commit && grep -q 'npx lint-staged' .husky/pre-commit && python3 -c "import json; p=json.load(open('package.json')); s=p['scripts']; assert s.get('lint')=='eslint .', s; assert s.get('format')=='prettier --write .', s; assert s.get('format:check')=='prettier --check .', s; assert s.get('prepare')=='husky', s; assert s.get('test')=='vitest run', s; assert 'lint-staged' in p, 'missing lint-staged block'; ls=p['lint-staged']; assert '*.{ts,tsx,astro,js,jsx}' in ls, ls; print('OK')" && npm run lint > /tmp/lint-04.log 2>&1 && npm run format:check > /tmp/format-04.log 2>&1 && npx astro check 2>&1 | grep -qE 'Result.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls husky` reports `husky@9.x` and `npm ls lint-staged` reports `lint-staged@17.x`
    - `.husky/pre-commit` exists and contains `npx lint-staged`
    - `package.json` `scripts` contains `lint`, `format`, `format:check`, `prepare`, `test` (in addition to the scaffold-provided `dev`/`build`/`preview`/`astro`)
    - `package.json` top-level `lint-staged` block exists with both glob patterns
    - `npm run lint` exits 0 with no ESLint errors against current file set
    - `npm run format:check` exits 0 with no Prettier diffs
    - `npx astro check` still exits 0
  </acceptance_criteria>
  <done>husky pre-commit hook in place; `npm run lint` and `npm run format:check` both gate clean; `package.json` script surface complete for FOUND-05 + Plan 05 CI consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the lint/format/husky tooling set</name>
  <files>(commit only)</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-20 — Conventional Commits documented not enforced; we still WRITE conventional commits, but no commitlint config)
    - .planning/phases/01-foundation/01-PATTERNS.md (Conventional Commits style example)
  </read_first>
  <action>
    Stage the new files and the updates from Tasks 1–2:

    `git add eslint.config.js .prettierrc.json .prettierignore .husky/pre-commit package.json package-lock.json`

    Verify staging — `git diff --cached --name-only` should show those 6 paths (and only those).

    Commit with Conventional Commits scope `01`:
    `chore(01): wire ESLint 9 + Prettier + husky + lint-staged`

    Commit body should reference: D-15 (ESLint composition), D-16 (Prettier separate), D-17 (Prettier rules), D-18 (ignore lists), D-19 (enforcement layers: CI + pre-commit), and FOUND-05.

    Push: `git push origin main`.

    Step B (optional but recommended per D-20) — Document Conventional Commits convention. D-20 says it can go in `CONTRIBUTING.md` OR be appended to `.claude/CLAUDE.md`. Phase 1 is the right time to document, but D-20 is also explicit that this is NOT gated (no commitlint). Recommendation: APPEND a short section to the existing `.claude/CLAUDE.md` (file already exists per gitStatus) titled "Commit Conventions" naming the format `<type>(<scope>): <subject>` and listing accepted types `feat | fix | chore | docs | test | refactor`. Scope is the phase number (e.g., `01`, `02`).

    If this append happens, stage and add to the same commit (or a separate `docs(01): document Conventional Commits convention` commit). The append is small enough that adding it to the rollup commit is fine; reflect it in the commit body.
  </action>
  <verify>
    <automated>git diff HEAD~1 HEAD --name-only > /tmp/last-commit-04.txt && grep -q 'eslint.config.js' /tmp/last-commit-04.txt && grep -q '.prettierrc.json' /tmp/last-commit-04.txt && grep -q '.prettierignore' /tmp/last-commit-04.txt && grep -q '.husky/pre-commit' /tmp/last-commit-04.txt && grep -q 'package.json' /tmp/last-commit-04.txt && git log -1 --pretty=%s | grep -qE '(ESLint|Prettier|husky|lint)' && git log -1 --pretty=%s | grep -q '(01)' && [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit introduces all 5+ tooling files (verified by `git diff HEAD~1 HEAD --name-only`)
    - Commit subject is Conventional Commits with scope `(01)` and references ESLint/Prettier/husky/lint-staged
    - `git rev-parse HEAD` equals `git rev-parse @{u}` (pushed to origin/main)
  </acceptance_criteria>
  <done>One commit on `main` containing eslint.config.js + .prettierrc.json + .prettierignore + .husky/pre-commit + package.json updates + lockfile. FOUND-05 closed (final CI confirmation comes from Plan 05's CI workflow).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Lint+format+husky packages enter `node_modules` |
| Git pre-commit hook → developer disk | `.husky/pre-commit` runs `npx lint-staged` against staged files; any local Node misconfiguration trips this |
| `.claude/` files → lint/format pipeline | `.planning/` and `.claude/` markdown are critical artifacts; misconfigured ignore lists would mass-rewrite them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | npm install | mitigate | All packages (`eslint`, `@eslint/js`, `typescript-eslint`, `eslint-plugin-astro`, `eslint-plugin-jsx-a11y`, `eslint-config-prettier`, `globals`, `eslint-plugin-react-hooks`, `prettier`, `prettier-plugin-astro`, `prettier-plugin-tailwindcss`, `husky`, `lint-staged`) cleared by slopcheck in RESEARCH.md Package Legitimacy Audit |
| T-01-04-02 | Tampering | `.planning/` + `.claude/` markdown | mitigate | Both directories in ESLint `ignores` AND `.prettierignore` (D-18). Task 1 grep gates verify both ignore lists include these paths |
| T-01-04-03 | Tampering | ESLint version pin | mitigate | Pin `eslint@^9.38` per Pitfall A — ESLint 10 would break jsx-a11y peer; npm ls assertion in Task 1 catches drift |
| T-01-04-04 | Denial of Service | pre-commit hook | mitigate | husky/lint-staged engines require Node 22.22.1+ (Pitfall G); dev env is 23.x; `engines.node` pinned to `>=22.13.0` for documentation |
| T-01-04-05 | Tampering | prettier-plugin-tailwindcss order | mitigate | Pitfall F — tailwindcss plugin MUST be last in `plugins` array; verify command uses python3 to assert this |

| Category | Applies | Rationale |
|----------|---------|-----------|
| Spoofing | no | No auth |
| Tampering | yes | Supply chain (slopcheck approved); plugin order; ignore-list correctness |
| Repudiation | no | Single-dev |
| Information Disclosure | no | Lint+format produce no secrets |
| Denial of Service | yes (low) | Misconfigured pre-commit could block developer commits; verify-time gates catch this |
| Elevation of Privilege | no | Hooks run with user privileges only |
</threat_model>

<verification>
- `npm ls eslint` reports `eslint@9.38+` (NOT 10.x, NOT 8.x)
- `npm run lint` exits 0
- `npm run format:check` exits 0
- `npx astro check` exits 0
- `eslint.config.js` ignores `.planning/` AND `.claude/`
- `.prettierignore` ignores `.planning/`, `.claude/`, and `package-lock.json`
- `.prettierrc.json` `plugins[last]` is `prettier-plugin-tailwindcss`
- `.husky/pre-commit` runs `npx lint-staged`
- One commit landed on origin/main
</verification>

<success_criteria>
- All 10 `must_haves.truths` demonstrably true
- All 5 `must_haves.artifacts` exist on disk
- FOUND-05 fully satisfied at the file/script level (Plan 05's CI workflow runs `npm run lint && npm run format:check` again as the final gate)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done. Record:
- `eslint.config.js` contents (or diff against Pattern 5)
- `.prettierrc.json` contents
- `.prettierignore` contents
- The output of `npm ls eslint eslint-plugin-jsx-a11y prettier husky lint-staged`
- The commit SHA
- Confirmation that `npm run lint && npm run format:check` both exit 0
- Note: Plan 05 runs the same gates inside a CI workflow on push/PR to `main`
</output>
