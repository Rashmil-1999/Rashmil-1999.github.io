---
phase: 01-foundation
plan: 05
type: execute
wave: 5
depends_on:
    - 01-04-PLAN.md
files_modified:
    - vitest.config.ts
    - tests/global-setup.ts
    - tests/smoke.test.ts
    - .github/workflows/ci.yml
    - package.json
    - package-lock.json
autonomous: true
requirements:
    - FOUND-06
must_haves:
    truths:
        - "`vitest.config.ts` exists at repo root and uses `getViteConfig` from `astro/config` to share Astro's Vite pipeline"
        - "`vitest.config.ts` declares `globalSetup: ['./tests/global-setup.ts']`"
        - "`tests/global-setup.ts` spawns `astro build` via `node:child_process.spawnSync` and throws if exit code is non-zero (D-21)"
        - "`tests/smoke.test.ts` contains exactly 5 assertions per D-23: dist/index.html exists; contains 8 lowercase section ids; dist/__hydration-test/index.html exists; dist/_astro/ has a .js chunk; dist/_astro/*.css contains `#abc123`"
        - "`.github/workflows/ci.yml` triggers on push to `main` AND pull_request to `main` (NOT `master`)"
        - "`.github/workflows/ci.yml` steps in order: checkout → setup-node@v4 (node 22, npm cache) → npm ci → npm run lint → npm run format:check → npx astro check → npm test (D-19 + D-25)"
        - "`.github/workflows/ci.yml` does NOT contain a `deploy` step, an `actions/deploy-pages` action, or a `permissions: { pages: write }` block (Phase 5 owns deploy)"
        - "`npm test` exits 0 — Vitest globalSetup spawns `astro build`, then all 5 smoke-test assertions pass"
        - "Deliberately renaming the marker class from `text-[#abc123]` to `text-[#ffffff]` in About.astro and re-running `npm test` causes assertion #5 to fail (validates the test actually gates on the marker, per SC #3)"
    artifacts:
        - path: "vitest.config.ts"
          provides: "Vitest config sharing Astro's Vite pipeline (D-24)"
          contains: "getViteConfig"
        - path: "tests/global-setup.ts"
          provides: "Vitest globalSetup that spawns `astro build` once (D-21)"
          contains: "spawnSync"
        - path: "tests/smoke.test.ts"
          provides: "5-assertion smoke suite per D-23"
          contains: "describe"
        - path: ".github/workflows/ci.yml"
          provides: "CI gate (NOT deploy) per D-25"
          contains: "npm test"
    key_links:
        - from: "vitest.config.ts"
          to: "tests/global-setup.ts"
          via: "globalSetup config option"
          pattern: "global-setup"
        - from: "tests/global-setup.ts"
          to: "astro build subprocess"
          via: "spawnSync('npx', ['astro', 'build'])"
          pattern: "spawnSync"
        - from: "tests/smoke.test.ts"
          to: "dist/"
          via: "fs read of dist/index.html, dist/__hydration-test/, dist/_astro/"
          pattern: "join\\(DIST"
        - from: ".github/workflows/ci.yml"
          to: "npm scripts (lint, format:check, test)"
          via: "run steps"
          pattern: "npm test"
---

<objective>
Install Vitest. Author `vitest.config.ts`, `tests/global-setup.ts`, `tests/smoke.test.ts` per RESEARCH.md Pattern 7 — Vitest's globalSetup spawns `astro build` once via `node:child_process.spawnSync` (Pitfall 30: render-time tests don't catch what build-time tests do), then 5 assertions run against `dist/`. Author `.github/workflows/ci.yml` per Pattern 10 — the four-step gate (lint, format:check, astro check, test) on push/PR to `main`. No deploy step (Phase 5 owns deploy).

Purpose: Closes FOUND-06 (Vitest 3 [or 4] configured with one smoke test that runs `astro build` and asserts each section marker). Closes the final piece of ROADMAP Phase 1 SC #3 ("deliberately breaking the build causes the test to fail" — implicitly satisfied by globalSetup throw per D-21) and SC #5 (Tailwind utility in `.astro` produces matching CSS in `dist/_astro/` — assertion #5 of the smoke test).

Output: 4 new files + `package.json` (Vitest dep added, `test: vitest run` script already present from Plan 04) + a passing `npm test` run + one commit landing on `main`.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest, author vitest.config.ts + global-setup.ts + smoke.test.ts</name>
  <files>package.json, package-lock.json, vitest.config.ts, tests/global-setup.ts, tests/smoke.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 7 — vitest.config.ts, global-setup.ts, smoke.test.ts exact shapes; Standard Stack — `vitest@^3.2 or ^4.3`; Pitfall 30 — render-time tests don't suffice; Assumption A4 — `node:child_process.spawnSync` is sufficient; Open Question 1 — pick Vitest 4.x)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-21..D-25 smoke test mechanics; D-23 the 5 assertions; D-24 file paths)
    - .planning/phases/01-foundation/01-PATTERNS.md (Pattern source table — these 3 files have No analog, use Pattern 7 verbatim)
    - tests/__fixtures__/HydrationCheck.tsx (already exists from Plan 03 — must NOT be deleted; smoke test reads `dist/_astro/*.js` not the fixture source)
    - package.json (verify `test: vitest run` script is already there from Plan 04 Task 2)
  </read_first>
  <action>
    Step A — Install Vitest. Per RESEARCH.md Open Question 1 recommendation (Vitest 4.3.x): `npm install -D vitest@^4.3`.

    Per Assumption A2: if Vitest 4 has any interop surprise with Astro 6's Vite version (it should not — both use Vite 5+), fall back to `vitest@^3.2` and document the swap in the SUMMARY. Don't install both.

    Step B — Author `vitest.config.ts` at the repo root per RESEARCH.md Pattern 7 (lines 700–713). Exact shape:
    - Source-attribution comment header naming `vitest.dev/config/globalsetup` and `CONTEXT.md D-21, D-24`
    - Import `getViteConfig from 'astro/config'`
    - Default-export `getViteConfig({ test: { environment: 'node', include: ['tests/**/*.test.ts'], globalSetup: ['./tests/global-setup.ts'] } })`

    `environment: 'node'` is correct — the smoke test reads filesystem only; no jsdom needed (Phase 2+ may install jsdom if DOM tests are added — not Phase 1's job).

    `include: ['tests/**/*.test.ts']` keeps the test discovery scoped to `tests/` (D-24 — tests are NOT under `src/`).

    Indent 4 spaces per D-17.

    Step C — Author `tests/global-setup.ts` per RESEARCH.md Pattern 7 (lines 715–735). Exact shape:
    - Source-attribution comment header noting "Spawns `astro build` ONCE before any test file runs" and naming `CONTEXT.md D-21`
    - Import `spawnSync from 'node:child_process'`
    - Default-export a function `setup` (NOT a top-level call — Vitest 4 invokes the default export)
    - Inside `setup`: call `spawnSync('npx', ['astro', 'build'], { stdio: 'inherit', env: process.env })`
    - If `result.status !== 0`: `throw new Error(\`astro build exited with status ${result.status} — smoke test aborted.\`)`
    - No teardown — leave `dist/` in place for inspection (per Pattern 7 comment "No teardown")

    Indent 4 spaces.

    Step D — Author `tests/smoke.test.ts` per RESEARCH.md Pattern 7 (lines 737–792). Exact shape:
    - Comment header naming `CONTEXT.md D-23`
    - Imports: `existsSync`, `readFileSync`, `readdirSync` from `node:fs`; `join` from `node:path`; `describe`, `expect`, `it` from `vitest`
    - Constant `DIST = join(process.cwd(), 'dist')`
    - Constant `sectionIds = ['sidenav', 'about', 'education', 'work', 'skills', 'projects', 'leadership', 'testimonials']` (lowercase per D-23 — these MUST match the ids Plan 03 Task 2 authored)
    - `describe('Phase 1 smoke', () => { ... })` containing 5 `it(...)` blocks:

    1. `it('emits dist/index.html', ...)` — `expect(existsSync(join(DIST, 'index.html'))).toBe(true)`
    2. `it('dist/index.html contains all 8 section ids', ...)` — readFile, loop the 8 ids, `expect(html).toContain(\`id="${id}"\`)` for each
    3. `it('emits dist/__hydration-test/index.html', ...)` — `expect(existsSync(join(DIST, '__hydration-test', 'index.html'))).toBe(true)`
    4. `it('emits a React JS chunk in dist/_astro/', ...)` — readdir `dist/_astro`, filter `.js`, `expect(jsChunks.length).toBeGreaterThan(0)`
    5. `it('emits the Tailwind marker utility in dist/_astro/*.css', ...)` — readdir `dist/_astro`, filter `.css`, concatenate file contents, `expect(cssBlob).toMatch(/#abc123/i)` (Pitfall 29 / D-12 mitigation)

    All 5 assertions are derived literally from RESEARCH.md Pattern 7 — they form the minimum-viable D-23 set.

    Indent 4 spaces.

    Step E — Run `npm test`. Expected behavior:
    - Vitest discovers `tests/smoke.test.ts`.
    - globalSetup spawns `astro build` (stdio inherit — output appears in test log).
    - `astro build` exits 0 (no regressions from Plans 01–04).
    - Vitest runs the 5 assertions; all pass.
    - `npm test` exits 0.

    If `npm test` fails:
    - globalSetup throw → `astro build` is broken; re-run `npm run build` to see the actual build error and fix it.
    - Assertion 1/3 fail → check `dist/` structure; the build wrote a different layout (unlikely with Astro 6 default).
    - Assertion 2 fails → re-verify Plan 03 Task 2 stub ids match the lowercase set exactly.
    - Assertion 4 fails → `dist/_astro/` has no JS; the hydration island didn't ship a chunk. Possible cause: `client:load` directive missing from `__hydration-test.astro` (verify Plan 03 Task 3).
    - Assertion 5 fails → Pitfall 29 firing! Tailwind v4 source detection missed the `.astro` file. Add `@source "../**/*.{astro,html,ts,tsx}";` to `src/styles/global.css` immediately after `@import "tailwindcss";`, then re-run `npm test`. Record this in the SUMMARY because it's a known potential pitfall (Pitfall 29 / Pitfall B).

    Step F (optional sanity check per the "deliberately breaking the build causes the test to fail" clause of ROADMAP SC #3): temporarily rename `text-[#abc123]` → `text-[#ffffff]` in `src/components/About.astro`, run `npm test`, expect assertion #5 to fail. Then revert the rename. This is the inversion test from D-21 (it isn't a separate test file — the globalSetup-throws-on-build-failure pattern + the marker assertion together cover SC #3 implicitly). Document in SUMMARY whether this manual inversion was performed.

    Do NOT commit yet — Task 2 of this plan adds the CI workflow and commits together.
  </action>
  <verify>
    <automated>npm ls vitest 2>&1 | grep -qE 'vitest@(3|4)\.' && test -f vitest.config.ts && grep -q "from 'astro/config'" vitest.config.ts && grep -q 'getViteConfig' vitest.config.ts && grep -q "globalSetup: \['./tests/global-setup.ts'\]" vitest.config.ts && test -f tests/global-setup.ts && grep -q "from 'node:child_process'" tests/global-setup.ts && grep -q 'spawnSync' tests/global-setup.ts && grep -q 'throw new Error' tests/global-setup.ts && test -f tests/smoke.test.ts && grep -q "from 'vitest'" tests/smoke.test.ts && grep -q "describe\(.Phase 1 smoke" tests/smoke.test.ts && grep -c 'it(' tests/smoke.test.ts | grep -qE '^5$' && grep -q "sectionIds = \[" tests/smoke.test.ts && grep -q "'sidenav'" tests/smoke.test.ts && grep -q "'work'" tests/smoke.test.ts && grep -q '/#abc123/' tests/smoke.test.ts && npm test > /tmp/smoke-test.log 2>&1 && grep -qE '5 passed|✓.*5' /tmp/smoke-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls vitest` reports `vitest@3.x` or `vitest@4.x`
    - `vitest.config.ts` exists at repo root and uses `getViteConfig` from `astro/config`
    - `vitest.config.ts` `globalSetup` points to `./tests/global-setup.ts`
    - `tests/global-setup.ts` uses `spawnSync` from `node:child_process` and throws on non-zero exit
    - `tests/smoke.test.ts` exists, imports from `vitest`, contains exactly 5 `it(` blocks, references the 8 lowercase section ids (including `sidenav` and `work`), and contains the `/#abc123/` regex
    - `npm test` exits 0 — globalSetup builds successfully and all 5 assertions pass
  </acceptance_criteria>
  <done>Vitest installed; smoke test 5-assertion suite per D-23 passes against Plan 03's build output. FOUND-06 satisfied (one smoke test, asserting each of 8 section markers + hydration page + React chunk + Tailwind marker).</done>
</task>

<task type="auto">
  <name>Task 2: Author .github/workflows/ci.yml, run the full CI gate locally, commit</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 10 — ci.yml exact shape; Pitfall 12 noted — Phase 5 deploy.yml is separate; Pitfall H — workflow MUST trigger on `main` not `master`; Pitfall G — Node 22 needed for lint-staged)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-19 enforcement layers — CI gate runs the 4 commands; D-25 ci.yml triggers on PR + push to `main`, SEPARATE from Phase 5 deploy.yml)
    - .planning/phases/01-foundation/01-PATTERNS.md (.github/workflows/ci.yml — No analog — new file)
  </read_first>
  <action>
    Step A — Author `.github/workflows/ci.yml` per RESEARCH.md Pattern 10 (lines 874–902). Exact shape:

    - Top-level comment header naming `docs.github.com/en/actions/quickstart`, `github.com/actions/setup-node`, and `CONTEXT.md D-25`
    - `name: CI`
    - `on:` block with `push: { branches: [main] }` AND `pull_request: { branches: [main] }` — both on `main`, NOT `master` (Pitfall H)
    - `jobs: { ci: { runs-on: ubuntu-latest, steps: [ ... ] } }`
    - Steps in this exact order:
      1. `uses: actions/checkout@v4`
      2. `uses: actions/setup-node@v4` with `with: { node-version: '22', cache: 'npm' }` (Pitfall G — node 22 LTS; Pattern 10 line 894)
      3. `run: npm ci` (enforces single-lockfile constraint per FOUND-07 — fails if `yarn.lock` reappears)
      4. `run: npm run lint`
      5. `run: npm run format:check`
      6. `run: npx astro check`
      7. `run: npm test`

    No `permissions:` block (Pattern 10 note: CI is read-only; default `GITHUB_TOKEN` permissions are read-only on PRs). Phase 5's `deploy.yml` adds `pages: write` and `id-token: write` separately.

    DO NOT add any deploy step here. DO NOT add `actions/deploy-pages`. DO NOT add `withastro/action`. Those belong to Phase 5's separate `deploy.yml` (per ROADMAP Phase 5 and D-25 "SEPARATE from the Phase 5 deploy.yml; both workflows coexist after Phase 5 ships").

    Indent 2 spaces (YAML idiom — Prettier handles YAML with 2-space indent by default; this is one of the few file types where the project's `tabWidth: 4` doesn't apply because YAML's parser is whitespace-significant in a different way and community standard is 2-space. Verify by running `npm run format:check` after authoring; if Prettier reformats it, accept Prettier's output as canonical).

    Step B — Local CI dry-run. Simulate what the CI workflow will do, locally:

    1. `npm ci` (ensures lockfile-only install works — this is what CI runs). May fail if `node_modules/` state has drifted from `package-lock.json`; if so, this is the same failure CI would surface — fix locally first by `rm -rf node_modules && npm ci`.
    2. `npm run lint`
    3. `npm run format:check`
    4. `npx astro check`
    5. `npm test`

    All 5 commands must exit 0. If any fail, fix BEFORE committing — the CI workflow will reject the push otherwise (in fact it will allow the push but show a red X on the commit; better to land green).

    Step C — Stage and commit. `git add .github/workflows/ci.yml`. Commit message:
    `ci(01): add CI workflow (lint + format:check + astro check + test)`

    Note the scope is `ci` here, not `chore` — Conventional Commits typically uses `ci:` for workflow files. Body should reference D-25 (CI gate vs Phase 5 deploy.yml) and FOUND-06.

    Push: `git push origin main`. This push triggers the CI workflow itself on GitHub. After a minute, verify (manually or via `gh run list --limit 3`) that the workflow ran and exited green. If it fails on GitHub but passes locally, the most likely cause is Node version mismatch (Pitfall G — dev env is Node 23, CI is Node 22) or an OS-specific path issue. Fix and push a follow-up commit.

    Step D — Verify the workflow ran successfully on GitHub. Use `gh run list --workflow=ci.yml --limit 1` to check status. If `gh` CLI is not configured, instruct the user to check the Actions tab on GitHub manually. The first run might take 1–3 minutes; do NOT block on it indefinitely — record the workflow URL/ID in the SUMMARY and note "first run in progress / passed / failed" as the final state.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'name: CI' .github/workflows/ci.yml && grep -q 'branches: \[main\]' .github/workflows/ci.yml && ! grep -q 'branches: \[master\]' .github/workflows/ci.yml && grep -q 'actions/checkout@v4' .github/workflows/ci.yml && grep -q 'actions/setup-node@v4' .github/workflows/ci.yml && grep -q "node-version: '22'" .github/workflows/ci.yml && grep -q "cache: 'npm'" .github/workflows/ci.yml && grep -q 'npm ci' .github/workflows/ci.yml && grep -q 'npm run lint' .github/workflows/ci.yml && grep -q 'npm run format:check' .github/workflows/ci.yml && grep -q 'npx astro check' .github/workflows/ci.yml && grep -q 'npm test' .github/workflows/ci.yml && ! grep -q 'actions/deploy-pages' .github/workflows/ci.yml && ! grep -q 'withastro/action' .github/workflows/ci.yml && ! grep -q 'pages: write' .github/workflows/ci.yml && git log -1 --pretty=%s | grep -qE '(CI|ci\()' && [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - `name: CI` present
    - Workflow triggers on `branches: [main]` for both push and pull_request (and does NOT trigger on `master`)
    - Steps in order: checkout@v4, setup-node@v4 (node 22, cache npm), npm ci, npm run lint, npm run format:check, npx astro check, npm test
    - Workflow does NOT contain `actions/deploy-pages`, `withastro/action`, or `pages: write` (Phase 5 owns deploy)
    - HEAD commit message starts with `ci(01):` or includes `CI` in subject
    - `git rev-parse HEAD` equals `git rev-parse @{u}` (pushed to origin/main)
    - (Manual / SUMMARY-recorded) The CI workflow's first run on GitHub Actions exited 0 (status: success). Verified via `gh run list --workflow=ci.yml --limit 1` if available.
  </acceptance_criteria>
  <done>CI workflow on `main` runs lint+format:check+astro-check+test on every push and PR. No deploy step (Phase 5 owns that). FOUND-06 fully closed; ROADMAP Phase 1 SC #1, SC #2, SC #3, SC #4, SC #5 all gated by CI from this commit forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo | The CI workflow runs with the default read-only `GITHUB_TOKEN`; it does NOT need write access to repo or Pages in Phase 1 |
| Vitest globalSetup → child process (`astro build`) | A subprocess spawn that inherits stdio + env; build failures translate to test failures |
| `dist/` (build output) → test assertions | The test reads `dist/` contents (filesystem only); no network, no execution of `dist/` code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | npm install (vitest) | mitigate | `vitest` was cleared by slopcheck in RESEARCH.md Package Legitimacy Audit |
| T-01-05-02 | Elevation of Privilege | GitHub Actions token | mitigate | No `permissions:` block → defaults to read-only `GITHUB_TOKEN`. Phase 5's deploy.yml separately scopes write permissions (Pitfall 12) |
| T-01-05-03 | Denial of Service | live site during cutover | mitigate | CI workflow does NOT deploy. The `gh-pages` branch (live site) stays untouched until Phase 5's cutover bundle |
| T-01-05-04 | Tampering | branch trigger | mitigate | Workflow triggers on `main` not `master` (Pitfall H). Verify command greps for the absence of `master`. |
| T-01-05-05 | Information Disclosure | test output (stdio: inherit) | accept | Build log on CI shows file paths but no secrets; the repo has no secrets/env vars in Phase 1 (RESEARCH.md Security Domain) |
| T-01-05-06 | Tampering | Vitest globalSetup default-export shape | mitigate | RESEARCH.md Assumption A4 — Vitest 4's globalSetup accepts a default-export function that throws on failure; if the shape is incompatible, fallback to Vitest 3.2 (Open Question 1) |

| Category | Applies | Rationale |
|----------|---------|-----------|
| Spoofing | no | No auth surface |
| Tampering | yes | Supply chain (vitest — approved); branch-trigger correctness; build-output integrity |
| Repudiation | no | git + Actions logs sufficient |
| Information Disclosure | no | No secrets in Phase 1 |
| Denial of Service | yes (low) | Misconfigured CI could spam runner minutes; Phase 1 is one workflow, scoped to main |
| Elevation of Privilege | yes (mitigated) | Default GITHUB_TOKEN is read-only; no permissions block elevates anything |
</threat_model>

<verification>
- `npm test` exits 0 (locally)
- `npm ls vitest` reports `vitest@3` or `vitest@4`
- `.github/workflows/ci.yml` triggers on `main` for push + PR, has the 4-gate step sequence, has no deploy step
- The CI workflow's first run on GitHub Actions exits 0 (recorded in SUMMARY)
- All 5 smoke test assertions defined per D-23
- One commit landed on origin/main with the CI workflow
</verification>

<success_criteria>
- All 9 `must_haves.truths` demonstrably true
- All 4 `must_haves.artifacts` exist
- FOUND-06 fully satisfied (Vitest configured, one smoke test spawns `astro build`, asserts each of 8 section markers + hydration page + React chunk + Tailwind marker)
- ROADMAP Phase 1 SC #1, SC #2, SC #3, SC #4, SC #5 all are now provable end-to-end:
  - SC #1: `npm run build` produces `dist/` with working `index.html` — verified by smoke assertion 1+2
  - SC #2: `npx astro check` exits 0 AND `npm run lint` exits 0 — both run in CI step 4 + 5
  - SC #3: `npm test` exits 0 AND broken build kills suite — D-21 globalSetup throw covers
  - SC #4: One lockfile, no duplicate React — `npm ci` in CI step 3 enforces; `npm ls react react-dom @astrojs/react` clean (Plan 02 Task 2 verified)
  - SC #5: Tailwind utility in `.astro` produces matching CSS in `dist/_astro/` — smoke assertion 5
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done. Record:
- The contents of `vitest.config.ts`, `tests/global-setup.ts`, `tests/smoke.test.ts` (or diffs from Pattern 7)
- The contents of `.github/workflows/ci.yml` (or diff from Pattern 10)
- The output of `npm test` (full passing log)
- Confirmation of all 5 ROADMAP SC items (with the verifying command per criterion)
- Whether Pitfall 29 fired (`@source` escalation needed) — yes/no
- Whether the inversion test (Step F of Task 1) was performed — yes/no/skipped
- The CI workflow's first-run status on GitHub Actions (success / pending / failed) with the run URL
- The commit SHA
- Mark Phase 1 as ready for `/gsd-transition`. All 8 FOUND-* requirements closed.
</output>
