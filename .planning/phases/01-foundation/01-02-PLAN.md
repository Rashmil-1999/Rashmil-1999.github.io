---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
    - 01-01-PLAN.md
files_modified:
    - astro.config.mjs
    - tsconfig.json
    - package.json
    - package-lock.json
autonomous: true
requirements:
    - FOUND-01
    - FOUND-02
    - FOUND-03
    - FOUND-04
    - FOUND-07
    - FOUND-08
must_haves:
    truths:
        - "`astro.config.mjs` sets `site: 'https://Rashmil-1999.github.io'`, `output: 'static'`, integrations `[react()]`, vite plugins `[tailwindcss()]`, and does NOT set `base`"
        - "tsconfig extends `astro/tsconfigs/strictest`"
        - "`npx astro check` exits 0 with strict TypeScript active"
        - "`npm run build` exits 0 (produces dist/index.html — placeholder content is fine in this plan)"
        - "Exactly one lockfile exists (`package-lock.json`); `npm ls react react-dom @astrojs/react` reports no UNMET / EXTRANEOUS / INVALID entries"
        - "`react@^19` and `react-dom@^19` are installed (not 18.x)"
    artifacts:
        - path: "astro.config.mjs"
          provides: "Astro config wired with React + Tailwind v4 via Vite plugin + canonical site URL (FOUND-01, FOUND-03, FOUND-04, FOUND-08)"
          contains: "site: 'https://Rashmil-1999.github.io'"
        - path: "tsconfig.json"
          provides: "TypeScript strictest preset (FOUND-02 + D-10)"
          contains: "astro/tsconfigs/strictest"
        - path: "package.json"
          provides: "Single-lockfile npm ecosystem with @astrojs/react + tailwindcss + @tailwindcss/vite (FOUND-03, FOUND-04, FOUND-07)"
          contains: "@tailwindcss/vite"
    key_links:
        - from: "astro.config.mjs"
          to: "@tailwindcss/vite"
          via: "vite.plugins array"
          pattern: "tailwindcss\\(\\)"
        - from: "astro.config.mjs"
          to: "@astrojs/react"
          via: "integrations array"
          pattern: "react\\(\\)"
        - from: "tsconfig.json"
          to: "astro/tsconfigs/strictest"
          via: "extends"
          pattern: "astro/tsconfigs/strictest"
---

<objective>
Wire React + Tailwind v4 integrations into `astro.config.mjs`, set the canonical site URL, tighten `tsconfig.json` to extend `astro/tsconfigs/strictest`, and verify a clean build + clean `astro check` + single-lockfile state.

Purpose: Close out FOUND-01 (Astro 6 static output), FOUND-02 (TS strict + astro check), FOUND-03 (Tailwind via `@tailwindcss/vite`), FOUND-04 (`@astrojs/react` installed + ready for hydration in Plan 03), FOUND-07 (single npm lockfile), FOUND-08 (`site` set, `output: 'static'`, no `base`). The hydration fixture itself ships in Plan 03 — this plan only verifies the integrations install and resolve.

Output: An `astro.config.mjs` matching RESEARCH.md Pattern 1, a `tsconfig.json` matching Pattern 8, an installed dep tree with React 19 + Tailwind v4 + integrations, all gated by `astro check` exit 0 and `astro build` exit 0.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @astrojs/react and Tailwind v4 (via @tailwindcss/vite) integrations</name>
  <files>astro.config.mjs, package.json, package-lock.json</files>
  <read_first>
    - astro.config.mjs (current scaffold state from Plan 01 Task 4)
    - package.json (verify the dep set inherited from the scaffold)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 — `astro.config.mjs` shape; Installation Command block — exact `npx astro add` sequence; Pitfall D — already mitigated, `yarn.lock` is gone; Anti-Patterns — never `@astrojs/tailwind`)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 BaseHead is stubbed in Plan 03; D-13 BaseHead minimal; FOUND-08 — `site` set, `output: 'static'`, no `base`)
    - .planning/phases/01-foundation/01-PATTERNS.md (No analog — uses Pattern 1 verbatim with annotations)
  </read_first>
  <action>
    Step A — Run `npx astro add react --yes` to install `@astrojs/react`, the React 19 peer pair (`react`, `react-dom` at `^19`), and `@types/react`/`@types/react-dom` at `^19`. This command modifies `astro.config.mjs` to add `integrations: [react()]` and updates `tsconfig.json` (which Task 2 of this plan will overwrite). Use `--yes` to accept all prompts non-interactively.

    Step B — Run `npx astro add tailwind --yes` to install `tailwindcss` + `@tailwindcss/vite` and add the Vite plugin to `astro.config.mjs`. Per RESEARCH.md State of the Art table + Anti-Patterns: this MUST use `@tailwindcss/vite` (Astro 5.2+ default), NOT `@astrojs/tailwind` (which is v3-only, last published 2024-09). Verify post-install that `astro.config.mjs` contains `vite: { plugins: [tailwindcss()] }` and that `package.json` lists `@tailwindcss/vite` and `tailwindcss` — NOT `@astrojs/tailwind`. If by any chance the older integration is added (it should not be in current versions), `npm uninstall @astrojs/tailwind && npm install -D @tailwindcss/vite tailwindcss` and hand-edit `astro.config.mjs`.

    Step C — Hand-edit `astro.config.mjs` so the final file matches the shape in RESEARCH.md Pattern 1 (lines 414–427):
    - `defineConfig` import from `astro/config`
    - `react` import from `@astrojs/react`
    - `tailwindcss` import from `@tailwindcss/vite`
    - `site: 'https://Rashmil-1999.github.io'` (FOUND-08 — exact string, preserve case)
    - NO `base` field (FOUND-08 + Anti-Pattern: user-site repo serves at `/`; setting `base: '/Rashmil-1999.github.io/'` doubles the URL per Pitfall 10)
    - `output: 'static'` (FOUND-01)
    - `integrations: [react()]` (FOUND-04)
    - `vite: { plugins: [tailwindcss()] }` (FOUND-03)

    The Pattern 1 reference includes the source-attribution comment header at the top (`// astro.config.mjs` plus the canonical source URLs and CONTEXT.md D-13). Copy that header verbatim per PATTERNS.md "File-header source-attribution comment style".

    Step D — Verify `package.json` deps. Expected `dependencies` after this task (versions floating to latest minor/patch in their major):
    - `astro` ^6.3
    - `@astrojs/react` ^5.0
    - `react` ^19
    - `react-dom` ^19
    - `tailwindcss` ^4.3
    - `@tailwindcss/vite` ^4.3

    Expected `devDependencies` (may be promoted by `astro add` either way):
    - `@types/react` ^19
    - `@types/react-dom` ^19
    - `@astrojs/check` ^0.9 (carried from the existing pre-Phase-1 install OR added by scaffold — confirm present)
    - `typescript` ^5.9 (verify — current `package.json` had `^6.0.3` per PATTERNS.md, RESEARCH.md Standard Stack recommends `^5.9`; @astrojs/check 0.9.9 peer accepts both, but pin to `^5.9` for safety. If `typescript@^6` was installed, `npm install -D typescript@^5.9` to downgrade.)

    Do NOT install Vitest, ESLint, Prettier, husky, or lint-staged in this plan — Plans 04 and 05 own those.

    Step E — Run `npm run build` to confirm integrations resolve and the scaffold still builds. Expect `dist/index.html` to be produced. Failures here usually mean `astro.config.mjs` syntax or an integration was added but its package wasn't installed.

    Step F — Do NOT commit yet — Task 2 of this plan finishes the strictest tsconfig and commits both.
  </action>
  <verify>
    <automated>grep -q "site: 'https://Rashmil-1999.github.io'" astro.config.mjs && grep -q "output: 'static'" astro.config.mjs && grep -q "react()" astro.config.mjs && grep -q "tailwindcss()" astro.config.mjs && ! grep -q "^\s*base:" astro.config.mjs && ! grep -q "@astrojs/tailwind" package.json && grep -q '"@tailwindcss/vite"' package.json && grep -q '"@astrojs/react"' package.json && grep -q '"react": "\^19' package.json && grep -q '"react-dom": "\^19' package.json && grep -q '"tailwindcss": "\^4' package.json && test -f package-lock.json && test ! -f yarn.lock && npm run build > /tmp/build-02-1.log 2>&1 && test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` contains the exact line `site: 'https://Rashmil-1999.github.io'`
    - `astro.config.mjs` contains `output: 'static'`
    - `astro.config.mjs` does NOT contain a top-level `base:` key (grep `^\s*base:` returns nothing)
    - `astro.config.mjs` imports `react` from `@astrojs/react` and `tailwindcss` from `@tailwindcss/vite` and uses both in the appropriate config slots
    - `package.json` contains `"@tailwindcss/vite"` AND does NOT contain `"@astrojs/tailwind"`
    - `package.json` contains `"@astrojs/react"`, `"react": "^19..."`, `"react-dom": "^19..."`
    - Only `package-lock.json` exists at repo root (no `yarn.lock`)
    - `npm run build` exits 0 and writes `dist/index.html`
  </acceptance_criteria>
  <done>`astro.config.mjs` matches Pattern 1 exactly (with the source-attribution comment header); React + Tailwind v4 deps installed and resolving; baseline `npm run build` still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten tsconfig to strictest, verify astro check, verify single-lockfile npm tree, commit</name>
  <files>tsconfig.json, package.json, package-lock.json</files>
  <read_first>
    - tsconfig.json (current — scaffold + `astro add react` modifications)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 8 — tsconfig shape; the 9 flags strictest adds on top of `strict`; Pitfall C — `recommendedTypeChecked` needs `parserOptions.project` but that's Plan 04's concern; Pitfall E — keep `include: ["**/*"]` so `tests/` is reachable for the Plan 03 hydration fixture)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-10 — tsconfig extends strictest; D-09 — hydration fixture path crosses `src`/`tests` boundary)
    - .planning/phases/01-foundation/01-PATTERNS.md (`tsconfig.json` — No analog — new file per Pattern 8)
  </read_first>
  <action>
    Step A — Overwrite `tsconfig.json` so it matches Pattern 8 exactly (RESEARCH.md lines 800–806). The full file contents must be a JSON object with:
    - `"extends": "astro/tsconfigs/strictest"` (NOT `strict` — strictest adds the 9 extra flags per D-10)
    - `"include": [".astro/types.d.ts", "**/*"]` (the `**/*` glob covers `tests/` for the Plan 03 hydration fixture import — Pitfall E mitigation)
    - `"exclude": ["dist", "node_modules"]`

    Do NOT add `compilerOptions` overrides in this plan. Plan 03's hydration fixture (`.tsx`) and Plans 04+05 will adapt rules if `astro check` complains; the goal here is the strictest baseline.

    Step B — Run `npx astro check` from the repo root. Expected: exit 0 with zero diagnostics. The scaffolded `src/pages/index.astro` (Astro "Welcome" placeholder) compiles clean under strictest. If `astro check` reports errors:
    - "Cannot find module 'astro/tsconfigs/strictest'": means `astro` isn't installed. Re-check Task 1.
    - Per-file errors in `src/pages/index.astro`: means the Astro scaffold's placeholder has some quirk under strictest. Fix in place (the placeholder is replaced in Plan 03 anyway, but it must compile NOW for FOUND-02 to pass).
    - Errors about "noUnusedLocals" or "noUnusedParameters" in scaffold-provided files: remove any unused locals/imports.

    Step C — Run `npm ls react react-dom @astrojs/react` and verify the output reports no UNMET DEPENDENCY, no EXTRANEOUS, no INVALID. Expected output: a clean tree with `react@19.x.x`, `react-dom@19.x.x`, `@astrojs/react@5.x.x`. If duplicates exist (e.g., a leftover React 18 from the pre-wipe `node_modules` even though Plan 01 Task 3 ran `rm -rf node_modules`), run `rm -rf node_modules package-lock.json && npm install` to clean-build. (Per RESEARCH.md SC 4 verbiage: "no duplicate or invalid React versions.")

    Step D — Run `npm run build` again as a final sanity check. Confirm `dist/index.html` is regenerated.

    Step E — Commit. Stage `astro.config.mjs`, `tsconfig.json`, `package.json`, `package-lock.json`. Commit message (Conventional Commits, scope `01`):
    `chore(01): wire @astrojs/react + Tailwind v4 + tsconfig strictest`

    Body should mention FOUND-01..04, FOUND-07, FOUND-08 by ID and reference D-10 (strictest) and D-13/Pattern 1.

    Step F — Push: `git push origin main` (no force).
  </action>
  <verify>
    <automated>grep -q '"extends": "astro/tsconfigs/strictest"' tsconfig.json && grep -q '"\*\*/\*"' tsconfig.json && npx astro check 2>&1 | grep -qE 'Result.*0 errors' && npm ls react react-dom @astrojs/react 2>&1 | grep -E 'UNMET|EXTRANEOUS|INVALID' | head -1 | wc -l | grep -q '^[[:space:]]*0$' && npm ls react 2>&1 | grep -qE 'react@19\.' && npm run build > /tmp/build-02-2.log 2>&1 && test -f dist/index.html && git log -1 --pretty=%s | grep -qE '(tailwind|react|tsconfig|strictest)'</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json` contains `"extends": "astro/tsconfigs/strictest"` (literal string)
    - `tsconfig.json` `include` contains `"**/*"` (covers `tests/` directory for Plan 03)
    - `npx astro check` exit code is 0 and the result line shows `0 errors`
    - `npm ls react react-dom @astrojs/react` exit code is 0 (no missing/invalid/extraneous flagged)
    - `npm ls react` shows a `react@19.x.x` entry (NOT 18.x)
    - `npm run build` exits 0
    - `git log -1 --pretty=%B` references the integrations and tsconfig change with Conventional Commits scope `(01)`
    - `git rev-parse @{u}` matches `git rev-parse HEAD` (commit pushed to origin)
  </acceptance_criteria>
  <done>FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-07, FOUND-08 each have their config landing point in place and verified by an automated check. Plan 03 fills the stub sections and the hydration fixture next.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Each `npx astro add` and any explicit `npm install` pulls from the npm registry and writes to `node_modules/` + `package-lock.json` |
| `astro.config.mjs` → `dist/` | Misconfigured `base` or `site` would generate wrong absolute URLs in the build output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | npm install | mitigate | All packages installed here (`@astrojs/react`, `react`, `react-dom`, `tailwindcss`, `@tailwindcss/vite`, `@types/react`, `@types/react-dom`) were cleared by slopcheck in RESEARCH.md Package Legitimacy Audit. No `[ASSUMED]`/`[SUS]` packages. |
| T-01-02-02 | Tampering | Tailwind plugin selection | mitigate | Task 1 explicit assertion that `@astrojs/tailwind` is NOT installed and `@tailwindcss/vite` IS installed (anti-pattern per RESEARCH.md State of the Art) |
| T-01-02-03 | Information Disclosure | site URL | accept | `https://Rashmil-1999.github.io` is the intended public URL; no `base` set per FOUND-08 |
| T-01-02-04 | Denial of Service | build pipeline | mitigate | `astro check` + `astro build` run at end of Task 2 — broken config blocks landing |

| Category | Applies | Rationale |
|----------|---------|-----------|
| Spoofing | no | No auth |
| Tampering | yes | Supply chain (slopcheck-approved); tailwind variant correctness |
| Repudiation | no | Single-developer; git provenance sufficient |
| Information Disclosure | no | All config is non-sensitive |
| Denial of Service | yes | Misconfigured `astro.config.mjs` would break build — `astro check` + `astro build` gate this |
| Elevation of Privilege | no | No execution surface in config files |
</threat_model>

<verification>
- `npx astro check` exit 0 (FOUND-02)
- `npm run build` exit 0 + `dist/index.html` exists (FOUND-01)
- `npm ls react react-dom @astrojs/react` clean (FOUND-07 + FOUND-04 readiness)
- `astro.config.mjs` contains exact strings for `site`, `output`, react, tailwindcss; no `base` key (FOUND-08, FOUND-03, FOUND-04)
- `tsconfig.json` extends strictest (FOUND-02 + D-10)
- One commit landed on origin/main
</verification>

<success_criteria>
- All `must_haves.truths` demonstrably true (verified by automated commands)
- All three `must_haves.artifacts` exist and contain the named markers
- All three `must_haves.key_links` are present in `astro.config.mjs` / `tsconfig.json`
- Phase 1 Requirements satisfied (config landing): FOUND-01, FOUND-02 (TypeScript strictest installed; final SC #2 "passes" gate verified again in Plan 05 CI), FOUND-03 (Tailwind installed via Vite plugin — the `.astro` source-detection marker test is in Plan 05), FOUND-04 (`@astrojs/react` installed — actual hydration fixture in Plan 03), FOUND-07 (single lockfile), FOUND-08 (config strings exact)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done. Record:
- The final `astro.config.mjs` contents (or a diff against Pattern 1)
- The final `tsconfig.json` contents
- The output of `npm ls react react-dom @astrojs/react`
- The commit SHA
- Confirmation that `npx astro check` exits 0
- Open note: hydration fixture (FOUND-04 verification) is created in Plan 03; the React JS chunk in `dist/_astro/` is asserted in Plan 05's smoke test
</output>
