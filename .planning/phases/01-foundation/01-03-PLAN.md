---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
    - 01-02-PLAN.md
files_modified:
    - src/styles/global.css
    - src/layouts/BaseLayout.astro
    - src/components/BaseHead.astro
    - src/components/SideNav.astro
    - src/components/About.astro
    - src/components/Education.astro
    - src/components/Work.astro
    - src/components/Skills.astro
    - src/components/Projects.astro
    - src/components/Leadership.astro
    - src/components/Testimonials.astro
    - src/pages/index.astro
    - src/pages/__hydration-test.astro
    - src/content.config.ts
    - src/assets/.gitkeep
    - tests/__fixtures__/HydrationCheck.tsx
autonomous: true
requirements:
    - FOUND-03
    - FOUND-04
    - FOUND-06
must_haves:
    truths:
        - "`src/pages/index.astro` composes the 8 section stubs in roadmap order: SideNav, About, Education, Work, Skills, Projects, Leadership, Testimonials"
        - "Each of the 8 section stubs renders a `<section>` with the D-23 lowercase id: sidenav, about, education, work, skills, projects, leadership, testimonials"
        - "Exactly one stub (About.astro) carries the Tailwind marker utility `text-[#abc123]`"
        - "`src/styles/global.css` contains `@import \"tailwindcss\";` and an empty `@theme {}` block"
        - "`src/layouts/BaseLayout.astro` imports `../styles/global.css` and renders the BaseHead inside `<head>` and a `<slot />` inside `<body>`"
        - "`src/components/BaseHead.astro` emits ONLY: charset, viewport, title, favicon link, two preconnects — NO OG, NO Twitter, NO canonical, NO description, NO font stylesheet"
        - "`tests/__fixtures__/HydrationCheck.tsx` is a React 19 component with `useState`; `src/pages/__hydration-test.astro` mounts it with `client:load`"
        - "`src/content.config.ts` exists as a placeholder"
        - "`npm run build` produces dist/index.html with all 8 ids, dist/__hydration-test/index.html, dist/_astro/*.js, and dist/_astro/*.css containing `#abc123`"
        - "`npx astro check` exits 0"
    artifacts:
        - path: "src/pages/index.astro"
          provides: "Single page composing 8 section stubs (FOUND-06 markers)"
          contains: "SideNav"
        - path: "src/pages/__hydration-test.astro"
          provides: "React 19 hydration fixture mount (FOUND-04 + D-09)"
          contains: "client:load"
        - path: "src/components/About.astro"
          provides: "About stub carrying the Tailwind marker (D-12)"
          contains: "text-[#abc123]"
        - path: "src/styles/global.css"
          provides: "Tailwind v4 CSS entry (FOUND-03 + D-14)"
          contains: '@import "tailwindcss"'
        - path: "src/layouts/BaseLayout.astro"
          provides: "HTML shell + global.css import + BaseHead (D-13)"
          contains: "../styles/global.css"
        - path: "src/components/BaseHead.astro"
          provides: "Minimal head per D-13"
          contains: "preconnect"
        - path: "tests/__fixtures__/HydrationCheck.tsx"
          provides: "React 19 island fixture using useState (FOUND-04 + D-09)"
          contains: "useState"
        - path: "src/content.config.ts"
          provides: "Empty placeholder for Phase 2 Zod schemas (D-11)"
    key_links:
        - from: "src/pages/index.astro"
          to: "src/layouts/BaseLayout.astro"
          via: "import + JSX wrap"
          pattern: "BaseLayout"
        - from: "src/pages/index.astro"
          to: "the 8 section stubs"
          via: "import statements"
          pattern: "from '../components/"
        - from: "src/pages/__hydration-test.astro"
          to: "tests/__fixtures__/HydrationCheck.tsx"
          via: "import + client:load"
          pattern: "client:load"
        - from: "src/layouts/BaseLayout.astro"
          to: "src/styles/global.css"
          via: "side-effect import"
          pattern: "../styles/global.css"
---

<objective>
Author the Astro file set per CONTEXT.md D-07..D-14 and D-23: layout + minimal BaseHead + 8 empty section stubs (About carrying the Tailwind marker) + React 19 hydration fixture + Tailwind v4 entry CSS + empty `content.config.ts` placeholder. Wire `src/pages/index.astro` to compose the stubs in roadmap order.

Purpose: Materializes everything Plan 05's smoke test will assert: 8 lowercase section ids in `dist/index.html`, hydration page in `dist/__hydration-test/`, React chunk in `dist/_astro/`, Tailwind marker `#abc123` in `dist/_astro/*.css`. Closes the structural half of FOUND-04 (hydration island shipped) and FOUND-06 (the 8-section markers exist). Sets section-id baseline at the D-23 lowercase values: `sidenav` (NOT `sideNav`) and `work` (NOT `experience`).

Output: 16 new files committed in one Conventional Commits commit on `main`.
</objective>

<execution_context>
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rashmilpanchani/Documents/Rashmil-1999.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author layout, BaseHead, global.css, content.config.ts, src/assets/.gitkeep</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/components/BaseHead.astro, src/content.config.ts, src/assets/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 for global.css/BaseLayout/BaseHead shape; D-13 boundary; D-14 boundary)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11 src/ layout; D-13 BaseHead minimal; D-14 global.css minimal; D-17 Prettier tabWidth 4)
    - .planning/phases/01-foundation/01-PATTERNS.md (Pattern source table; Shared Patterns — file-header source-attribution comments)
    - src/pages/index.astro (current scaffold "Welcome" placeholder — Task 3 overwrites it; just confirm it exists)
  </read_first>
  <action>
    File 1 — `src/styles/global.css` per RESEARCH.md Pattern 2 (lines 437–445).
    Contents required:
    - Comment header naming `tailwindcss.com/docs/installation/framework-guides/astro` and `CONTEXT.md D-14`
    - The directive `@import "tailwindcss";` (exact string, double quotes)
    - An empty `@theme { }` block with a TODO comment referencing Phase 3 STYLE-04 brand tokens
    - NO base reset, NO brand colors, NO font tokens (D-14 excludes those)

    File 2 — `src/layouts/BaseLayout.astro` per RESEARCH.md Pattern 2 (lines 449–472).
    Frontmatter (between `---` fences) must:
    - Import BaseHead from `'../components/BaseHead.astro'`
    - Side-effect-import `'../styles/global.css'`
    - Declare `interface Props { title: string }`
    - Destructure `const { title } = Astro.props;`
    Template must emit:
    - `<!doctype html>`
    - `<html lang="en">`
    - `<head>` containing `<BaseHead title={title} />`
    - `<body>` containing `<slot />`
    Include a source-attribution comment header in the frontmatter naming `docs.astro.build/en/basics/layouts/` and `CONTEXT.md D-13`. Indent 4 spaces per D-17.

    File 3 — `src/components/BaseHead.astro` per RESEARCH.md Pattern 2 (lines 474–492).
    Frontmatter:
    - `interface Props { title: string }`
    - Destructure `Astro.props`
    Template emits ONLY these tags in this exact order:
    1. `<meta charset="utf-8" />`
    2. `<meta name="viewport" content="width=device-width, initial-scale=1" />`
    3. `<title>{title}</title>`
    4. `<link rel="icon" href="/favicon.svg" />`
    5. `<link rel="preconnect" href="https://fonts.googleapis.com" />`
    6. `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />`

    Do NOT emit OG/Twitter/canonical/description/font-stylesheet — those land Phase 4 SEO-01 and Phase 3 STYLE-05 (D-13). Comment header names `CONTEXT.md D-13`.

    File 4 — `src/content.config.ts` is an EMPTY placeholder per D-11. Contents: a single TypeScript comment naming `CONTEXT.md D-11` and pointing at Phase 2 (where Zod schemas land). Do NOT export anything, do NOT import `defineCollection`. The file exists so Phase 2 can edit-in-place.

    File 5 — `src/assets/.gitkeep` is an empty file (zero bytes) to materialize the empty `src/assets/` directory on disk and in git (D-11).

    Verify after writing: run `npx astro check` — must exit 0 with 0 errors.
    Do NOT commit yet — Task 4 commits the whole plan rollup.
  </action>
  <verify>
    <automated>test -f src/styles/global.css && grep -q '@import "tailwindcss"' src/styles/global.css && grep -q '@theme' src/styles/global.css && test -f src/layouts/BaseLayout.astro && grep -q "from '../components/BaseHead.astro'" src/layouts/BaseLayout.astro && grep -q "'../styles/global.css'" src/layouts/BaseLayout.astro && grep -q 'slot' src/layouts/BaseLayout.astro && test -f src/components/BaseHead.astro && grep -c 'preconnect' src/components/BaseHead.astro | grep -q '^2$' && ! grep -q 'og:' src/components/BaseHead.astro && ! grep -q 'twitter:' src/components/BaseHead.astro && ! grep -q 'canonical' src/components/BaseHead.astro && ! grep -q 'name="description"' src/components/BaseHead.astro && test -f src/content.config.ts && test -f src/assets/.gitkeep && npx astro check 2>&1 | grep -qE 'Result.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/global.css` exists and contains `@import "tailwindcss";` and an `@theme {` block
    - `src/layouts/BaseLayout.astro` imports `../styles/global.css` and `BaseHead from '../components/BaseHead.astro'`, declares Props with `title: string`, renders `<slot />` inside `<body>`
    - `src/components/BaseHead.astro` contains exactly 2 `preconnect` lines, a `<title>{title}</title>` block, and a favicon link
    - `src/components/BaseHead.astro` does NOT contain `og:`, `twitter:`, `canonical`, or `name="description"` (greps return zero matches)
    - `src/content.config.ts` and `src/assets/.gitkeep` exist
    - `npx astro check` exits 0
  </acceptance_criteria>
  <done>Layout + BaseHead + global.css + content.config.ts placeholder + assets directory all in place per RESEARCH.md Pattern 2 and D-13/D-14. All files type-check under strictest.</done>
</task>

<task type="auto">
  <name>Task 2: Author 8 section stubs with D-23 ids (About carries the Tailwind marker)</name>
  <files>src/components/SideNav.astro, src/components/About.astro, src/components/Education.astro, src/components/Work.astro, src/components/Skills.astro, src/components/Projects.astro, src/components/Leadership.astro, src/components/Testimonials.astro</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 composition; D-08 empty stub shape; D-12 marker on one stub; D-23 lowercase id list)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 — stub shape; Pitfall B — marker is the Pitfall 29 mitigation)
    - .planning/phases/01-foundation/01-PATTERNS.md (8 Section Stubs block — id divergence table; About.astro is the marker-carrier recommendation)
  </read_first>
  <action>
    Author 8 separate `.astro` files. Each is a complete component with a short frontmatter comment and one `<section>` element.

    Section id assignment (LOCKED — D-23 lowercase ids):

    | File | id value |
    |------|----------|
    | src/components/SideNav.astro | sidenav |
    | src/components/About.astro | about |
    | src/components/Education.astro | education |
    | src/components/Work.astro | work |
    | src/components/Skills.astro | skills |
    | src/components/Projects.astro | projects |
    | src/components/Leadership.astro | leadership |
    | src/components/Testimonials.astro | testimonials |

    For 7 of the 8 stubs (all EXCEPT About): the template body is `<section id="${id}"></section>` — a literal empty section element (Pattern 3 lines 509–516).

    For About.astro: the template body is `<section id="about" class="text-[#abc123]"></section>`. This is the D-12 Tailwind marker. The class MUST be the exact string `text-[#abc123]`. Plan 05's smoke test greps `dist/_astro/*.css` for the literal `#abc123`.

    Comment header per file:
    - All 8: `CONTEXT.md D-08: empty stub. Phase 3 fills with real content.`
    - About.astro additional line: `CONTEXT.md D-12: this component carries the Tailwind marker utility (Pitfall 29 mitigation).`
    - SideNav.astro additional line: `CONTEXT.md D-23: id is "sidenav" (lowercase) — diverges from the deleted Navbar.jsx "sideNav" id.`
    - Work.astro additional line: `CONTEXT.md D-23: id is "work" — diverges from the deleted Work.jsx "experience" id. The resumeData.json snapshot still has "#experience" in links[]; Phase 2 re-derives nav links from Content Collections.`

    No Bootstrap classes, no content text, no roles, no markup beyond the literal `<section>`. Phase 3 fills these in place.

    Indent 4 spaces per D-17.

    Verify after authoring: `npx astro check` exits 0.
    Do NOT commit yet.
  </action>
  <verify>
    <automated>for f in SideNav About Education Work Skills Projects Leadership Testimonials; do test -f src/components/$f.astro || (echo "MISSING: $f.astro" && exit 1); done && grep -q 'id="sidenav"' src/components/SideNav.astro && grep -q 'id="about"' src/components/About.astro && grep -q 'id="education"' src/components/Education.astro && grep -q 'id="work"' src/components/Work.astro && grep -q 'id="skills"' src/components/Skills.astro && grep -q 'id="projects"' src/components/Projects.astro && grep -q 'id="leadership"' src/components/Leadership.astro && grep -q 'id="testimonials"' src/components/Testimonials.astro && grep -q 'text-\[#abc123\]' src/components/About.astro && ! grep -l 'text-\[#abc123\]' src/components/SideNav.astro src/components/Education.astro src/components/Work.astro src/components/Skills.astro src/components/Projects.astro src/components/Leadership.astro src/components/Testimonials.astro && ! grep -lr 'id="experience"' src/components/ && ! grep -lr 'id="sideNav"' src/components/ && npx astro check 2>&1 | grep -qE 'Result.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - All 8 stub files exist at the listed paths
    - Each file's `<section>` has the exact lowercase id from the D-23 table (greps verify all 8 ids)
    - About.astro contains the literal `text-[#abc123]` (D-12 marker)
    - The other 7 stubs do NOT contain `text-[#abc123]` (only About carries the marker)
    - No stub contains `id="experience"` or `id="sideNav"` (D-23 divergence respected)
    - `npx astro check` exits 0
  </acceptance_criteria>
  <done>8 minimum-viable section stubs authored with D-23 ids; About.astro carries the marker per D-12. Phase 3 fills these in place later.</done>
</task>

<task type="auto">
  <name>Task 3: Author index.astro composition + hydration fixture (HydrationCheck.tsx + __hydration-test.astro)</name>
  <files>src/pages/index.astro, src/pages/__hydration-test.astro, tests/__fixtures__/HydrationCheck.tsx</files>
  <read_first>
    - src/pages/index.astro (current scaffold "Welcome" placeholder — overwrite entirely)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 for index.astro composition; Pattern 4 for hydration fixture; Pitfall E — cross-tree import works because tsconfig include is `**/*`)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 composition; D-09 hydration fixture path and `client:load`; D-22 hydration page ships in the same build)
    - .planning/phases/01-foundation/01-PATTERNS.md (Pattern 4 — `client:load` intentional for verification fixture per Pitfall 5)
    - tsconfig.json (confirm Plan 02 Task 2 set `include: [".astro/types.d.ts", "**/*"]` — otherwise Pitfall E will block the build)
  </read_first>
  <action>
    File 1 — `src/pages/index.astro` per RESEARCH.md Pattern 3 (lines 519–544). Overwrite the Astro scaffold placeholder entirely.

    Frontmatter imports in this exact order:
    1. `BaseLayout from '../layouts/BaseLayout.astro'`
    2. `SideNav from '../components/SideNav.astro'`
    3. `About from '../components/About.astro'`
    4. `Education from '../components/Education.astro'`
    5. `Work from '../components/Work.astro'`
    6. `Skills from '../components/Skills.astro'`
    7. `Projects from '../components/Projects.astro'`
    8. `Leadership from '../components/Leadership.astro'`
    9. `Testimonials from '../components/Testimonials.astro'`

    Template:
    - `<BaseLayout title="Rashmil Panchani">` wrapping all content
    - First child: `<SideNav />`
    - Then a `<main>` containing the 7 content sections in roadmap order: About, Education, Work, Skills, Projects, Leadership, Testimonials

    Indent 4 spaces per D-17. Comment header names `CONTEXT.md D-07` and a source URL.

    File 2 — `tests/__fixtures__/HydrationCheck.tsx` per RESEARCH.md Pattern 4 (lines 553–567). React 19 functional component that:
    - Imports `useState` from `'react'`
    - Default-exports a function `HydrationCheck` with NO props
    - Renders one `<button>` with attributes `type="button"`, `onClick={() => setN(n + 1)}`, `data-testid="hydration-check"`, and text content `Hydration count: {n}`

    The `type="button"` attribute is mandatory — jsx-a11y rules (Plan 04) require it and strictest already gates on similar issues. Comment header names `react.dev/reference/react/useState` and `CONTEXT.md D-09 + FOUND-04`.

    File 3 — `src/pages/__hydration-test.astro` per RESEARCH.md Pattern 4 (lines 570–582). Frontmatter:
    - `BaseLayout from '../layouts/BaseLayout.astro'`
    - `HydrationCheck from '../../tests/__fixtures__/HydrationCheck.tsx'` (cross-tree import per D-09 + Pitfall E)

    Template:
    - `<BaseLayout title="Hydration test">` wrapping `<HydrationCheck client:load />`

    `client:load` is intentional per RESEARCH.md Pattern 4 + Pitfall 5 (production code defaults to no directive; this is the verification island and we want unambiguous proof the React chunk ships). Comment header names `CONTEXT.md D-09` and notes "Phase 5 may delete this fixture (deferred)".

    After authoring all three files, run `npm run build`. Expected:
    - `dist/index.html` exists and contains `id="sidenav"`, `id="about"`, `id="education"`, `id="work"`, `id="skills"`, `id="projects"`, `id="leadership"`, `id="testimonials"`
    - `dist/__hydration-test/index.html` exists
    - At least one `.js` file in `dist/_astro/`
    - At least one `.css` file in `dist/_astro/` contains the literal `#abc123`

    If `dist/_astro/*.css` does NOT contain `#abc123` (Pitfall 29 firing): add `@source "../**/*.{astro,html,ts,tsx}";` immediately after the `@import "tailwindcss";` line in `src/styles/global.css` and rebuild (RESEARCH.md Pitfall B escalation path).

    Run `npx astro check` — must exit 0. Cross-tree TS import works because tsconfig include is `**/*`.

    Do NOT commit yet.
  </action>
  <verify>
    <automated>test -f src/pages/index.astro && grep -q "import BaseLayout from '../layouts/BaseLayout.astro'" src/pages/index.astro && for c in SideNav About Education Work Skills Projects Leadership Testimonials; do grep -q "import $c from '../components/$c.astro'" src/pages/index.astro || (echo "MISSING IMPORT: $c" && exit 1); done && grep -q '<SideNav />' src/pages/index.astro && grep -q '<main>' src/pages/index.astro && test -f tests/__fixtures__/HydrationCheck.tsx && grep -q "useState" tests/__fixtures__/HydrationCheck.tsx && grep -q 'type="button"' tests/__fixtures__/HydrationCheck.tsx && test -f src/pages/__hydration-test.astro && grep -q 'client:load' src/pages/__hydration-test.astro && grep -q "from '../../tests/__fixtures__/HydrationCheck.tsx'" src/pages/__hydration-test.astro && npm run build > /tmp/build-03-3.log 2>&1 && test -f dist/index.html && test -f dist/__hydration-test/index.html && for id in sidenav about education work skills projects leadership testimonials; do grep -q "id=\"$id\"" dist/index.html || (echo "MISSING ID in dist/index.html: $id" && exit 1); done && ls dist/_astro/*.js > /dev/null 2>&1 && grep -l '#abc123' dist/_astro/*.css > /dev/null 2>&1 && npx astro check 2>&1 | grep -qE 'Result.*0 errors'</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` imports BaseLayout and all 8 section components by name
    - `src/pages/index.astro` renders `<SideNav />` and a `<main>` wrapping the 7 content sections
    - `tests/__fixtures__/HydrationCheck.tsx` exists with `useState` import + `type="button"` attribute
    - `src/pages/__hydration-test.astro` contains `client:load` and the cross-tree import path
    - `npm run build` exits 0
    - `dist/index.html` contains all 8 lowercase ids (verified by 8 separate greps)
    - `dist/__hydration-test/index.html` exists
    - At least one `.js` file in `dist/_astro/`
    - At least one `.css` file in `dist/_astro/` contains the literal `#abc123`
    - `npx astro check` exits 0
  </acceptance_criteria>
  <done>index.astro composes the 8 stubs; hydration fixture mounts via `client:load`; build produces all artifacts Plan 05's smoke test will assert. FOUND-04 (React 19 hydration) and FOUND-06 (8-section markers) structurally complete.</done>
</task>

<task type="auto">
  <name>Task 4: Commit the section + layout + fixture file set</name>
  <files>(commit-only; no new files)</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (Conventional Commits style; repo uses `docs(01): ...` / `chore(...)` scope)
  </read_first>
  <action>
    Stage all 16 files from Tasks 1–3:

    `git add src/styles/global.css src/layouts/BaseLayout.astro src/components/BaseHead.astro src/components/SideNav.astro src/components/About.astro src/components/Education.astro src/components/Work.astro src/components/Skills.astro src/components/Projects.astro src/components/Leadership.astro src/components/Testimonials.astro src/pages/index.astro src/pages/__hydration-test.astro src/content.config.ts src/assets/.gitkeep tests/__fixtures__/HydrationCheck.tsx`

    Commit with Conventional Commits message (heredoc per parent commit protocol):
    `feat(01): scaffold layouts, 8 section stubs, hydration fixture, Tailwind entry`

    Commit body should reference: D-07 (composition), D-08 (empty stubs), D-09 (hydration fixture mount), D-12 (Tailwind marker), D-13 (BaseHead minimal), D-14 (global.css minimal), D-23 (lowercase ids).

    Push: `git push origin main`.
  </action>
  <verify>
    <automated>git diff HEAD~1 HEAD --name-only > /tmp/last-commit-files.txt && grep -q 'src/styles/global.css' /tmp/last-commit-files.txt && grep -q 'src/layouts/BaseLayout.astro' /tmp/last-commit-files.txt && grep -q 'src/components/About.astro' /tmp/last-commit-files.txt && grep -q 'src/pages/index.astro' /tmp/last-commit-files.txt && grep -q 'src/pages/__hydration-test.astro' /tmp/last-commit-files.txt && grep -q 'tests/__fixtures__/HydrationCheck.tsx' /tmp/last-commit-files.txt && git log -1 --pretty=%s | grep -qE '(stub|hydration|scaffold|section)' && git log -1 --pretty=%s | grep -q '(01)' && [ "$(git rev-parse HEAD)" = "$(git rev-parse @{u})" ]</automated>
  </verify>
  <acceptance_criteria>
    - The most recent commit (HEAD) introduces the listed files (verified by `git diff HEAD~1 HEAD --name-only`)
    - The commit subject contains the Conventional Commits scope `(01)` and references stubs/hydration/scaffold
    - `git rev-parse HEAD` equals `git rev-parse @{u}` (commit pushed to origin/main)
  </acceptance_criteria>
  <done>One commit on `main` containing all 16 layout/stub/fixture files. Plan 04 (lint/format/husky) and Plan 05 (vitest + CI) run after this.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/` → build output (`dist/`) | Markup in stubs becomes hydratable HTML; client:load on the hydration fixture creates an entry point for shipped JS |
| `tests/` → `src/` (cross-tree import) | The hydration fixture lives in `tests/` but the `__hydration-test.astro` page imports it; Astro must process it via Vite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | hydration fixture page (`/_astro/*.js` chunk) | accept | The `__hydration-test` route is intentionally public (D-09); contains no secrets, no PII; Phase 5 may remove it. |
| T-01-03-02 | Information Disclosure | section ids | accept | Section ids `sidenav`, `about`, etc. are intentionally public (anchor-nav targets) — no sensitive surface |
| T-01-03-03 | Denial of Service | build pipeline (Tailwind v4 source detection) | mitigate | D-12 marker test in Plan 05; Pitfall B escalation (`@source "..."`) is documented if marker is missing |
| T-01-03-04 | Tampering | cross-tree TS import | mitigate | tsconfig include `**/*` (set in Plan 02 Task 2) covers `tests/`; Pitfall E mitigation acknowledged |

| Category | Applies | Rationale |
|----------|---------|-----------|
| Spoofing | no | No authentication surface |
| Tampering | yes (low) | Static markup; primary tampering vector is supply chain — already mitigated upstream |
| Repudiation | no | Single-dev repo; git history sufficient |
| Information Disclosure | no | Stubs are empty; hydration fixture is intentional public test artifact |
| Denial of Service | yes (low) | Misconfigured Tailwind source-detection would break build — D-12 marker is the canary |
| Elevation of Privilege | no | No runtime privilege surface |
</threat_model>

<verification>
- `npm run build` exits 0
- `dist/index.html` contains all 8 lowercase ids: sidenav, about, education, work, skills, projects, leadership, testimonials
- `dist/__hydration-test/index.html` exists
- `dist/_astro/` contains at least one `.js` chunk
- `dist/_astro/*.css` contains the literal `#abc123` (Pitfall 29 mitigation verified)
- `npx astro check` exits 0
- One commit landed on origin/main with the 16-file payload
</verification>

<success_criteria>
- All 10 `must_haves.truths` demonstrably true
- All 8 `must_haves.artifacts` exist on disk
- All 4 `must_haves.key_links` are present (verified by grep patterns in `<verify>`)
- Structural completion of FOUND-04 (hydration island ships) and FOUND-06 (8 section markers exist for Plan 05's smoke test). Final FOUND-04/FOUND-06 verification happens in Plan 05's smoke test against the build output.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Record:
- List of the 16 files committed and the commit SHA
- The output of `find dist -maxdepth 2 -type f` after the final build
- Confirmation that Pitfall 29 did NOT fire (the `@source` escalation was NOT needed), OR a note if it did fire and `@source` was added
- Confirmation that `npx astro check` exits 0 and `npm run build` exits 0
- Note: Plan 04 will add ESLint/Prettier/husky; Plan 05 will add Vitest + CI workflow. Once Plan 05's smoke test passes, FOUND-04 and FOUND-06 are FULLY closed.
</output>
